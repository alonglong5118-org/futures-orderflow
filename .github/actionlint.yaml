# actionlint 配置
# 忽略 shellcheck info/style 级别（阻塞级只看 warning/error）
shellcheck:
  enabled: true
  # SC2086 (info): glob quoting — 影响小，保持原样
  # SC2129 (style): 重定向风格 — 不影响功能
  ignore: |
    shellcheck:.*SC2086.*info
    shellcheck:.*SC2129.*style
