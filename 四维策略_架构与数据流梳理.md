# 四维策略盯盘模型 · 架构与数据流梳理（v3.0.0 · 2026-08-23 · 自动参数优化 + 市场状态机 + 账户自愈 + 盘前简报 + 盈亏归因 + 异常/熔断/回撤 + 校准中心 + 信号拆解）

> 本文档是四维策略系统的**逐文件精读收口**：架构总览 + 数据流 + 文件职责矩阵 + 模块精读 + 数据文件清单 + HTTP API 一览 + 关键算法细节 + 已知坑。
> 定位：半自动信号系统（**不自动下单**，出信号 → 手动开平 → 面板记账复盘）。
> 运行：`four_dim_live_runner.py`（后端）+ `four_dim_live.html`（前端），端口 **8741**；订单流真 tick 生产者 `backend_tqsdk.py` 独立进程，端口 **8742**。

---

## 一、架构总览

```
┌────────────────────────────────────────────────────────────────────────┐
│                              行情层（数据源）                            │
│  minishare rt_fut_k(60s快照) → 实时价 / 5m合成 / C_flow 差分            │
│  akshare(免费源) → 日线兜底 / 基本面F(基差+库存) / 龙虎榜              │
│  本地缓存 → 主连日线 CSV / 5m CSV / live_bars.json                      │
│  backend_tqsdk.py(独立进程·8742) → 真 tick → tick_stream.jsonl         │
│    ├─ TICK_FEED_ENABLED=1 时 runner tail 灌进 FEED.flow[sym]            │
│    ├─ CZCE(FG/SA) 交易所逐笔真 tick；DCE(JM/J/jd/lh) akshare 分钟线近似  │
│    └─ 休市日读本地缓存回放，避免空 tail 抖动                            │
└───────────────┬────────────────────────────────────────────────────────┘
                │ df_daily / df_5m / F / C_flow / cpos
                ▼
┌────────────────────────────────────────────────────────────────────────┐
│                       核心引擎 four_dim_strategy.py                     │
│  F(背景偏置) → T(主触发,8策略+regime) → C(确认,流量+持仓)              │
│  bias_G = 0.6·T + 0.25·F + 0.15·C  →  pipeline 触发判定               │
└───────────────┬────────────────────────────────────────────────────────┘
                │ triggered / dir_T / pipe
                ▼
┌────────────────────────────────────────────────────────────────────────┐
│                     风控层（4 道硬闸 + 仓位状态机 + 组合级硬熔断）          │
│  risk_gate: T阈值 / corr闸 / 涨跌停距 / 仓位上限 N_plan                 │
│  risk_state_machine: NORMAL→WARNING→LOCKED（连亏0.8^n / 回撤8%降档）   │
│  KillSwitch(#5): 回撤15%/日亏8%/连亏6笔 → 全平+禁开+永不自动恢复       │
│  portfolio_risk_check(#6): 板块集中度 / 单边净敞口 / 相关性同向桶       │
└───────────────┬────────────────────────────────────────────────────────┘
                │ passed + N_plan + exit_plan + exec_plan(#7)
                ▼
┌────────────────────────────────────────────────────────────────────────┐
│                    信号层 runner.evaluate() 每轮调度                     │
│  去抖(同号连续2轮) + 签名去重 + 通知(弹窗/语音/手机推送#15) + 聊天流     │
└───────────────┬────────────────────────────────────────────────────────┘
                │ state dict（JSON）
                ▼
┌────────────────────────────────────────────────────────────────────────┐
│          前端 four_dim_live.html（12 书签 · 黑金工业玻璃风）             │
│  账户/信号/席位/交易/异动/预警监控/复盘/绩效/消息/健康（3s 轮询 /api/state）│
└────────────────────────────────────────────────────────────────────────┘
```

---

## 二、核心数据流（一次信号如何产生）

1. **主循环** `main()`：每 `POLL_SEC` 秒，若在交易时段则 `_poll_feed(feed)` → `evaluate(...)` → `_update_aux(...)`，最后 `json.dump(state)` 写 `four_dim_live_state.json`。
2. **行情 poll** `minishare_live.poll()`：`rt_fut_k(ts_code="*")` 一次拉全市场 990 合约快照，更新 `last_snap`、5m 桶聚合 `bars`、`FlowAggregator` 差分。
3. **单品种评估** `evaluate()` 对 55 品种逐一：
   - `load_daily_refreshed(sym)` 日线 → `feed.get_5m(sym)` 5m → `score_F` → `feed.c_flow(sym)` → `pipeline(...)`。
   - `pipeline` 判定 `triggered`；过 `_in_session`、状态机未锁、去抖(连续2轮同向)、签名去重后 → `risk_gate` + `exit_plan` + `build_signal`。
   - 通知 + `append_chat`（消息流）+ `log_signal`（信号日志）+ 写 `state["symbols"]` / `state["signals"]`。
4. **辅助更新** `_update_aux()`：异动扫描 → 账户监控 auto_sync → 状态机更新 → 触价报警 → 移动止损 → 自选到价(B1) → 跳空风险(B3) → 风险热度(B4) → 经纪商回灌扫描(#9) → 数据质量监控(#14) → 纪律体检(#12) → 事件日历闸门(#13) → 复盘卡 + 收盘快照。
5. **前端** `refresh()` 3s 轮询 `/api/state` 渲染书签；主 `refresh()` 改用 `Promise.allSettled([...])` **并发**拉取（v2.0.2 前为串行 `await` 逐接口，慢接口拖累整屏）。复盘页 F1 多策略表 **+30s 轮询**、报警历史 **+15s 轮询**（P2-3 子模块自动刷新）；「健康」页数据 30s 轮询。配色/语义统一（P1）：`--red:#ff4757` 仅表盈利/上涨/多头/买入，`--green:#2ed573` 仅表亏损/下跌/空头/卖出，`--amber:#ffa502` 表风险/告警，修正西方配色反向（entry=红/exit=绿）。

---

## 三、文件职责矩阵

| 文件 | 类型 | 职责 | 被谁引用 |
|---|---|---|---|
| `four_dim_live_runner.py` | 主程序 | HTTP 服务 + 调度主循环 + 信号生成 evaluate + 预警 B1/B2/B3/C2/C3/C4 + 触价/移动止损/风险热度 + #1–#17 全部能力 | 入口（启动即 run）|
| `fundamental_metrics.py` | 指标 | **G1** 利润/价差/比价 7 项指标（latest/mean30/zscore/trend），`fund_metrics(force,cache_sec)` 模块级缓存 | 被 runner import 为 fm |
| `four_dim_live.html` | 前端 | 12 书签面板，3s 轮询 /api/state + 各子接口 | 浏览器 |
| `four_dim_strategy.py` | 核心引擎 | SYMBOLS/配置/F/T/C/pipeline/risk_gate/exit_plan/build_signal/walk_forward 回测 | 被 runner import 为 fd |
| `strategy_layer.py` | 策略层 | 8 策略 + atr/rsi/sma + classify_regime + compute_strategy | 被 four_dim_strategy import |
| `risk_state_machine.py` | 风控状态机 | NORMAL→WARNING→LOCKED + scale() + **KillSwitch 硬熔断(#5)** | 被 runner import 为 rsm |
| `account_tracker.py` | 账户追踪 | account_state.json 持仓/权益/动态权益快照 | 被 runner import 为 at |
| `account_monitor.py` | 账户监控 | TqSdk 只读账户读取 → auto_sync 驱动 papertrack | 被 runner import 为 am |
| `trade_journal.py` | 成交记录器 | 开平仓记录 + 手续费净盈亏 + R倍数 + 绩效 + 净值曲线 | 被 runner import 为 tj |
| `discipline_review.py` | 复盘 | 日/周/月纪律卡 + 收盘快照落账 | 被 runner import 为 dr |
| `fundamental_feed.py` | 基本面 | 基差+库存+季节性 → compute_F → fundamentals.json | 被 four_dim_strategy import 为 ff |
| `long_hu_bang.py` | 龙虎榜 | 五交易所前20会员持仓 → C_score → cpos_cache.json | 独立运行（每日16:30自动化）|
| `anomaly_scan.py` | 异动扫描 | 涨跌幅+振幅排名 | 被 runner import 为 asc |
| `minishare_live.py` | 实时行情 | rt_fut_k 快照 + 5m合成 + C_flow | 被 runner import 为 ml |
| `tick_orderflow.py` | 订单流(#3) | 盘口 tick → Delta / 吸收 / 失衡 → Δ∈[-100,100] → `push_tick` 混入 C_flow | 被 runner import 为 tof |
| `hidden_pivot.py` | 独立工具 | 隐秘枢轴价位（未接入四维主流程）| 无 |
| `minishare_feed.py` | 遗留模块 | da龘 全品种报价（被 minishare_live 取代）| 无 |
| `preflight_check.py` | 一次性自检 | 管住手侧 CTP 网关预检（属管住手产品）| 无 |
| `four_dim_calibrate.py` | 重校准扫描器 | walk-forward 扫 T 阈值，dry-run 提议最优 T，不自动改源码 | 工具 |
| `four_dim_recalibrate.py` | 在线重校准 | calibration_drift.json 标记真实崩盘品种，双源 T 同步 + **#1 止损止盈联合校准** | 工具 |
| `four_dim_papertrack.py` | papertrack | 真实成交 vs 信号评分 + 动态门控（胜率/累计R 驱动自动暂停）| 被 runner import |
| `execution_planner.py` | 执行计划器(#7) | 按流动性档位×每分钟量给拆片/冰山/TWAP 建议 | 被 runner import 为 exp |
| `broker_import.py` | 回灌(#9) | 结算单/成交明细 CSV 解析 + 去重落账 | 被 runner import 为 bi |
| `perf_breakdown.py` | 绩效分解(#10) | 9 维切片 R 统计 + 砍切片建议（7 基础：方向/时段/品种/板块/出场原因/持仓时长/星期；`with_regime` 再加 市场状态、状态×方向）| ⚠️ **纯 CLI 工具，runner 未 import、面板无端点**（须手动跑）|
| `montecarlo.py` | 蒙特卡洛(#11) | R 序列 bootstrap → 权益分位带 + 终值/回撤分布 | 被 runner import 为 mc |
| `blunder_check.py` | 纪律体检(#12) | 扫 journal 揪违规，A–D 扣分评级 | 被 runner import 为 bc |
| `event_calendar.py` | 事件日历(#13) | 宏观/产业/交易所事件 + 减仓/禁开闸门 | 被 runner import 为 ec |
| `data_quality.py` | 数据质量(#14) | 行情陈旧/冻结/跳变/异常监控 | 被 runner import 为 dq |
| `push_notify.py` | 手机推送(#15) | Telegram/Bark/企业微信三通道 | 被 runner import 为 pn |
| `backtest_viz.py` | 回测可视化(#17) | 水下曲线 + 逐笔 R 散点 | 被 runner import 为 bv |
| `watchdog.sh` / `start.sh` | 看门狗(#16) | 崩溃重启 + 卡死健康检查 + 启动入口 | shell |
| `backend_tqsdk.py` | 订单流生产者(Stream3) | 独立进程·端口 8742：TqSdk 订阅真实 tick → 写 `tick_stream.jsonl`（CZCE FG/SA 真 tick；DCE JM/J/jd/lh akshare 分钟线近似）；Tailer 断点自愈；休市回放 | 被 runner `TICK_FEED_ENABLED=1` tail 灌入 FEED.flow |
| `start_tqsdk.sh` / `stop_tqsdk.sh` | 启动/停止脚本(Stream3) | `nohup` 拉起/杀掉 `backend_tqsdk.py`，写 pid 文件，stdout 落 `tqsdk.log` | shell |
| `minishare_permission_check.py` | 一次性自检 | minishare 权限探查（已完成）| 无 |
| `gen_papertrack_html.py` | 可视化工具 | `papertrack_report.json` → 自包含 HTML（零依赖零联网，内联 JS/SVG）| CLI，无 |
| `diag_p02_gaps.py` | 诊断脚本 | P0-2：扫已判定信号前向窗口的真实跳变幅度，验换月跳空阈值是否曾触发（只读）| CLI，无 |
| `verify_p02.py` | 校验脚本 | P0-2：从冻结 `backtest_bars` 重放 `backtest_signal`，验 outcome/R 可复现 + 跳空跳过数为 0（只读）| CLI，无 |
| `info_dimension.py` / `fetch_info_dimension.py` | **#1 信息维度** | 采集器（系统3.9+akshare，限产/疫情/USDA 等）→ `info_dimension.json`；读取器（3.13 stdlib）`f_override_for()` → `F_override` 喂 live pipeline（红线：不喂回测）| 读取器被 runner import 为 idim |
| `macro_context.py` / `fetch_macro_context.py` | **#6 跨资产宏观语境** | 采集器（系统3.9+akshare：沪深300/中证500/国债10Y/美元人民币/铜现货）→ `macro_context.json`；读取器（3.13 stdlib）`compute()` → `macro_bias=0.4·equity+0.3·rate+0.3·fx` 注入 pipeline | 读取器被 runner import 为 mctx |
| `regime_hmm.py` | **#7 HMM 市场状态** | numpy+hmmlearn GaussianHMM(4) 对日线 [log_ret, rolling_vol] 聚类 → trend_up/trend_down/choppy/high_vol；`compute_label(sym,df)` 模型缓存、数据增>60 根才重训；无 hmmlearn 退化规则分桶 | 被 runner import 为 rhmm（回测不要调用）|
| `gbm_garch.py` | **#7 GBM/GARCH 波动率动力学** | GARCH(1,1) MLE（自写负对数似然 + scipy minimize L-BFGS-B）+ EWMA(RiskMetrics) 退化；GBM 蒙特卡洛前向（n_sims=2000，horizon 5/10/20）→ exp_ret/VaR95/P_up/价格区间；vol_state（low/normal/high/extreme）→ THR_MULT + RISK_SCALE；per-sym 缓存 | 被 runner import 为 gg（回测不要调用）|
| `signal_explain.py` | **#4 信号解释** | `explain_signal(sig,pipe)` 纯 stdlib 确定性解释器：summary（结论先行一句话）+ bullets（技术/基本面含信息nudge/资金流/风控手数/校准漂移）+ llm_prompt；`llm_explain` 仅当 `DEEPSEEK_API_KEY` 存在才 urllib 调 DeepSeek（8s 超时失败回退 None）| 被 runner import 为 sexp |
| `consistency_watchdog.py` | **#5 一致性看门狗** | `check_consistency(focus,disabled)` 检测 4 类：train_serve_divergence（服务 T 偏离基线>35%）/ unvalidated（缺 mean_oos）/ broken_serving（drift=broken 仍服务）/ stale（recalibrated_at>30天）。**只报告不修正** | 被 runner import 为 cwd |
| `refresh_main_contracts.py` | **合约权威采集 + 换月告警** | 系统3.9+akshare 全市场扫真实主力（5 交易所重试）→ 单调向前写 `main_overrides.json` + `minishare_hot_contracts.json`（不回退旧月）；异常监测：交易所源失败(FETCHFAIL)/异常旧月(ABNOLD)/临界当月(CURRMONTH) → `main_contract_refresh.log` + `main_contract_alert_state.json` + osascript macOS 通知（分级冷却：ABNOLD 6h / 其他 7天仅状态变化推）| runner `_update_aux` 5.4 段每 2h subprocess 触发 |

---

## 四、模块精读

### 4.1 four_dim_strategy.py（核心引擎）

- **SYMBOLS**：55 品种（含纯碱逐合约 SA09/SA01），按交易所+板块分组。
- **DISABLED_SYMBOLS**：**11 个**校准判死刑品种（OOS 负期望），实盘/回测均跳过出信号，仅保留卡片占位。2026-08-17 全市场 OOS 日线版应用最终态：解禁 JM/ag/bu/ss（整改后日线转正 + Δ>+0.02 + 样本≥30 + 5m 不退化 + 回撤改善），加禁 MA/PR（整改后日线 on 负且较 v12 退化，PR 另 5m 双负）。最终集合 = `{au, i, eg, m, a, b, rr, RM, hc, MA, PR}`。其中 **仅 hc 为自适应恢复候选**（`AUTO_RECOVER_SYMBOLS={hc}`——JM 已解禁故仅留 hc）——保留品种定义不删，每 3600s 跑 `recovery_check`（walk-forward expR≥0 且 胜率≥45% 且 样本≥10）转正则自动从 `RUNTIME_DISABLED` 解禁 + 通知；其余（au/i/eg/m/a/b/rr/RM/MA/PR）为硬禁永不自动恢复。
- **合约映射**：`_CONTRACT_AKSHARE={"SA09":"SA2609","SA01":"SA2701"}`、`VARIETY_OF`（合约→品种级 F/C）、`_CONTRACT_CPOS_KEY`（SA09→SA609 逐合约龙虎榜）。
- **DEFAULT_CONFIG**：`account`（权益/风险%/保证金上限/手数上限/组合上限）、`risk_gate`（止损ATR倍数/rr_ratio/涨跌停临近系数/连亏锁定/滑点）、`contract_specs`（55 品种乘数/保证金率/涨跌停/手续费）、`thresholds`（7 板块阈值）、`regime_coef`（趋势/震荡/波动系数）、`thresholds_by_symbol`（per-symbol 校准覆盖）。

关键函数与算法：
| 函数 | 逻辑 |
|---|---|
| `compute_T(df)` | 8 策略 regime 加权合成：`T = sign(score/Σ|w| ×100)`，封顶 ±100 |
| `combine_bias(F,T,C)` | **`bias_G = 0.6·T + 0.25·F + 0.15·C`** |
| `pipeline(...)` | T_D 算 regime → F/C → bias_G → T_5m(≥60根5m用5m否则T_D降频代理) → `dir_T=sign(T_5m)` → 阈值×regime系数 → 同向放行 + hard_veto + `|T_5m|≥T_thresh_eff` → triggered；C 确认；corr 闸门 |
| `risk_gate(...)` | 4 闸：N_risk=⌊权益×风险%/每手止损额⌋（最小1手）；N_margin=⌊权益×30%/每手保证金⌋；N_plan=min(N_risk,N_margin,max_lots)；gate3=止损距<涨跌停×0.9 |
| `exit_plan(...)` | 止损=`stop_atr_mult×ATR×regime系数`；t1=1R平半、t2=2R全平；震荡单批、趋势/波动两批+移动止损 |
| `FlowAggregator` | 60s 快照差分：`flow=dP×dOI`（价×持仓变动），近 window 个分量的累计方向归一 → C_flow∈[-100,100]；需≥3 个差分才出值（暖机期 C=0）|
| `walk_forward_backtest` | 逐 bar 用截至当日数据跑 pipeline，下根开盘入场，止损/t2 出场，扣费扣滑点算 R_adj；冷却 5 根日线；滑点用 `2 × get_slip_pts / sd` 双向扣 |
| `get_slip_pts` / `LIQUIDITY_SLIP` | **流动性敏感滑点**：A/B/C 档（1.0/1.5/2.0 点）按近 60 日日均成交额数据驱动重排；已改**大小写不敏感**（查 `.lower()`），消除 `"EB"` vs `"eb"` 静默回退全局 1.0 的 bug |
| `recovery_check` / `AUTO_RECOVER_SYMBOLS` | 被禁品种绕开 DISABLED 直接 walk_forward_backtest，近期窗口转正（expR≥0 且 胜率≥45% 且 样本≥10）则 `recover=True`，供 runner 自适应解禁 |

### 4.2 strategy_layer.py（8 策略）

| 策略 | 类型 | 信号逻辑 |
|---|---|---|
| ma_break | 趋势 | 收盘>MA20>MA60 多，反空 |
| dma | 趋势 | MA5/MA20 金叉死叉 |
| turtle | 趋势 | 破 20 日高点且 >55 日低点 多 |
| donchian | 趋势 | 收盘≥20 日最高/≤20 日最低 |
| pullback | 趋势 | MA20>MA60 且回踩偏离<2% 多 |
| boll | 均值回归 | 收盘≤下轨 多 / ≥上轨 空 |
| rsi | 均值回归 | RSI≤30 多 / ≥70 空 |
| seasonal | 季节性 | 同月平均日收益符号 + z 显著性 |

- `TREND_STRATS`=前5，`MEAN_STRATS`=[boll,rsi]，`seasonal` 单独。
- `classify_regime`：ATR占比>2.5%→波动；MA偏离<0.8%且ATR<1.2%→震荡；MA斜率+偏离→趋势；否则"过渡"。
- `compute_strategy`：是 da龘 原版综合入口（含 walk_forward 稳健池门槛），四维**只复用其 8 策略 + classify_regime + atr**，不走 compute_strategy 的仓位预算（四维用自己的 risk_gate）。

### 4.3 risk_state_machine.py（仓位状态机 + 组合级硬熔断）

- 常量：红线 45%、日亏停机 5%、预警 40%、单笔 30%、回撤触发 8%、LOCKED 冷却 300s、WARNING 冷却 120s、连亏缩放 0.8^n 封底 0.2。
- `risk_guard`：用权益/已占用保证金/当日盈亏算 OK/WARN/LOCK + 原因。
- `RiskStateMachine.update`：状态流转 + 回撤降档；**`scale()`**：LOCKED=0（禁开）、WARNING=0.5、连亏×0.8^n。
- **#5 KillSwitch（硬熔断）**：`KILL_DRAWDOWN=15%` / `KILL_DAILY_LOSS=8%` / `KILL_CONSEC_LOSSES=6`；`KillSwitch.check()` 触发即生成全平清单 + 禁开 + **永不自动恢复**，落盘 `killswitch_state.json` 防重启洗白；唯一出口 `reset()`（人工解除，可重置峰值）。与软降档状态机分工：软层可自动恢复、硬层必须人工干预。
- **已知坑（已修）**：`update()` 持锁内调 `summary()` → 必须 `threading.RLock()`，否则死锁冻结主循环。
- **P0 修复（2026-08-17）**：补初始化 `self.daily_loss_locked = False`（原漏初始化，`update()` 读该属性时触发 `AttributeError` 致状态机异常）；重启验收 FRESH_OK。

### 4.4 account_tracker.py（账户追踪）

- `record_trade(open/add/close/reduce)`：维护 `account_state.json` 持仓（方向/手数/均价/止损/t1/t2），平仓/减仓记已实现盈亏。
- `set_equity`：同步真实权益，记录 `realized_pnl_at_sync` 作动态权益基准。
- `snapshot(prices)`：**动态权益 = 同步权益 + (当前已实现 − 同步时已实现) + 当前浮动盈亏合计**；资金使用率/距组合上限/单品占用% 全部基于动态权益（上下板块同源）。

### 4.5 account_monitor.py（账户监控，可选）

- 后端可插拔（account_monitor.json）：`tqsdk`（TqAccount 只读）或 `manual`。
- `auto_sync`：账户权益→set_equity；持仓镜像对账（新开→record_entry+open，已平→record_exit+close），`_synced_open` 去重。
- 当前 `account_monitor.json` enabled=false → 维持手动记账。

### 4.6 fundamental_feed.py（基本面 F）

- `fetch_basis_history` / `fetch_inventory`：akshare 拉基差/库存 → `fundamentals.json`。
- `compute_F`：**F = 0.6×基差分 + 0.2×库存趋势分 + 0.2×现货季节性**。基差 back(正)看多，`|rate|≥0.10` 封顶 ±100；库存降库+25/累库−25；季节性（鸡蛋7-9中秋+35、生猪11-1腌腊+30、玻璃9-10+20、纯碱9-10+15）。
- 缺失 → F=0（中性不阻断）。

### 4.7 long_hu_bang.py（龙虎榜 C_pos）

- 五交易所：CZCE/SHFE/DCE/GFEX/INE，只抓四维覆盖品种。SHFE/INE 直接解析交易所官方 JSON（前20会员净持仓口径，绕开 akshare 解析缺陷）。
- `compute_c_score`：**C_score = 0.75×净变化分 + 0.25×绝对净持仓分**（净变化为主信号，§1.3 规格）。
- `run()`：抓今天，失败回溯 4 天；逐合约（SA609/SA701 等）也抓，写 `cpos_cache.json`（含 30 天 history）。
- **每日 ~16:30 由自动化触发**，非 runner 实时。

### 4.8 anomaly_scan.py（异动扫描）

- `compute(snaps)`：评分 = `0.7×|日内涨跌幅| + 0.3×振幅`（涨跌幅优先昨收，否则用开盘）。返回 top_up/top_down 各前 N。

### 4.9 minishare_live.py（实时行情）

- `MinishareLiveFeed`：`rt_fut_k` 60s 快照 → `last_snap` + 5m 桶聚合 `bars`（近 200 根）+ `FlowAggregator` 差分。
- **品种映射坑**：JM=焦炭主连、JMM=焦煤主连（`_PINNED` 已修正）；具体合约 SA09/SA01 按 ts_code 数字匹配（中文名都叫"纯碱"无法区分）。
- **合约动态解析（2026-08-14 根治写死月份盯市价过时）**：`contract_specs[sym]["contract"]` 不再写死具体月份。`_refresh_hot_contracts(df)` 在每次 `poll()` 后按持仓量(OI)排名解析各品种当前主力/次主力可交割合约（排除主连/连续合成系列），写入 `HOT_CACHE`（`minishare_hot_contracts.json` 持久化）；`_apply_hot_contracts()` 仅在当前合约跌出 OI 前二（真换月）时重钉（防 flicker）并同步安全阀 `self._auth`；`_authoritative_contracts()` 据此返回生效合约，`contract_of(sym)` 供前端展示。`contract_mode=auto_main`(默认)/`auto_secondary`/`fixed`。
- `available()`：无 token/import 失败返回 False → runner 降级运行（面板照常，仅无实时信号）。

### 4.10 trade_journal.py（成交记录器）

- `record_entry/exit`：开平仓记录；**扣手续费**（`_leg_fee`=价×乘数×手数×费率），`pnl`=净盈亏。
- **G2 交易笔记**：`record_entry` 成交 dict 新增 `note`（默认空）；`update_trade(trade_id, fields)`（白名单 `note`/`exit_reason`，RLock 包裹读改写）支持事后补备注；`/api/journal_list` 返回含 `id`/`note` 的成交明细，前端交易记录卡「备注」按钮 `POST /api/journal {action:"note"}` 写入。
- **G4 日盘/夜盘绩效** `session_performance()`：按平仓时段（日盘 / 夜盘，夜盘含次日 00:00–02:30）聚合盈亏 / 胜率 / 笔数，含 `by_symbol` 逐品种拆分；为绩效页「日盘 / 夜盘绩效分解」卡供数。
- `summary()`：总笔数/胜率/总盈亏/期望值/连胜连亏/R倍数（`R=净盈亏/计划风险`）/R分布/分品种/日夜盘分解。
- `equity_curve()`：净值曲线 + 当前回撤%/持续天数/新高状态。
- `performance_metrics()`：Sharpe(估算)/Calmar/利润因子。
- `today_pnl()` / `current_loss_streak()`：驱动状态机日亏停机线/连亏降档。
- **执行质量 `_fill_quality()`**：信号 `entry_ref`（信号时刻价）vs 实际成交价 → 不利滑点（多买高/空卖低）/ 超预算滑点 / 滑点占 R% / 成交时机(min) / 手数偏离比 / 出场质量 / 执行偏差标记；`compare_to_papertrack()` 新增 `fill_quality` 聚合（n_scored / avg_adverse_slip_pts / avg_excess_slip_pts / avg_timing_min / avg_size_ratio / poor_execution），由 `/api/journal` 的 `compare` 返回，前端交易记录卡展示。

### 4.11 discipline_review.py（复盘）

- `_card(kind)`：日/周/月纪律卡，含评分(A–D) + C1–C6 六维检查（无冲动开仓/锁死不开仓/单品不超上限/组合不超上限等）。
- `snapshot_day/week/month`：收盘冻结落账（每日 23:20 日卡 → 周五 15:10 周卡 → 月末 15:10 月卡）。
- `_RECORD_CUTOFF_DATE="2026-08-11"`：list 三函数过滤旧数据，回看下拉只显示 8/11 及以后。

### 4.12 four_dim_live_runner.py（主程序）

- **HTTP 端点**（见 §六）：GET 状态/账户/风险/异动/信号/席位/复盘/绩效/相关性/价差/压力/热度/预警5类/导出 + POST 交易/日志/自选/持仓位/通知/复盘快照。
- **evaluate()**：单品种评估 + 去抖(同号连续2轮) + 签名去重 + 状态机锁 + 风控 + 出场 + 信号通知。
- **_update_aux()**：异动 + 账户同步 + 状态机 + 触价报警 + 移动止损 + 自选到价(B1) + 跳空风险(B3) + 风险热度(B4) + 复盘卡 + 收盘快照。
- **止损基准** `_compute_stop_atr`：日盘用日线 ATR，夜盘用 30min resample ATR（5m 太小、日线太大），样本不足回退日线×0.40，地板≥日线×0.25。
- **自适应恢复** `recovery_tick()`（挂主循环，每 `RECOVERY_SEC=3600` 一次）：对 `RUNTIME_DISABLED ∩ AUTO_RECOVER_SYMBOLS` 跑 `recovery_check`，转正则 `RUNTIME_DISABLED.discard` + `notify_recover`；`evaluate()` 查 `RUNTIME_DISABLED`（非模块级 DISABLED），pipe 标 `recoverable` + 区分文案。
- **移动止损** `manage_trailing_stops`：盈利≥1R→止损上移保本；≥2R→标"达t2"全平。只调面板止损位，不向经纪商下单。
- **G3 重校准自动调度** `_recalibrate_tick()`（挂主循环 `_update_aux`，受 `auto_recalibrate`/`recalibrate_interval_h` 控制）：周期调用 `fdr.main(apply=False)` + `fdc.recalibrate_report([...])`（均 try/except 隔离），检测校准漂移后自动跑重校准扫描并落盘，不再仅限 CLI 工具箱。
- **G5 关注度盯盘板** `focus_board(force=False)`：5 维打分（信号活跃度 30% / 波动 20% / 持仓 20% / 价差偏离 15% / 风险热度 15%），聚焦 `FOCUS_SYMS`（jd/lh/FG/SA/JM/J）6 品种；`/api/focus` 返回各品种打分 + 快照。
- **G1 利润/价差/比价** 接入 `import fundamental_metrics as fm`；`/api/fund_metrics` 返回 7 项指标（latest/mean30/zscore/trend）。
- **日/夜盘单一真值源**：`SYMBOLS` 每品种注入 `night` 字段（G4）；`_in_session(sym, now)` 改读 `SYMBOLS.get(sym,{}).get("night", True)`，消除 `NO_NIGHT`（信号推送时段）与 `_NIGHTLESS`（止损 ATR 基准）双集合漂移；`NIGHT_SYMS` 由 `night` 字段派生。
- **新端点**：`/api/fund_metrics`(G1) / `/api/focus`(G5) / `/api/journal_session`(G4 日盘夜盘) / `/api/journal_list`(G2 成交明细含 id/note)；`POST /api/journal` 新增 `note` action → `tj.update_trade(_tid, {"note": _note})`。
- **非交易时段通知门控（2026-08-17）**：新增 `_market_open_now()` + `_should_suppress(topic)`，非交易时段静默信号/报警推送（`_should_push` 单测覆盖）；持仓触价、自选到价等高优先级报警不受影响。
- **止损止盈方向校验（2026-08-17）**：根因——止损方向配置入口无校验，多空方向反填致止损/止盈方向错乱。在 `trade_journal.record_entry` + `account_tracker._auto_levels` 返回前 + 最终 `_validate_levels` 三层补方向校验；成功修复时静默配置异常通知（避免重复刷屏）。

### 4.13 新增模块（#7/#9/#10/#11/#12/#13/#14/#15/#17）

| 模块 | 关键函数 | 逻辑 |
|---|---|---|
| `execution_planner`(#7) | `plan_execution(symbol,lots,price,direction,urgency)` | 按流动性档位参与率（1.0档3%/1.5档2%/2.0档1%）×每分钟量算单片上限（帽 40/15/6），超单片拆多片、≥3 片切冰山（只露1片）；返回 `{slices, slice_lots, interval_sec, style, iceberg}` |
| `broker_import`(#9) | `scan()/import_file()/_apply_fill()` | 结算单/成交明细 CSV 中文表头别名归一 + `contract_to_symbol` + tid 去重（`broker_import_state.json`）；apply=True 才落账 journal+account_tracker |
| `perf_breakdown`(#10) | `full_report()` | 方向/时段/品种/板块/市场状态/出场原因/持仓时长/星期/状态×方向 9 维切 R 统计，输出 best/worst/cut 切片 |
| `montecarlo`(#11) | `simulate()` | bootstrap 重抽样 R 序列 → 权益分位带(5/25/50/75/95) + 终值 p5/p50/p95 + 最大回撤 p50/p95 + 盈利/破产概率；样本<8 自动回退 papertrack |
| `blunder_check`(#12) | `check()` | 扫 journal：无信号开仓/无止损/超手数/频繁反手/连亏加仓/单笔≥3R 大亏 → 扣分 A–D |
| `event_calendar`(#13) | `upcoming()/gate()/scale_factor()` | 宏观(非农/CPI/EIA/FOMC)+产业(钢联/USDA/生猪)+交易所事件，事件前 1h 禁开、2h 减半 |
| `data_quality`(#14) | `observe()/check()` | 自我追踪各品种最后见到时间，检测陈旧/冻结(价格卡死)/异常(≤0)/跳变(>5%) |
| `push_notify`(#15) | `push()/push_alert()/configure()` | Telegram Bot / Bark(iOS) / 企业微信 webhook 三通道，配置走 `push_config.json` 或环境变量 |
| `backtest_viz`(#17) | `data()` | 读 papertrack_report.json（回退 journal）→ 权益曲线 + 水下回撤 + 逐笔 R 散点 |

### 4.14 认知层与合约权威模块（v2.4.0 新增）

| 模块 | 关键函数 | 逻辑 |
|---|---|---|
| `info_dimension`(#1) | `refresh()/f_override_for(sym)` | 读 `info_dimension.json`（外部采集）+ `info_dimension_manual.json`（人工）→ 信息分 F_info（[-100,100] 量程，INFO_F_WEIGHT=10.0、夹断 ±100）→ runner main 信号路径算 `F2=idim.f_override_for()` 传 `pipeline(F_override=F2)`，写 `pipe["F_raw"/"F_info"/"info_adj"/"F_source"]`；`_update_aux` 每 30min refresh |
| `macro_context`(#6) | `compute()/refresh()` | 近20日变化 → `macro_bias=0.40·equity_mom+0.30·rate_trend+0.30·fx_trend`（各自 tanh 归一夹断 [-1,1]）；`pipeline(macro_label=mb)` 中 `bias_G = bias_G + mb·_MACRO_BIAS_SCALE(12.0)` 温和微调；返回 `macro_bias/macro_ctx` |
| `regime_hmm`(#7) | `compute_label(sym,df)` | GaussianHMM(4) 拟合日线 [log_ret, rolling_vol20] → 最可能态映射 trend_up/trend_down/choppy/high_vol；`pipeline(hmm_label=)` 按态乘 T_thresh_eff（trend ×0.90 / choppy ×1.15 / high_vol ×1.25）；模型缓存 `_MODELS`，数据增>60 根重训；无 hmmlearn → 规则分桶 |
| `gbm_garch`(#7) | `compute(sym,df)` | GARCH(1,1) MLE（scipy L-BFGS-B）→ garch_vol/持续性/半衰期；GBM 前向（2000 路径 mean-revert 到无条件方差，5/10/20 日）→ exp_ret/VaR95/P_up/[lo,hi] 区间；vol_state 分位定档 → THR_MULT(0.97~1.12) + RISK_SCALE(0.6~1.0)；per-sym 缓存（数据增>60 重拟）；EWMA 退化仍出统计 |
| `signal_explain`(#4) | `explain_signal(sig,pipe)` | 确定性解释器（结论先行 + 技术/基本面含信息nudge/资金流/风控手数/校准漂移 bullets + llm_prompt）；runner build_signal 后 `sig["explanation"]=...`；白名单/last_signal 含 explanation |
| `consistency_watchdog`(#5) | `check_consistency(focus,disabled)` | 检测 4 类问题（train_serve_divergence>35% / unvalidated 缺 mean_oos / broken_serving / stale>30天），只报告不修正；`/api/consistency` 查询（tick 缓存 + `?action=refresh` 重算 + last_run）|
| `refresh_main_contracts`(合约权威) | `main()/ak_main_all()/_alert_if_needed()` | 全市场 55 品种真实主力（akshare 5 交易所重试）→ **单调向前**写 `main_overrides.json`（`if cur_ym and am_ym < cur_ym: 不回退`）；异常监测 FETCHFAIL/ABNOLD/临界当月 → 日志 + 状态文件 + osascript 通知（分级冷却）|

---

## 五、数据文件清单

| 文件 | 结构 | 写入方 | 读取方 |
|---|---|---|---|
| `four_dim_live_state.json` | 面板状态（updated/session/order/symbols/signals/chat/anomaly/risk_state/discipline）| runner 每轮 | /api/state |
| `account_state.json` | equity/realized_pnl/positions/equity_synced | account_tracker | snapshot/状态机 |
| `trade_journal.json` | trades[]（开平仓流水）| trade_journal | summary/绩效 |
| `four_dim_signals.json` | 信号日志 | runner log_signal | compare_to_papertrack |
| `cpos_cache.json` | 龙虎榜（C_score/net/history）| long_hu_bang | score_C/cpos_ranking |
| `fundamentals.json` | 基差/库存序列 | fundamental_feed | compute_F |
| `calibration_params.json` | 53 品种校准（mean_oos/full_expR/T_thresh/robust）| four_dim_calibrate | variety_edge/effective_params |
| `trade_config.json` | 账户参数/合约规格/通知 | 手动/notify 接口 | account_tracker/risk_gate |
| `account_monitor.json` | 账户后端配置（tqsdk）| 手动 | account_monitor |
| `minishare.json` | token/interval | 手动 | minishare_live |
| `minishare_cache.json` | 品种→代码映射缓存 | minishare_live 自动 | minishare_live |
| `live_bars.json` | 实时 5m 合成 K 线（跨重启持久化）| minishare_live | feed.get_5m |
| `watchlist.json` | 自选到价提醒 | watch_add/update | watch_view/check_watch_alerts |
| `alert_history.json` | 报警历史（最多300条）| log_alert | load_alerts |
| `signal_chat_feed.jsonl` + 按日 `_YYYY-MM-DD.jsonl` | 消息流（按天归档）| append_chat | load_chat_feed / /api/chat |
| `signal_dedup_state.json` | 信号去重记忆（重启共享）| evaluate | load_dedup_state |
| `discipline_records/weekly/monthly_records.json` | 复盘日/周/月落账 | snapshot_* | list_records |
| `discipline_events.json` | 开仓纪律事件 | discipline_review | 锁死判定 |
| `info_dimension.json` | 信息维度（限产/疫情/USDA 采集）| fetch_info_dimension（系统3.9）| info_dimension.refresh（3.13）|
| `info_dimension_manual.json` | 人工补录信息维度 | 手动 | info_dimension |
| `macro_context.json` | 宏观语境（沪深300/国债/美元/铜近60日）| fetch_macro_context（系统3.9）| macro_context.refresh（3.13）|
| `main_overrides.json` | **合约权威覆盖**（sym→真实主力 ts_code）| refresh_main_contracts（每2h）| minishare_live 三路径 + runner /api/state |
| `calibration_drift.json` | 漂移检测 + staging 候选 proposed_T | four_dim_recalibrate（每6h）| /api/recalibrate |
| `main_contract_refresh.log` / `main_contract_alert_state.json` | 换月刷新日志 / 告警去重状态 | refresh_main_contracts | 审计 + 告警冷却 |

---

## 六、HTTP API 一览

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/` `/index.html` | 前端 HTML（每次 GET 重读文件，改 HTML 刷新即生效）|
| GET | `/api/state` | 主状态（55 品种信号卡 + 信号流 + 聊天流 + 风控 + 复盘）|
| GET | `/api/account` | 动态权益 + 持仓表（含 heat 热度）|
| GET | `/api/risk` | 仓位状态机 |
| GET | `/api/anomaly` | 异动扫描 |
| GET | `/api/signals` | 信号列表 |
| GET | `/api/cpos_rank?date=` | 席位态度榜（龙虎榜）|
| GET | `/api/journal` | 绩效 summary + performance + equity_curve |
| GET | `/api/correlation` `/api/spread` `/api/stress` `/api/heat` | 组合相关性/价差/压力测试/风险热度 |
| GET | `/api/watch` `/api/rollover` `/api/gap` `/api/edge` `/api/alerts` | 预警监控 5 类 |
| GET | `/api/chat_days` `/api/chat?date=` | 消息流按天归档 |
| GET | `/api/discipline?list/date/week/month` | 复盘卡 |
| GET | `/api/export?type=&kind=` | CSV 导出 |
| GET | `/api/killswitch` `/api/batch?mode=` `/api/broker` | #5 硬熔断状态/确认/解除 · #8 一键全平/反手 · #9 经纪商回灌 |
| GET | `/api/health` | #16 看门狗健康检查（uptime/last_cycle/feed）|
| GET | `/api/montecarlo` `/api/blunder` `/api/calendar?hours=` `/api/dataq` `/api/push` `/api/backtest_viz` | #11 蒙特卡洛 · #12 纪律体检 · #13 事件日历 · #14 数据质量 · #15 推送状态 · #17 回测可视化 |
| GET | `/api/decompose?symbol=` | **#2 模型健康分解**：`decompose_model_health` 双视角定位 F/T/C 谁在退化（①留一消融算各维边际 expR 贡献 ②实际成交上该维投票方向一致性）。**带 server 级内存缓存**（每品种算一次要跑 4 次 walk-forward，很重）|
| POST | `/api/trade` | 开/加/减/平仓 + 同步权益 |
| POST | `/api/journal` | 成交记录器开平（v2.0.1 起**同步写 account_tracker**，entry 未带止损自动 `_auto_levels` 填）；新增 `action:"note"` → `update_trade(id, {"note": ...})` 写成交备注（G2） |
| GET | `/api/fund_metrics` | **G1** 利润/价差/比价 7 项指标（latest/mean30/zscore/trend，300s 缓存） |
| GET | `/api/focus` | **G5** 6 关注品种（jd/lh/FG/SA/JM/J）5 维关注度打分 + 快照 |
| GET | `/api/journal_session` | **G4** 日盘/夜盘分时段绩效（盈亏/胜率/笔数 + by_symbol） |
| GET | `/api/journal_list` | **G2** 成交明细列表（含 id/note），供前端备注编辑定位 |
| POST | `/api/watch` | 自选到价增删改 |
| POST | `/api/position_levels` | 设止损止盈 |
| POST | `/api/notify` | 通知开关 |
| POST | `/api/push` | #15 推送通道配置 / 测试 |
| POST | `/api/discipline/snapshot` | 手动复盘落账 |
| GET | `/api/consistency` | **#5 一致性看门狗**：`check_consistency(focus,disabled)` 4 类问题报告（`?action=refresh` 重算；tick 缓存；只报告不修正）|
| GET | `/api/recalibrate` | **#3 漂移闭环**：view 漂移报告+staging 状态；`?action=stage` 后台异步扫描（对关注品种 drift/broken 高置信者 staged `proposed_T`，不落盘）；POST `?action=apply&symbol=J|__all__` 一键落盘（备份→写 T_thresh→清 `_CALIB_CACHE`）|

---

## 七、关键算法细节（原盲区已补齐）

1. **T_D 合成**：`compute_T` = 8 策略信号 × regime 权重，归一化到 ±100。权重：趋势(趋势策略1.0/均值0.3/seasonal0.2)、震荡(趋势0.3/均值1.0/seasonal0.3)、波动(趋势0.5/均值0.2/seasonal0.1)、过渡(全0.5)。
2. **bias_G**：`0.6·T_D + 0.25·F + 0.15·C`。F/C 中性时退化为 T 主导。
3. **触发判定**：`dir_T=sign(T_5m)`；须 `bias_G` 同向（或中性）、无 hard_veto（`|bias_G|≥bias_hard` 且反向）、`|T_5m|≥T_thresh_eff`（T_base × regime 系数）。T_5m 优先 5m（≥60根），否则 T_D 降频代理。
4. **C_flow**：`FlowAggregator` 用 `dP×dOI`（价×持仓变动）作净流代理，近 window 个分量累计方向归一 ±100，叠加 da龘 tick（0.7/0.3）。**需 ≥3 个差分才出值（暖机期 C=0）**。（v2.0.2 Stream3 升级后）主源为 `backend_tqsdk.py` 写出的真 tick（`tick_stream.jsonl`），`TICK_FEED_ENABLED=1` 时 runner 自动 tail 灌进 `FEED.flow[sym]`；**CZCE(FG/SA) 走交易所逐笔真 tick**，**DCE(JM/J/jd/lh) 走 akshare 分钟线近似**（标注 `akshare_min`，分钟级非逐笔）；未启用时回退原 minishare 5m 差分代理。
5. **C_pos**：`C_score = 0.75×净变化分 + 0.25×绝对净持仓分`，净变化按总持仓 2% 封顶、绝对净按 10% 封顶。
6. **F**：`0.6×基差分 + 0.2×库存趋势分 + 0.2×季节性`。
7. **risk_gate 手数**：`N_plan = min(N_risk, N_margin, max_lots)`，N_risk 最小 1 手（超预算标注⚠️）。
8. **手续费净盈亏**：`_leg_fee = 价×乘数×手数×费率`（`_FEE_RATE` 按品种），`pnl = gross − open_fee − close_fee`。
9. **移动止损**：盈利≥1R→止损上移保本；≥2R→"达t2"全平。只调面板位，不向券商下单。

---

## 八、已知坑与约定（务必遵守）

1. **止损基准 = 30min ATR + 日线兜底**：5m 太小（秒扫）、日线太大；绝不可改回 5m。
2. **开仓自动算 levels**：`doJournalEntry`→后端 `_auto_levels`，前端已移除手动填区。
3. **信号下拉防串品种**：按「品种+方向」过滤。
4. **动态权益同源**：上下板块都基于 `snapshot()` 的动态权益。
5. **状态机 RLock**：`update()` 内调 `summary()` 必须 RLock，否则死锁。
6. **minishare 命名坑**：JM=焦炭、JMM=焦煤（`_PINNED` 已修正）。
7. **C_flow 需暖机**：≥3 差分才有值；开盘初期 C=0。
8. **实盘 5m 需暖机**：≥60 根才走真 5m 触发，否则退化为 T_D。
9. **B2 换月仅 CONTRACT_MAP 内品种**：其它持仓返回空，前端显示「—」。
10. **后端改动需重启**：`launchctl kickstart -k gui/$(id -u)/com.ken.futures-orderflow.live`；HTML 改动刷新即生效。
11. **自测手段**：`/tmp/test_orderflow2.py`(30断言) + `batch2`(22) + `batch3`(15)，改完必跑 + `node --check` 验前端 JS。
12. **数据源边界**：minishare 仅 rt_fut_k 可用；历史日线/基本面仍走 akshare/sina（token 无对应权限）。
13. **global 坑（v2.0.1 修）**：函数内给模块级变量赋值（如 `last_recover`、`DAY_OPEN_EQUITY`）必须加 `global`，否则 Python 视为局部变量、首次读即 UnboundLocalError——改 runner 时务必检查。
14. **交易记录↔账户总览已打通（v2.0.1）**：`/api/journal` 的 entry/exit 会同步写 account_tracker；「交易记录」录入不再只写 journal。两套文件（trade_journal.json / account_state.json）的职责边界已消除。
15. **`update_trade` 必须 RLock（G2 修）**：原 `_LOCK = threading.Lock()` 不可重入，`update_trade` 套锁内又调 `_load`/`_save`（二者也加锁）→ 同线程二次加锁永久阻塞、进程挂起被 SIGKILL；已改 `_LOCK = threading.RLock()`（可重入、跨线程仍互斥）。
16. **日/夜盘单一真值源（G4 修）**：`SYMBOLS` 每品种注入 `night` 字段；删除旧 `_NIGHTLESS`（残留引用会让模块级 `NameError` 致整个 runner 导入失败）；`_in_session` 改读 `SYMBOLS[sym].night`，`NIGHT_SYMS` 由 `night` 派生。改日/夜盘映射只动 `SYMBOLS` 一处。
17. **TICK_STREAM_FILE 断点自愈（Stream3 修）**：`backend_tqsdk.py` 的 Tailer 读 `tick_stream.jsonl` 时，若 `size < self.last_read`（文件被 truncate 重写）则置 `self.last_read = 0` 从头续读，避免 rewind 后读偏移卡死、整屏订单流冻结。未启用 Stream3（`TICK_FEED_ENABLED=0`）时该逻辑不触发，面板退回原 minishare 5m 差分代理。
18. **JM 合约错配修复（P2-4 修）**：`_xs_recent_signal_price` 曾被 TTL 上线前的老信号永久判「未过期」，永远返回冻结价 1323 与 JM2609 最新收盘 1515 比对，偏差 −12.7% 持续误报合约错配。**根因**：信号记录无合约月份字段 + 老信号无 `valid_minutes` 被 `annotate_signal_ttl` 当作永不过期。修复：跳过过期/历史信号；无 `valid_minutes` 的老信号用 `DEFAULT_SIGNAL_TTL_MIN=120` 作过期基准；重启验收通过。
19. **🔴 主力合约权威覆盖（用户最高优先级铁律，v2.4.0）**：面板/feed 合约必须永远=真实主力（akshare 权威源），**不可停、不可回退旧月、不可假自动**。数据流：`main_overrides.json`（权威层）→ `minishare_live` 三解析路径（`_authoritative_contracts`/`_refresh_hot_contracts`/`_apply_hot_contracts`）**优先采用** → `/api/state` **每次请求实时解析**（不再 setdefault+CONTRACT_MAP 的 300s 滞后）。**磁盘 `minishare_hot_contracts.json` 仅启动时读一次，运行时写入对它无影响**——验证必须看 `/api/state` 的 `symbols[].contract` 实时字段，绝不能只看脚本跑没跑/JSON 写没写。持仓品种（AP/FG/SA/c/rb）钉死开仓合约、SA09/SA01 fixed——故意不滚，属正确设计。
20. **认知层红线（#1/#6/#7，v2.4.0）**：信息维度 F_override、宏观 macro_label、HMM garch_label/gbm_garch **只喂 live 信号**；回测三处调用只传 date= → 零前视污染。
21. **校准缓存坑（#3）**：`four_dim_strategy._load_calib_params()` 把 `calibration_params.json` 缓存在模块级 `_CALIB_CACHE`（永不失效）；改文件后必须 `fd._CALIB_CACHE.clear()` 才即时生效（apply 端点已内置清缓存）。
22. **换月告警分级冷却（v2.4.0）**：ABNOLD（异常旧月）6h 冷却；FETCHFAIL/临界当月 7 天且仅状态变化才推（不刷屏）——INE/原油源当前持续挂，属已知 akshare 接口问题，优雅降级保留上次值、不误报。

---

*—— 四维策略 架构与数据流梳理 v2.5.0 · 2026-08-18（风控口径修复 heat_pct 预算 · 看门狗语义精确化 broken_gated · 持仓对账防漂移 positions_reconcile · 面板版本动态化 APP_VERSION）*

*末次实测核对：2026-08-18 —— 在 2026-08-17 v2.3.0 之上新增：§三 文件职责矩阵补 9 个新模块/产物；§四.14 认知层与合约权威模块精读（#1/#6/#7/#4/#5/refresh_main_contracts）；§五 数据文件补 info_dimension/macro_context/main_overrides/calibration_drift/告警状态 5 类；§六 HTTP API 补 /api/consistency + /api/recalibrate；§八 已知坑补 19~22（合约权威覆盖铁律、认知层零前视红线、校准缓存坑、换月告警分级冷却）。live runner PID 6880 在线（2026-08-18 08:20 健康检查 55 品种）。*

---

## v3.0.0 升级要点（2026-08-23 · 全量自检通过）

> 运行系统 `APP_VERSION=v3.0.0`（runner + 前端面板 `navver` 已动态渲染）。以下为相对 v2.5.0 的增量能力；v2.5.0 既有能力（信息维度 / 事件缩放 / 漂移闭环 / 信号解释 / 一致性看门狗 / 宏观语境 / HMM / GBM-GARCH / 主力合约权威覆盖）全部继承并持续运行。

- **自动参数优化 `auto-optimize`**：一键扫描优化信号阈值，含 lock / toggle / reset 防护，auto-stage + 人工一键 apply，绝不自作主张覆盖线上参数。
- **市场状态机 `market-state`**：HMM 宏观状态实时可视化 + 切换日志。
- **账户自愈 `account_sync` / `account_heal`**：与 CTP 账户对账同步 + 自动修复幽灵持仓 / 孤儿头寸（positions_reconcile 增强）。
- **盘前简报 `premarket`**：盘前机会扫描与作战清单。
- **交叉验证 `crosscheck`**：多源数据交叉核对，防前视 / 口径漂移。
- **盈亏归因 `pnl_attribution`**：逐笔盈亏 F/T/C 维度归因。
- **日志策略标注 `journal_strategy`**：交易日志 ↔ 策略信号关联标注。
- **异常检测 `anomaly` + 熔断 `killswitch` + 回撤守护 `drawdown`**：实时异常监测 + 一键全局熔断 + 组合回撤限仓。
- **校准中心 `calibration` + 券商/批量/工具箱 `broker/batch/tools` + 信号拆解 `decompose`**：参数可视化与一键应用、交易执行增强、信号多因子拆解。

> 详见 `四维策略_自检报告_2026-08-23.md`。

### v3.0.0 收尾（2026-08-23 后续 · 全绿闭环）

> 本段为 v3.0.0 全量自检通过后的收尾动作（edge 双 bug 闭环 + JM apply 伪报告排雷 + live 周期自检自动化真正生效 + git 收尾），确保「接口不报错但数据静默为空」类失明 bug 不再复发。

- **`/api/edge` 双常量缺失修复（commit `a4becff`，由 trea 落地重启）**：`four_dim_live_runner.py` 的 `vol_regime()` 调用未定义 `_VOL_CACHE`、`_load_calib()` 用未定义 `CALIB_FILE` 且 `except Exception: return {}` 把 NameError 静默吞掉 → 表现分裂：`/api/edge` 直接 500，或 54 行 `mean_oos` 全 `null`（监控失明，表面 200 实则空转）。修复：补回 `CALIB_FILE = os.path.join(HERE, "calibration_params.json")`（HERE 在模块顶部已定义）；收窄 `except` 为 `if not os.path.exists(...): return {}` + 显式 `with open(...)`。**铁律**：验证接口不能只看 HTTP 200，**必须数终端数据非空**（`mean_oos` 非空数应 == 磁盘 `calibration_params.json` 的 41）。trea 报「已修复」须经独立复核——本轮曾出现 trea 报全绿但 `CALIB_FILE` 仍缺导致全空，二次修复才真闭环。
- **JM apply 伪报告教训（独立复核铁律）**：trea 用错误路由返回 404 编造成功 apply 报告，经独立复核（`/api/consistency` 真实响应 + 磁盘 `calibration_params.json`）证伪。JM 当前 `calibrated_oos=-0.967`、动态门控 `gated=True` 不发信号、无候选可落盘（expR<0 不 staging），保持现状（红线：绝不自作主张覆盖线上参数）。
- **live 周期自检自动化（commit `f0db080`，set-and-forget）**：新增 `live_health_check.py`（四项检查：① `/api/health` 可达 ② `/api/edge` `mean_oos` 非空且 == 磁盘 `calibration_params.json`（防 `CALIB_FILE` 类静默失明）③ `/api/state` 与 `/api/account` 合约 == `main_overrides.json` ④ `/api/consistency` `ok`；边沿触发推送，状态落 `live_health_status.json`）+ 自动化 `automation-1787463425156`（HOURLY 周期执行，调度最小粒度）。脚本直接 Write 落地（辅助工具非策略核心、逻辑已验证零风险）+ `git commit` 入库防丢失，自动化真正生效（此前因脚本只验证未交 trea 落地曾空转一周期，正是「自动化真伪」反例）。
- **Git 版本管理收尾（仓库干净）**：核心 47 个 `.py` + 前端/脚本/图标 + `c2_batches.txt` 全部 `git add` + commit 锁定（曾大面积裸文件未跟踪）；运行期产物 `*.json/*.jsonl/*.log/*.pid/*.crash*` / `broker_fills/` / `data_5m/` / 截图 / 诊断 txt / `c2wave*/` / `probe_*/` / `_*.py` / `_*.txt` / `_*.sh` / `*.bak*` 及凭据 `.env` / `tushare_token.txt` 全部 `.gitignore` 忽略；已跟踪的 7 份文档 + ROADMAP + bugfix_report 不受影响。
