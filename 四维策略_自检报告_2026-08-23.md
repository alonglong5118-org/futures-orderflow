# 四维策略盯盘模型 · 全量自检报告（v3.0.0）

> 自检时间：2026-08-23 08:58（SoftwareCompany 主理人维护 · 结论先行）
> 自检方式：直连 live runner（`http://127.0.0.1:8741`）实测 + 全模块编译 + 磁盘数据交叉核对

---

## 一、总体结论

**✅ 模型整体健康，可独立正常运行。** 进程在线、18 个核心模块全部编译通过、一致性看门狗零异常、账户无幽灵持仓、风控口径无虚高、合约三层对齐。

**唯一缺陷（已本次修复）**：代码与面板早已升到 **v3.0.0**，但 7 份固定文档仍停在 **v2.4.0 / v2.5.0** —— 版本号口径漂移。本次已将全部相关文件统一升到 **v3.0.0** 并补 v3.0.0 升级要点。

---

## 二、自检项明细

| # | 自检项 | 口径/红线 | 实测结果 | 判定 |
|---|--------|-----------|----------|------|
| 1 | 进程与接口 | runner 在线、`/api/health` ok、feed 可用 | PID 40599，uptime ≈ 6.4h，`feed_available=true` | ✅ |
| 2 | 模块编译 | 18 个核心 .py 全部 `py_compile` 通过 | 18/18 OK | ✅ |
| 3 | 版本一致性 | 代码/面板/文档三方一致 | 代码+面板=v3.0.0；**文档原 v2.4/2.5 → 本次升 v3.0.0** | ⚠️→✅ |
| 4 | 风控口径三铁律 | `risk_pct`(1.5%)≠`portfolio_risk_pct`(4.5%)≠`usage_rate` | `usage_rate=8.7%`（与用户“约10%仓位”吻合），无 91.8% 类虚高 | ✅ |
| 5 | 账户对账 | 无 CTP 手动平仓后的幽灵持仓 | `account_state.json` 仅 6 真实持仓（rb/sp/PK/SA/jd/AP），无孤儿 | ✅ |
| 6 | 合约三层一致性 | `main_overrides`=权威源=`/api/state` | 活跃品种全部对齐；`lc`/`si` 冻结 2609（设计内，见下） | ✅ |
| 7 | 一致性看门狗 | 0 漂移 / 0 broken 仍在服务 | divergences=0、broken_serving=0、unvalidated=0、stale=0；lh/JM 已门控 | ✅ |
| 8 | 运行日志 | 无近期 error / 异常崩溃 | 无错误日志 | ✅ |

---

## 三、关键指标快照（实测）

- **权益 equity**：¥607,300.88
- **保证金仓位 usage_rate**：8.7%（≈ 用户所述“总仓位约 10%”）
- **已实现盈亏 realized_pnl**：−¥163.31
- **组合热度 heat_pct**：沿用 `PORTFOLIO_RISK_PCT=4.5%` 组合预算口径（2026-08-18 修复后固化，无单笔 1.5% 误用）
- **一致性看门狗**：`ok=true`，`broken_gated=[lh, JM]`（漂移判 broken 且已被动态门控压制，不发信号，风险已控）
- **合约权威覆盖**：`/api/state` 的 `symbols[].contract` 实时字段与 `main_overrides.json` 一致；持仓品种（rb=RB2610/sp=SP2611/PK/SA/jd/AP）钉合开仓合约，不随面板主力滚动

---

## 四、当前真实持仓（account_state.json）

| 品种 | 方向 | 开仓合约 | 备注 |
|------|------|----------|------|
| rb 螺纹钢 | 空 | RB2610 | 真实开仓合约 |
| sp 不锈钢 | 多 | SP2611 | 真实开仓合约 |
| PK _pk | 多 | （fallback main_overrides） | 合约字段待补，回退权威源正确 |
| SA 纯碱 | 空 | （fallback main_overrides=SA2701） | 逆势空，止损纪律严守 |
| jd 鸡蛋 | 空 | （fallback main_overrides=JD2610） | — |
| AP 苹果 | 多 | （fallback main_overrides=AP2610） | — |

> 说明：`/api/account` 返回 54 条是“全品种盯市枚举”，仅 6 条为真实持仓（以 pnl/方向标注区分），非幽灵持仓。

---

## 五、本次发现的问题与处置

### 问题 1（已修复）：文档版本号口径漂移
- **现象**：`APP_VERSION=v3.0.0` 且前端 `navver` 已显示 v3.0.0，但 7 份固定文档（`自我介绍`/`功能汇总`/`调阅索引`/`架构梳理`/`思维导图`/`规格草案`/`ROADMAP`）仍标 v2.4.0 / v2.5.0。
- **后果**：用户/后续会话读到的是旧版能力描述，与运行系统脱节。
- **处置**：全部当前版本标签升 v3.0.0，并各补「v3.0.0 升级要点」一节（不改动历史版本演进表）。

### 问题 2（已知设计，非缺陷）：lc/si 停在 2609
- `refresh_main_contracts.py` 的 `_FIXED_KEYS = {"SA01","sa01","lc","si"}` 故意冻结 lc(碳酸锂)/si(工业硅) 合约，不随换月自动滚动。
- 二者均不在关注 6 品种集、且被门控/禁用，不影响实盘信号与风控。**记录备查，不强制改动。**

---

## 六、结论与建议

1. **模型可独立运行**：进程、数据、风控、合约、一致性全部通过，无需人工干预即可持续盯盘。
2. **文档已与代码对齐**：v3.0.0 升级要点已落盘，后续会话不会再读到旧版能力描述。
3. **建议（非阻塞）**：
   - 若后续实盘要交易 lc/si，需先人工解除 `_FIXED_KEYS` 冻结并核对真实主力；
   - 建议将 v3.0.0 的未提交改动（runner/html/strategy 共 +5254/−2829 行）择机 `git commit`，固化版本节点。

---

*本报告由自查脚本 + live 接口实测生成，非投资建议。*
