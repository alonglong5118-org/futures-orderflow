# 四维策略（4D）框架 v1.1（定稿 · 自检补丁）

> 🏷️ **运行系统当前版本 v3.0.0（2026-08-23 全量自检通过）**：本草案为设计层定稿（v1.2），其落地运行系统已迭代至 v3.0.0，新增自动参数优化 / 市场状态机 / 账户自愈 / 盘前简报 / 盈亏归因 / 异常·熔断·回撤 / 校准中心 / 信号拆解（详见「功能汇总」v3.0.0 升级要点）。

> 本文档把「管住手下单前四维自检卡」自动化为**信号发生器 + 风控闸门**。
> 状态：**已定稿（v1.2）·§1.12 免费源已确认采用 · F 真实接入回测完成（§1.18）**。
> - v1.0：设计层 8 件全部共识落地。
> - v1.1：对定稿做一次穿透式自检，补齐 15 项缺口——
>   - **2 条硬伤（数据源方案·已查到免费源可解，已确认采用）**：§1.12 给出 T@5m 与 F 基本面的免费数据源落地方案。
>   - **13 项文档内可补规则/占位**：回测扣费滑点 + 换月对齐（§1.13）、风控补已有持仓 / 组合约束 / 实时保证金率（§3）、移动止损锚点（§1.8）、温和提示冷却 + 夜盘K线 / 长假跳空 / 信号价参考 / 合约换月 / 多信号优先级 / C_flow漂移监控（§1.14）。
> 下一步：数据源已确认（§1.12 免费源），落地 `four_dim_strategy.py`（核心引擎）。

> **修订记录（2026-08-13 · 实现层追加，不影响 v1.1/v1.2 设计定稿）**：本草案的设计框架已落地为 `four_dim_strategy.py` 等运行系统，并在 v1.9.0 基础上于 08-13 完成六大实现层升级（详见「功能汇总」§十七 / 「架构梳理」§4.1·§4.10·§4.12）：
> 1. **真重校准 5 失效品种**：校准参数双源陷阱（`calibration_params.json` 仅展示、`four_dim_strategy.thresholds_by_symbol` 才驱动 live）——两处都改；JM/hc 真死维持屏蔽，zn/eb/al 重校准后转正。
> 2. **流动性敏感滑点**：固定 1 点 → `get_slip_pts` A/B/C 档（1.0/1.5/2.0，按近60日成交额数据驱动），大小写不敏感（修 `"EB"` vs `"eb"` 静默回退 bug）。
> 3. **执行层成交质量监控**：信号记 `entry_ref`，`trade_journal._fill_quality` 算不利/超预算滑点、时机、手数比、出场质量。
> 4. **前端滑点档 + 重校准态**：Edge 卡片双列展示。
> 5. **流动性档位真实复核**：`recompute_liquidity_tiers` 重排 53 品种，lh 2.0→1.0、jd 1.5→1.0、J 1.0→2.0、FG/SA 1.0→1.5。
> 6. **JM/hc 自适应恢复**：`DISABLED_SYMBOLS` 加 JM/hc（保留定义不删），`AUTO_RECOVER_SYMBOLS={JM,hc}`，每 3600s `recovery_check` 转正自动解禁。
> 新增实现层工具：`four_dim_calibrate.py`（重校准扫描器）、`four_dim_recalibrate.py`（在线重校准检测）、`four_dim_papertrack.py`（真实 papertrack 评分 + 动态门控）。

> **修订记录（2026-08-13 二次 · 十七项功能缺口 #1–#17 全部补完，升至 v2.0.0）**：在 v1.9.1 六大升级之上，一次性补完 17 项模型功能缺口（详见「功能汇总」§十八 / 「自我介绍」§六）：
> - **风控**：#1 逐品种止损止盈+regime 联合校准、#4 fractional-Kelly 仓位缩放、#5 组合级硬熔断 KillSwitch（回撤15%/日亏8%/连亏6笔→全平+禁开+永不自动恢复，落盘防重启洗白）、#6 板块集中度+单边净敞口上限。
> - **执行**：#7 大单拆分/冰山/TWAP（execution_planner）、#8 一键全平/反手、#9 经纪商成交回灌（broker_import）。
> - **绩效**：#10 绩效多维分解（9 维）、#11 蒙特卡洛置信区间、#12 纪律自动体检（blunder）、#17 回测可视化（水下曲线/逐笔散点）。
> - **可靠性**：#13 事件/数据日历闸门、#14 数据质量/陈旧监控、#15 手机推送（Telegram/Bark/企业微信）、#16 进程看门狗自动重启。
> - **引擎**：#2 模型健康分解（F/T/C 留一消融）、#3 tick 订单流接入 C 维度。
> 新增模块 9 个（execution_planner / broker_import / perf_breakdown / montecarlo / blunder_check / event_calendar / data_quality / push_notify / backtest_viz）+ watchdog.sh / start.sh。

> **修订记录（2026-08-13 三次 · v2.0.1 · 启动修复 + 交易记录↔账户总览打通）**：
> - `/api/journal` 的 entry/exit 同步写 account_tracker（entry→open/add、exit→close/reduce），消除「交易记录」与「账户总览」两套脱钩——用户只报开单，系统自动填止损止盈并同步持仓。
> - entry 未带止损止盈时自动 `_auto_levels`（30min ATR）填好 `止损/t1/t2`，杜绝无止损裸奔。
> - `start.sh` 看门狗模式忽略多余参数（修中文注释被当参数透传崩溃）；runner 两处 `global` 修复（last_recover / DAY_OPEN_EQUITY UnboundLocalError）。

> **修订记录（2026-08-14 · 四次 · v2.2.0 · 全量自检修复 + G1–G5 功能补齐）**：
> - **全量代码自检（5 处真实 bug 修复）**：① #1 看门狗心跳 `LAST_CYCLE_TS` 漏 `global`（`main` 只更新局部、看门狗永不触发）；② #2 `record_exit` 部分平仓忽略 `lots`、journal↔账户脱钩；③ #3 动态权益重复计入（`account_tracker.set_equity` 存 `float_at_sync`、`snapshot` 扣减）；④ #4 `portfolio_var`/`correlation_breakdown_stress` 用静态权益做 VaR 分母（改持仓价动态权益）；⑤ #5 `_auto_levels` 止损 regime 硬编码（改 `fd.classify_regime`）。
> - **G1 利润/价差/比价指标**（F 维度补强）：新建 `fundamental_metrics.py`，7 项产业链指标（玻璃-纯碱加工利润/焦煤-焦炭化利润/生猪自繁自养/蛋鸡养殖/玻璃-纯碱比价/焦煤-焦炭比价/螺卷差），每项 latest/mean30/zscore/trend；端点 `/api/fund_metrics` + 前端「利润·价差·比价」书签。
> - **G2 交易笔记**：成交记录加 `note` 字段 + `update_trade`（白名单 `note`/`exit_reason`，RLock 包裹读改写）+ 新增 `/api/journal_list`（返回含 id/note 的成交明细）；前端交易记录卡「备注」按钮 `POST /api/journal {action:"note"}`。
> - **G3 重校准自动调度**：`_recalibrate_tick()` 接入主循环（受 `auto_recalibrate`/`recalibrate_interval_h` 控制），不再仅限 CLI 工具箱。
> - **G4 日盘/夜盘分时段绩效**：`trade_journal.session_performance()` 按平仓时段拆分（日盘/夜盘 + 逐品种）；`SYMBOLS` 每品种注入 `night` 字段作单一真值源（消除 `NO_NIGHT`/`_NIGHTLESS` 双集合漂移），`_in_session` 改读 `SYMBOLS[sym].night`；端点 `/api/journal_session` + 前端绩效页「日盘/夜盘绩效分解」卡。
> - **G5 关注度盯盘板**：`focus_board()` 5 维打分（信号30%/波动20%/持仓20%/价差偏离15%/热度15%）；端点 `/api/focus` + 前端「我的品种」书签聚焦 6 关注品种（jd/lh/FG/SA/JM/J）。
> - **本机实测跑通（2026-08-14）**：`python3 -c "import four_dim_live_runner"` 无导入错误；面板启动正常、G3 重校准自动调度日志可见触发；4 个新面板（利润·价差·比价 / 我的品种 / 交易备注 / 日盘夜盘）均能渲染。

> **修订记录（2026-08-15 · 第五次 · 面板二次自检 P1/P2-3/P2-4 修复 + 订单流真实 tick 升级 Stream3）**：
> - **P1 配色/语义收敛（前端）**：`four_dim_live.html` 47 处颜色字面量收口为语义变量——`--red:#ff4757` 仅表盈利/上涨/多头/买入；`--green:#2ed573` 仅表亏损/下跌/空头/卖出；`--amber:#ffa502` 表风险/告警。修正西方配色反向：entry(开仓)=红、exit(平仓)=绿（与中国期货红涨绿跌一致）。**不改动任何策略/风控逻辑**。
> - **P2-3 子模块刷新 + 主循环并发（前端）**：复盘页 F1 多策略表 **+30s 轮询**、报警历史 **+15s 轮询**；前端主 `refresh()` 由串行 `await` 逐接口改 `Promise.allSettled([...])` **并发**拉取，慢接口不再拖累整屏。不改动策略/风控。
> - **P2-4 JM 合约错配修复（后端 `_xs_recent_signal_price`）**：原老信号（`valid_minutes` 缺省）被 `annotate_signal_ttl` 永久判「未过期」，永远返回冻结价 1323 与 JM2609 最新收盘 1515 比对，偏差 −12.7% 持续误报合约错配（#126 多源交叉校验）。修复：跳过过期/历史信号；无 `valid_minutes` 老信号用 `DEFAULT_SIGNAL_TTL_MIN=120` 作过期基准；重启验收通过。
> - **Stream3 订单流真实 tick 升级（数据层）**：新增 `backend_tqsdk.py`（**独立进程·端口 8742**）作为真 tick 生产者——TqSdk 订阅真实 tick 写 `tick_stream.jsonl`，**CZCE(FG/SA) 走交易所逐笔真 tick**，**DCE(JM/J/jd/lh) 走 akshare 分钟线近似**（标注 `akshare_min`，分钟级非逐笔），休市日读本地缓存回放。runner 设 `TICK_FEED_ENABLED=1` 后 `tail` 其输出灌进 `FEED.flow[sym]`，**零改动接入**（主面板代码不碰）。Tailer 断点自愈（`size < self.last_read` 归零续读），防 `tick_stream.jsonl` 被 truncate 重写后整屏订单流冻结。启用：`bash start_tqsdk.sh` + runner 配置 `TICK_FEED_ENABLED=1` 重启；`stop_tqsdk.sh` 停止。未启用时回退原 minishare 5m 差分代理。**本升级属数据/管道层，不改动四维 F/T/C 权重、regime 过滤、品种启停等策略/风控逻辑**。

> **修订记录（2026-08-17 · 第六次 · 组合 VaR 升级 + 风控健壮性修复 + OOS 最终态 + 5m 回测死循环修复）**：
> - **组合 VaR 模块升级（③①②，hy3/kimi-k3 协作）**：③ 阈值全参数化（`portfolio_risk_pct`/`kelly_max`/`kelly_min` 接入 `trade_config.json`，`four_dim_strategy.py` 新增 `_tc_num`/`_load_tc` 供回测路径读取）；① VaR 由参数法正态升级为 **250 日历史模拟法 + EVT（广义帕累托 GPD 矩估计）** 尾部外推（取严叠加只加不减，`var_method=param` 或样本不足自动回退旧法）；② 协方差矩阵双层缓存（`_VAR_RETS_CACHE`+`_VAR_DATA_CACHE`），高频逐仓 VaR 真实环境加速 ~1075×（1611ms→1.5ms）。验收：QA 41 断言全过；真实在线 var_95_pct=0.85%、cap 余量 +2.45pp。
> - **全市场 OOS 日线版应用（品种启停最终态）**：解禁 JM/ag/bu/ss（整改后日线转正 + Δ>+0.02 + 样本≥30 + 5m 不退化 + 回撤改善）；加禁 MA/PR（整改后日线 on 负且较 v12 退化，PR 另 5m 双负）。`DISABLED_SYMBOLS` 最终 = 11 个 `{au,i,eg,m,a,b,rr,RM,hc,MA,PR}`；`AUTO_RECOVER_SYMBOLS={hc}`（JM 已解禁）。
> - **5m 回测死循环修复**：`fundamental_feed.load` 加 `_load_cache` 进程内缓存，修 SR/TA/al/c/sp/v/y/zn 等 8 品种 `walk_forward_backtest_5m_exit` 卡死，现 10–23s 跑通、OOS 全量报告收敛。
> - **风控健壮性修复（3 处）**：① 风控状态机 P0——补初始化 `daily_loss_locked=False`（原漏初始化，运行期 `AttributeError` 风险）；② 非交易时段通知门控——新增 `_should_suppress(topic)`+`_market_open_now()`，非交易时段静默信号/报警推送（高优先级报警不动）；③ 止损止盈方向异常——根因止损方向配置入口无校验、多空反填，在 `trade_journal.record_entry`+`account_tracker._auto_levels`+返回前 `_validate_levels` 三层补校验，成功修复时静默配置异常通知。live runner 重启链 PID→82784（FRESH_OK）。

> **修订记录（2026-08-17夜~18 · 第七次 · v2.4.0 认知层 + 波动率动力学 + 合约权威覆盖）**：
> - **认知层五件套（live 专属，回测零前视红线）**：#1 信息维度 `info_dimension.py`（F_override 喂 live F）；#4 信号解释 `signal_explain.py`（结论先行解释器挂 signal）；#5 一致性看门狗 `consistency_watchdog.py`（`/api/consistency` 4 类问题只报告不修正）；#6 跨资产宏观语境 `macro_context.py`（股/债/汇 → `macro_bias` 温和调制 bias_G ±12 分内）；#7 HMM 市场状态 `regime_hmm.py`（4 态调制 T_thresh_eff ×0.90~1.25）+ GBM/GARCH 波动率动力学 `gbm_garch.py`（GARCH(1,1) MLE + GBM 前向 VaR/区间 + vol_state→`risk_scale` **已接入实际手数** `min(scale,dd_scale,ev_scale,_gbm_scale)` 取严者，高波动 0.8/0.6 真实减仓）。
> - **🔴 主力合约权威覆盖（用户最高优先级铁律「以后一直自动切换」）**：`main_overrides.json` 权威层（55 品种→akshare 真实主力）+ `minishare_live` 三解析路径优先采用 + `/api/state` 实时解析；采集器每 2h 自动重写（单调向前防 OI 横跳）；换月异常 osascript 告警（ABNOLD 6h / FETCHFAIL·临界 7天状态变化）；SA09/SA01 fixed、持仓品种钉开仓合约不滚（正确设计）。自检口径永远以 `/api/state` 的 `symbols[].contract` 实时字段为准。
> **修订记录（2026-08-18 · 第八次 · v2.5.0 风控口径修复 + 看门狗语义 + 持仓对账 + 面板版本动态化）**：
> - **heat_pct 预算口径修复**：`compute_heat` 改 `PORTFOLIO_RISK_PCT`(4.5% 组合预算) 替代单笔 `risk_pct`(1.5%)，修「91.8% 虚高误报」；口径铁律 `risk_pct`≠`portfolio_risk_pct`≠`usage_rate`。
> - **看门狗语义精确化**：`RECAL_GRACE_DAYS=7`（近期重校不报 divergence）+ `broken_serving`/`broken_gated` 拆分（门控压制归提示不计 ok）。
> - **持仓对账防漂移**：`POST /api/positions_reconcile` 手动对账端点 + `account_marketsync` 持仓超期>7天提醒（CTP 手动平仓后防幽灵持仓）。
> - **面板版本号动态化**：`APP_VERSION` + `/api/state` 下发 version + 前端 navver 实时渲染。
> - **P3 数据源暂缓**：Tushare 骨架 `fetch_macro_context_full.py` + `tushare_token.txt` 就位；token 积分不够期货接口、macro_bias 只用三因子故暂缓。

---

## 0. 一句话架构（v0.4 · 流水线模型）

三维度不是「三个方向相加」，而是**不同角色串成一条链**：

```
基本面 F  →  技术面 T  →  资金面 C  →  【风控硬闸门】
(背景/偏置)   (触发/方向)   (确认/强度)
   │             │              │
   │ 反向→抑制    │ 定方向       │ 同向→加成 / 反向→打折
   └─────────────┴──────────────┘
              合成 combined(方向+置信度)
                          │
                  过风控闸门? ─ 是 ─→ 发信号(方向+入场+止损+止盈+理由) + 通知
                              └─ 否 ─→ 温和提示「最后一关风控未过」+ 原始信号 + 理由
```

- **方向来自技术面触发** `dir_T = sign(T_score)`，不来自加权求和。
- **基本面是背景偏置闸门**：同向则放行，反向则抑制（软闸门；硬反转信号可强否决）。
- **资金面是确认器**：与技术触发同向 → 置信度加成；反向 → 打折。
- 维度间冗余由「相关性闸门」自动降权（见 §1.4）。

---

## 1. 三维修分框架（v0.4 · 流水线 + 去重）

每维归一到 **−100 ~ +100 带符号**（偏多 + / 偏空 − / 中性 0）。

### 1.1 基本面 F（背景偏置 · 慢变量）
| 输入 | 打分草案 |
|---|---|
| 基差位置(对20日区间)、焦化/压榨利润、近远月结构(back/contango)、每日库存/供需慢变量偏置 | 基差偏强 +30、利润扩张 +25、back结构 +20、日偏置 ±25，加权合成 |
| **基本面季节性（现货供需/存栏周期）** | 鸡蛋中秋备货、生猪出栏节奏等**现货数据**驱动，**不来自价格回报**（与 1.3 技术季节性明确区分，避免季节效应双倍计数） |

- `bias_F = sign(F_score)`；出现硬反转（政策突变/库存极值/结构逆转）→ 可强否决技术触发。
- **数据源（见 §1.12）**：基差/库存用免费接口每日刷新；利润/期限结构/现货季节性由基差+库存派生。

### 1.2 技术面 T（触发/方向 · 纯价格）
- 沿用 da龘 8 大策略（**源码已证实 100% 只用开高低收，零成交量/持仓**），合成 `T_score ∈ [−100,+100]`。
- 大周期 `T@D`（日线 8 策略方向）算背景偏置；小周期 `T@5m`（5 分钟触发）算精确入场方向。
- `dir_T = sign(T@5m_score)`；`|T@5m_score| ≥ T_small_thresh` → 视为有效触发（见 §1.5/§1.6）。
- **技术季节性 s_seasonal = 价格回报的季节规律（动量视角）**，与 1.1 现货季节性是不同透镜，二者同向不额外加成、反向以现货(基本面)优先。

### 1.3 资金面 C（确认/强度 · 拆两组，组内只取主信号）
> 拆组目的：原 4 个子信号（成交量/资金进出/龙虎榜/多空比）内部同源——「资金进出≈量×价派生」、「龙虎榜≈多空比(持仓方向)」，会自己重复计数。

| 组 | 主信号（唯一计分） | 辅助（不计分，仅佐证） |
|---|---|---|
| **持仓定位组 C_pos** | 龙虎榜排名净变化方向（多头席位增/减仓） | 多空比净持仓方向（同源，仅确认不打分） |
| **流量组 C_flow** | 实时资金**净流入速率**方向（用速率，不用裸成交量，杜绝量≈流重复） | 成交量放大仅作速率可信度加权 |

- `C_score = combine(C_pos, C_flow)`：同向强化、反向取弱（取较小绝对值或中性 0）。
- 确认作用：`sign(C_score) == dir_T` → 置信度加成；反向 → 打折。
- **C_flow 历史不可回测**：盘中实时净流免费历史拿不到，回测用 C_pos 代理（见 §1.13/§1.15）；实盘加成（见 §1.14 C12 漂移监控）。

### 1.4 相关性闸门（治「派生相关性」· 坑C）
- 维护滚动窗口（trailing N=20 个评分点）的三维分数序列，算 `corr(T,C)`、`corr(T,F)`、`corr(F,C)`。
- 若 `corr(T,C) > 0.7`（典型：价格趋势时技术动量 + 资金流入同涨，隐性把"涨"算两遍）：
  - **降权低置信维度**（该维 conviction 贡献 ×0.5），或
  - **合并取「方向中位数 + 置信度 min」** 而非 sum。
- 目的：维度高度冗余时不再把同一行情加倍计权。

### 1.5 多周期「看大做小」与噪音控制（第5项第2件 · 已共识）
**原则**：大周期定偏置（背景），小周期定触发（执行）；小周期信号必须与大周期偏置同向才放行，反向一律抑制——**大偏置本身就是最强的噪音过滤器**。

| 层 | 周期（默认） | 负责的维度 | 作用 |
|---|---|---|---|
| 大（偏置） | 日线 D | F(日慢变量) + T@D(8策略日线方向) + C_pos(龙虎榜=前一日排名) | 战略方向与背景偏置 `bias_G` |
| 中（上下文·可选） | 1小时 1H | regime 分类 + T@1H 确认 | 大/小之间的过渡过滤 |
| 小（触发） | 5分钟 5m（微观 1m/tick 仅作 C_flow 速率源） | T@5m(触发) + C_flow(盘中实时净流) | 精确入场触发 `dir_T` |

- **大周期偏置** `bias_G = combine(F_daily, T@D_score, C_pos_daily)`；**小周期触发** `dir_T = sign(T@5m_score)`。
- **点火规则**：`dir_T` 必须与 `bias_G` 同向（或 `bias_G` 中性）才触发；`bias_G` 反向 → 抑制（软闸门）。这与 §0 流水线一致，只是把"背景偏置"显式放到大周期上算。
- **噪音控制三道闸**：
  1. **聚合窗口**：小周期触发须连续持 K 根 bar（建议 3 根 1m 或 1 根 5m 收盘）才确认，过滤单 tick 毛刺。
  2. **同方向冷却**：触发后对该品种同方向冷却 `cooldown = 300s`（复用 da龘）；反向冷却可短（如 60s），降炸屏。
  3. **小周期置信地板**：`T@5m_score` 须 ≥ `T_small_thresh`（待校准，建议 ≥ 主阈值的 1/2）才允许触发，弱摆动不点火。
- **温和提示冷却（补 B9）**：风控未过转温和提示时，**同样走冷却**——同品种同向温和提示在 `cooldown` 内不重复弹（只刷新不新增）；若 `bias_G` 长期反向（如持续偏空但小周期反复试探多），温和提示自动降级为「静默日志」不弹横幅，避免长期刷屏（可设「连续 N 次同向温和提示 → 静默 1 小时」）。

> 注：周期默认值（D / 1H / 5m）由 AI 初定，用户定稿时可按品种波动特性微调（如 scalping 更密可用 1m 触发）。

### 1.6 阈值校准方案与初值（第5项第3件 · 方法已定 + AI 初值）
**校准方法论（复用 da哥 v12 walk-forward 纪律）**：
- 历史切滚动窗口（1年训练 / 3个月测试，前滚）；阈值只在**测试段**评估 OOS 指标（胜率 / 期望值 / 类夏普 / 最大回撤 / 交易次数）。
- 准入门槛：`stability ≥ 0.70` 且 `OOS_expR ≥ 0.15`（复用 `walk_forward_gate`）；每品种每窗交易次数 ≥ 30 才显著。
- **绝不 in-sample 拟合**（过拟合必亏）。
- **必须扣费扣滑点**：OOS 期望值计算须扣除手续费（fee，复用 §1.15 的 universe 费率体系）与滑点（slip_pts，新增参数，默认 1 跳）后再评估（见 §1.13）。

**AI 初定值（按用户画像分三组，待 walk-forward 收紧）**：
> 用户画像：黑系+农产品半自动信号面板、手动下单、重风控（单笔1.5% / 保证金30%）、怕炸屏 → 阈值偏「少而精」；且风险已由 §3 硬闸门兜底，故阈值只管信号质量。

| 组 | 品种 | T_thresh(大/D) | T_small_thresh(小/5m) | conv_thresh | 设计理由 |
|---|---|---|---|---|---|
| 黑系 | JM, J | 22 | 15 | 50 | 趋势强，阈值偏低吃趋势；但仍需多数策略真共识 |
| 化工 | FG, SA | 22 | 15 | 55 | 居中 |
| 农产品 | jd, lh | 25（lh 28） | 18（lh 20） | 60（lh 62） | 无夜盘跳空多、噪声大，阈值最高；lh 波动最烈再上调 |

> **阈值实测校正（v1.1 自检补丁）**：回测发现 8 策略日线合成的 `T_D` 实际分布集中在 **±50 内**（6 主品种实测 max 仅 50~52，p80≈25、p90≈33），原稿 `40/45/55` 基于「`T_D` 可达 100」的错误假设，实际 `|T_D|≥45` 仅占 0.2%、几乎不可达 → 信号永远 0。已初步下调到 **黑系/化工 22、农产品 25**（对应 `|T_D|` p80 附近，仍属「少而精」）。最终阈值须由 walk-forward 回测收紧（§1.6 校准纪律）。

- `corr_gate = 0.70`（全组固定，冗余降权触发线）。
- `bias_G 硬否决`：`|bias_G| ≥ 60` 且反向 → 硬压制（农产品可降到 50，跳空易硬翻偏置）。
- 注：da龘 现有 8 策略 `direction` 触发仅约 9/100（过松）；四维要求 **22~28（按组，见 §1.6 表）**，比 9/100 仍严一个数量级，旨在出「决策级」信号而非「方向噪音」。

**历史数据现实（已定位）**：C_flow（盘中实时净流）历史免费拿不到 → **不可回测**；回测时 C 用 C_pos(龙虎榜，交易所日更可历史) 代理，甚至先 F+T 两维回测，C_flow 留实盘加成。但**日线主连历史 + 5m 历史均已可获取**（见 §1.12/§1.15）。

---

### 1.7 Regime 自适应（第5项第4件 · 已共识）

**核心原则**：趋势市松阈值吃趋势、震荡市紧阈值防假突破、波动市宽止损扛波动。

**复用而非重造**：直接复用 da龘 `strategy_layer.py` 的 `classify_regime(df, params)`（三分类：趋势 / 震荡 / 波动，支持分商品 `regime_params` 覆盖阈值）。且 da龘 在 8 策略合成时**已按 regime 调权重**（趋势市趋势策略1.0 / boll 0.3，震荡市反向，波动市折中）——四维的 `T_score` 已内含该加权，**不在四维内重做 regime 加权**（避免与 da龘 重复 / 冲突）。

**四维只在外层补两层 regime 映射（点火阈值松紧 + 风控旋钮）**：

旋钮 1 — 点火阈值随市况松紧（核心）：
| regime_D | T_thresh 系数 | conv_thresh 系数 | 理由 |
|---|---|---|---|
| 趋势 | ×0.85 | ×0.90 | 趋势市重技术确认，略松，别错过行情 |
| 震荡 | ×1.20 | ×1.15 | 假突破多，收紧过滤 |
| 波动 | ×1.00 | ×1.00 | 居中，靠 ATR 自适应 |

旋钮 2 — 止损 / 出场 / cooldown 随 regime：
| regime_D | stop_atr_mult | 出场风格 | 同方向 cooldown |
|---|---|---|---|
| 趋势 | ×1.0（可略宽） | 放宽 / 移动止损（let it run） | 300s |
| 震荡 | ×1.0 | 收紧（到了就走，mean reversion） | 450s |
| 波动 | ×1.2（波动大给空间） | 居中 | 300s |

> 出场风格的具体规则（初始止损 / 分批止盈 / 移动止损 / 被动平仓 / 时间止损）见 §1.8。

旋钮 3 — bias_G 硬否决幅度随 regime（偏置天然带 regime 语境）：
| 趋势 | 波动 | 震荡 |
|---|---|---|
| `\|bias_G\| ≥ 60`（保持） | `\|bias_G\| ≥ 65` | `\|bias_G\| ≥ 70`（别误杀区间反转） |

**regime 计算位置与迟滞**：
- 在**日线 D** 算 `regime_D`（背景层）；小周期 5m **不重算**（噪声大、易过拟合）。
- **必须加迟滞（hysteresis）**：维持当前 regime，直到反方连续 ≥ 3 根日线、或偏离 > 1.5× 阈值才切换，防边界 flip 致阈值抖动。

**校准纪律**：walk-forward 时**分 regime 子样本**验证系数（趋势/震荡/波动各自 OOS 期望值），不能只在全样本一锅调；regime 判定可历史回测（仅日线 atr/MA，无数据坑）；系数初值（0.85 / 1.20 / 1.2 等）先手感定，回测收紧。

---

### 1.8 出场细节（第5件 · 已共识）

**前提**：四维是半自动信号面板 → 出场规则**生成「出场计划」（价位 + 条件文本）**，由用户在券商软件手动挂单；系统内不自动平仓（与 da龘 下单纪律一致）。

**出场四件套**：

1. **初始止损（铁律，入场即挂）**
   - 多：`stop = entry − stop_atr_mult × ATR`；空：`stop = entry + stop_atr_mult × ATR`
   - `stop_atr_mult` 经 §1.7 regime 调整：趋势 ×1.0 / 震荡 ×1.0 / 波动 ×1.2。
   - 永远先挂，不裸奔。

2. **分批止盈（两批，50/50）**
   - 目标1 = `entry ± 1×止损距`（= 1R，锁一半利润）→ 平 50%
   - 目标2 = `entry ± rr_ratio×止损距`（= 2R，默认 `rr_ratio=2.0`）→ 平剩余 50%
   - **震荡市只做目标1**（到 1R 全走，不恋战，mean reversion 不追）；趋势/波动市目标1 平 50% 后，余下交移动止损。

3. **移动止损（trailing，趋势/波动市启动，震荡市不启动）**
   - 启动：目标1 平仓后，剩余 50% 启动 trailing。
   - 跟踪距 `trail_atr_mult × ATR`：趋势 2.0×ATR、波动 2.5×ATR（更宽防洗）。
   - **锚点算法（补 B8 · 明确可执行）**：多单 `track` 锚定最近 **N=10 根 5m K 的 swing low**（即比左右各 1 根更低的最低价；每出现新 swing low 即更新锚点）；空单对称锚定 **swing high**。则 `track = 锚点 ± trail_atr_mult×ATR`（多：swing_low + 倍数×ATR；空：swing_high − 倍数×ATR）。价格回破 track 即平。
   - 让利润奔跑（let it run），直到回破或被动平仓触发。

4. **被动 / 紧急平仓（非价格触发）**
   - **regime 翻转**：日线 `regime_D` 由趋势翻震荡/波动，或 `bias_G` 硬否决反转 → 平掉 trend-following 剩余仓位（保利润）。
   - **反向信号**：同品种出现反向综合信号 → 平旧（是否反手由用户手动决定，系统仅提示）。
   - **时间止损（农产品专属）**：jd/lh 无夜盘 + 周末停盘 2 天 → **收盘前 5 分钟（14:55）强平隔夜敞口**，规避跳空缺口；有夜盘品种（FG/SA/JM/J）可持，但**周五夜盘前减仓/平**。
   - **长假跳空（补 C11）**：春节 / 国庆 / 劳动节等长假前 1 个交易日 14:55，**全部品种强平隔夜敞口**（含 FG/SA/JM/J 有夜盘品种），长假后首日不开新仓或降仓观望，规避 5~7 天累积跳空。
   - **风控闸门4**：连续止损达 `consec_loss_lock` → 已禁新开；持仓浮亏扩大可提前手动平（系统提示）。

**与 §1.7 旋钮 2 的写死映射**：
| regime_D | 初始止损倍数 | 止盈结构 | 移动止损 | 隔夜 / 跳空 |
|---|---|---|---|---|
| 趋势 | ×1.0 | 两批（1R平50% + 余下trail） | 启动 2.0×ATR | 可持隔夜，周末/长假减仓 |
| 震荡 | ×1.0 | 单批（到1R全走） | 不启动 | 收盘前强平隔夜 |
| 波动 | ×1.2 | 两批（1R平50% + 余下trail 2.5×ATR） | 启动 2.5×ATR | 收盘前强平隔夜 |

**校准注意**：分批比例（50/50）、trail 倍数（2.0/2.5）、时间止损时点（14:55）、swing N（10 根）先手感定，回测看「移动止损是否真提升期望值 / 时间止损是否真降跳空亏损」。

---

### 1.9 冲突消解（第6件 · 已由流水线机制覆盖，无需独立规则）

**结论**：冲突消解难独立成规则——它在 v0.4「背景→触发→确认」流水线 + 看大做小 + `bias_G` 硬否决里已被**架构性消解**。早期「≥2 维同向才触发」的并列投票门槛（v0.3 求和模型）在流水线中已废除：方向只来自技术触发 T，F/C 只做放行/抑制/加成，**不存在「多数维压制少数维」的投票冲突**。

**三类冲突及其在框架内的消解位置**：
| 冲突场景 | 在哪定义 | 消解规则 |
|---|---|---|
| 小周期触发 vs 大周期偏置反向 | §1.5 点火规则 + §1.7 旋钮3 | `bias_G` 反向 → 抑制（软闸门，不触发）；`\|bias_G\| ≥ 阈值`（趋势60 / 波动65 / 震荡70）反向 → 硬压制（不触发，转温和提示） |
| 资金面确认 vs 技术触发反向 | §0 流水线 | C 为确认器，反向 → 置信度打折（不阻断、不开反方向） |
| 基本面硬反转 vs 技术触发 | §0 + §1.1 | F 出现硬反转（政策突变 / 库存极值 / 结构逆转）→ 强否决技术触发 |

**补充边界**：`bias_G` 中性（≈0）且小周期触发强 → 仍放行（中性偏置不抑制），仅「明确反向」才抑制；冷却期间（§1.5 同方向 300s / 反向 60s）同方向不重复触发。

---

### 1.12 数据源方案（自检补丁 · 解决 2 条硬伤 · 已确认采用·免费源）

> 原 v1.0 自检发现 2 条硬伤：① `T@5m` 数据源真空（da龘 8 策略仅日线，本地历史 5m 仅 JD/RM）；② 基本面 `F` 实时数据管线缺失（基差/库存/利润零支撑）。经核查 **两者均有免费源可解**，方案如下，**已确认采用**，无需付费 API。

**A1 · T@5m 触发数据源**
- **回测 / 历史**：用 `china-futures-free-data` 技能的 `futures_zh_minute_sina(symbol='FG2509', period='5')` 接口现拉 6 主品种 5 分钟历史（**实测 6 品各 ~1023 根，列 datetime/open/high/low/close/volume/hold**），落本地 `data_5m/_XX0_min5.csv` 缓存复用（JD/RM 兼用管住手目录原文件）。
- **minishare 权限实测（2026-08-11，token `CWIFB8…` 4 周不限次）**：逐项探查 `minishare.pro_api.query` 的权限，结论——**仅 `rt_fut_k`（实时快照，990 合约，不限次）可用**；以下全部「权限不足」：`fut_daily` / `fut_5min` / `fut_1min` / `fut_tick` / `fut_k` / `fut_main` / `fut_cont` / `fut_daily_main` / `fut_kline` / `fut_basis` / `fut_inventory` / `fut_spot`。即：**历史日线/分钟/tick 与基本面（基差/库存）端点该 token 一律未开权限**，minishare 只能做「实时快照」。自检脚本见 `minishare_permission_check.py`。
- **实盘触发（真·minishare 驱动，已落地 `minishare_live.py`）**：minishare `rt_fut_k` 不限次 60s 轮询，把快照 `close` 在 5 分钟桶内**聚合为 5m OHLCV 棒**作为实盘 `T@5m` 源；同时用快照 (价,持仓,量) 差分喂 `FlowAggregator` 出 `C_flow`，叠加 da龘 tick 订单流。品种映射按名匹配（坑：`JM`=焦炭主连、`JMM`=焦煤主连）：`FG→FGM, SA→SAM, JM→JMM, J→JM, jd→JDM, lh→LHM`。**实时路径（价/5m/C_flow）已全部 minishare 化，不再用 sina**。`load_min5(live=True)` 走此源。
- **C_flow 真 tick 升级（2026-08-15 · Stream3，可选增强）**：C 维流量组 `C_flow` 现可由独立进程 `backend_tqsdk.py`（端口 8742）生产**真实 tick**——TqSdk 订阅交易所逐笔 tick 写 `tick_stream.jsonl`，**CZCE(FG/SA) 走交易所逐笔真 tick**；**DCE(JM/J/jd/lh) 因 TqSdk 对该所逐笔 tick 权限/稳定性受限，走 akshare 分钟线近似**（标注 `akshare_min`，分钟级非逐笔）。runner 配 `TICK_FEED_ENABLED=1` 后 `tail` 该 jsonl 灌进 `FEED.flow[sym]`，与原 minishare 5m 差分代理叠加；未启用则回退原代理（见 §1.19 真相 1 暖机行为不变）。**此增强仅升级 C_flow 数据来源质量，不改变 §1.3 C 维计分结构（C_score = combine(C_pos, C_flow)）或任意策略/风控逻辑**。
- **历史 5m 仍走 sina（本 token 无法并）：** 因 `fut_5min` 权限不足，回测/历史 5m 仍用 `futures_zh_minute_sina` 落本地 `data_5m/`（仅近 ~1023 根）。若日后换带 `fut_5min`/`fut_daily` 权限的 token，可把历史也并到 minishare 单源。
- **降级**：sina 历史拉取失败/不足 + 无 fut_min 权限时，回测 `T@5m` 用「日线偏置降频代理」（`T@D` + 1H 确认代替 5m 触发），标注该品种阈值未做 5m OOS 验证。

**A2 · 基本面 F 实时数据源**
- **基差**：`china-futures-free-data` 技能 `futures_spot_price_daily`（期现基差，部分品种可用）。每日盘前刷新，算基差位置（对 20 日区间百分位）。
- **库存**：`futures_inventory_em`（库存，有限覆盖）。每周/每日刷新，算库存同比/环比偏置。
- **利润 / 期限结构 / 现货季节性（派生）**：由基差 + 库存派生——近远月结构（back/contango）由基差符号+展期成本判定；焦化/压榨利润由基差+关联品种价差近似；现货季节性由基差日序列的月内规律判定（与 §1.1 现货季节性对齐）。
- **minishare 不能替换 F**：该 token 无 `fut_basis`/`fut_inventory` 权限，基本面 F **仍走 akshare（免费源）**，无法全 minishare 化。属当前 token 客观限制，待换有权限 token 再评估。
- **刷新机制**：建 `fundamental_feed.py`，盘前（含周末）拉基差/库存，写 `fundamentals.json`（按品种缓存）；`F_score` 实时读取，零额外盘中 API 调用。
- **覆盖边界（诚实标注）**：基差/库存免费接口对部分品种（尤其新上市/小品种）覆盖有限，缺失品种 `F` 降级为「中性 0 + 仅用 T@D/C_pos」，不阻断信号。

---

### 1.13 回测严谨性（自检补丁 · 解决 A3 扣费滑点 / A4 换月）

> 原 v1.0 未明确回测扣费与换月处理。经核查：`universe_backtest.py` **已扣手续费**（UNIVERSE 含各品种 fee，回测用 `fee_frac` 折算），但其注释自承「主力连续换月有噪声」「单窗口未 walk-forward」——故需补两点。

**A3 · 必须扣费 + 扣滑点**
- 手续费：复用 `universe_backtest.py` 的 UNIVERSE 费率（`("pct",...)` 或 `("fix",...)`），`fee_frac` 折算等效比例。
- 滑点：新增 `slip_pts`（默认 1 跳 = 最小变动价位；农产品 jd 1 元/lh 5 元 等），每笔进出各扣 1 跳。
- **OOS 期望值必须在扣费扣滑点后计算**；阈值校准（§1.6）与准入门槛（`OOS_expR≥0.15`）均以此为准，否则阈值虚高、C_flow 加成效果失真。

**A4 · 主连换月跳空对齐（必须处理，否则回测污染）**
- 主连 `_XX0_daily.csv` 在换月日有跳空（旧主力收盘→新主力开盘），直接喂 8 策略会造假方向、假 ATR 暴增（止损距骤大→信号消失），并污染季节性。
- 处理方案（二选一，回测时固定）：
  1. **换月日对齐**：检测换月跳空（当日开盘相对前收 > 阈值，如 > 3×ATR），对该日价格做**差值平移**使连续（用复权因子），再算指标；或
  2. **改用展期加权连续**（back-adjusted continuous）：优先，平滑换月噪声。
- 落地时 `four_dim_strategy.py` 的 `load_daily` 在 `universe_backtest.load_daily` 基础上**增加换月对齐步骤**（标注已处理），并在回测报告注明所用方法。

---

### 1.14 实盘衔接细节（自检补丁 · 解决 B9 + C10~C15）

> 以下为 v1.0 未覆盖、但实盘必碰的边界。

- **C10 夜盘 K 线归属**：夜盘（21:00–23:00）在交易所会计入**次日日线**。`bias_G`（日线偏置）取「交易日所在日线」——即当日开盘前算好的当日偏置（含前一日收盘信息）；点火时若处夜盘时段，用**当日(交易日期)日线 + 当日实时价**，避免误用「昨日收盘」判定次日方向。
- **C11 长假跳空**：见 §1.8 第4点（长假前 1 日 14:55 全品种强平隔夜）。
- **C12 C_flow 实盘漂移监控**：回测仅 F+T+C_pos，实盘多 C_flow 确认 → 实盘信号特征≠回测 OOS。上线首月记录「有 C_flow 确认」vs「仅 F+T+C_pos」信号的后验表现差异；若偏差显著，触发 §1.6 阈值再校准（conv_thresh 据实盘提升反推）。
- **C13 信号价参考值**：`entry/stop/target` 为触发时快照参考价，**用户手动下单按实时价重算**——系统给出「止损距(点)=stop_atr_mult×ATR、手数=N_plan」公式而非固定数字；下单时以实时价 ± 止损距重算实际 stop/target。
- **C14 合约到期 / 换月实盘映射**：四维给**主连信号**，实盘交易**具体月份合约**。主连切换日，系统提示「主连换月，请移仓至新主力合约 XXX（代码）」；信号价以新主力连续价为参考；旧合约持仓移仓由用户手动决策，系统不自动移。
- **C15 多信号并发优先级**：6 品种同时响时，前端红横幅按优先级排序，避免一屏刷多个：
  1. 主排序：`raw_score` 降序（信号最强优先）；
  2. 加权：趋势市 regime 品种加权 ×1.1（吃趋势）、震荡市 ×0.9；
  3. 组合约束：按 §3 组合总保证金上限依次放行，超账户承受则低优先级信号转温和提示。
- **B9 温和提示冷却**：见 §1.5 末尾（同品种同向温和提示走冷却，长期反向自动静默）。

---

### 1.15 回测数据（第7件 · 已共识 + 自检补强）

- **历史数据已现成**：`/Users/ken/WorkBuddy/2026-07-28-12-52-27/管住手_实盘工作区_Ken/量化回测/` 含 54 品种主连日线 `_XX0_daily.csv`（含 6 主品种 FG/JM/SA/J/jd/lh，FG 实测 2012-12-03→2026-07-24 共 3304 行，远超 5 年窗口）。`universe_backtest.py` 的 `load_daily` 逻辑可原样复用（须叠加 §1.13 换月对齐）。
- **5m 历史获取**：JD/RM 本地已有；FG/SA/JM/J 用 `china-futures-free-data` 技能 `futures_zh_minute_sina` 现拉（见 §1.12 A1），落本地缓存复用。
- **C_flow 不可回测**：盘中实时净流历史免费拿不到 → 回测时 C 用 C_pos(龙虎榜) 代理，先 F+T 两维跑通，C_flow 留实盘加成。conv_thresh 上线后据实盘提升反推（见 §1.14 C12）。
- **校准纪律**：walk-forward（1年训练/3月测试前滚）分 regime 子样本验证；准入 `stability≥0.70 & OOS_expR≥0.15`（**扣费扣滑点后**，见 §1.13）；每窗 ≥30 笔；绝不 in-sample 拟合。

### 1.16 合规边界（第8件 · 已共识）

- **四维只 Ken 本人自用**（半自动信号面板，手动下单，与 da龘 同一定位）→ **无对外合规约束**，信号可给买卖 / 挂单 / 止损止盈建议，不触发「管住手 Lens 严禁喊单」那条对外红线。
- **不并入管住手对外交付**：四维独立于「管住手 Guard/Lens 产品体系」，不对外喊单、不进抖音内容、不对客户输出。若未来要对外（如赋能 Guard 实时版），需单独评估合规边界：届时 Lens 通道仍严禁喊单、只给三档数据分析。

---

### 1.17 回测自检结果（v1.1 落地 · `four_dim_strategy.py` 已可跑）

> ⚠️ **本节为校准前（v1.0）中间结果，已过时**。最终经嵌套 walk-forward OOS 校准（2026-08-11，见 §1.19）后，FG/JM/jd 数字已变（JM 修复、jd 扳正、FG 全样本转负但 OOS 正）。以 §1.19 为准。

**设置**：6 主品种、日线主连（§1.15 现成数据）、tail=1500（近 5 年）、F=C=0（C_flow 历史不可得、F 数据源未接，仅验证「技术触发 + 风控」管线）、扣费扣滑点（§1.13）、gate3 `limit_proximity=0.9`、N_risk 最小 1 手、冷却 5 根日线。

| 品种 | 笔数 | 期望R | 胜率 | 分 regime 期望R（主要） | 结论 |
|---|---|---|---|---|---|
| FG 玻璃 | 21 | **+0.243** | 42.9% | 趋势 +0.555 / 过渡 −0.538 | ✅ 趋势市吃肉 |
| SA 纯碱 | 25 | **+0.392** | 48.0% | 趋势 +0.748 / 波动 −1.023 | ✅ 趋势强 |
| JM 焦煤 | 60 | **+0.081** | 36.7% | 过渡 +1.953 / 趋势 −0.189 | ✅ 偏过渡市 |
| J 焦炭 | 40 | **+0.021** | 35.0% | 趋势 +0.459 / 波动 −0.060 | ✅ 微正 |
| jd 鸡蛋 | 76 | **−0.121** | 30.3% | 趋势 +0.086 / 过渡 −0.553 / 波动 −0.514 | ⚠️ 负（农产品噪声大） |
| lh 生猪 | 34 | **+0.220** | 41.2% | 波动 +0.868 / 趋势 +0.031 | ✅ 波动市吃肉 |

**结论**：6 主品种全部出信号（框架跑通），**5/6 正期望**；趋势市/波动市普遍吃肉、过渡/震荡市普遍亏损（正是 §1.7 regime 自适应要解决的——震荡市已加严、过渡市仍软）。jd 负期望符合「农产品无夜盘跳空多、噪声大」的设计预判，需后续接入 C 确认 / 提高农产品阈值 / 排除过渡市改善。

**回测中修复的 3 个致命 bug（v1.1 落地发现）**：
1. **回测冷却逻辑**：原「方向翻转才交易」使趋势品种（FG/JM 长期同向）几乎 0 信号 → 改为冷却 N 根日线重触发。
2. **gate3 过严**：`limit_proximity=0.5` 使 ATR 止损常超「半停板」→ 玻璃等全品种被风控否决（367 次触发全挡）→ 改 0.9。
3. **N_risk=0 卡死**：高乘数/高波动品种（焦煤/焦炭/生猪）一手风险已超 1.5% 预算 → `N_risk` 整除得 0 → 永不开仓 → 改「最小 1 手 + ⚠️标注」。

**诚实声明（回测局限，非框架缺陷）**：
- **日线近似**：`T@5m` 用 `T@D` 降频代理（无 5m 历史，待 §1.12 拉取），非真正「看大做小」分钟级触发。
- **仅 F+T 两维**：C_flow 历史免费拿不到、F 数据源未接，故回测不含资金确认增益（实盘接入后信号质量应更高）。
- **非严格 OOS 分区**：阈值已用全样本初步定（存在轻微前视），正式上线前须按 §1.6 分「训练/测试」段 walk-forward 再校准防过拟合。
- **「期末平」兜底**：末笔信号可能受 df 末尾截断影响，属边界效应。

---

### 1.18 三维度全真回测（v1.2 · F 真实接入 · 2026-08-11）

> ⚠️ **本节为校准前中间结果（tail=600 子集），已过时**。最终全样本 + per-symbol 校准结果见 §1.19。

**设置**：6 主品种、日线主连、tail=600（落在 2024+ 基差历史窗口内，F 全真）、**F=真实（fundamental_feed 基差/库存派生）、T=T_D 日线触发、C=中性**（C_flow 历史免费拿不到，留实盘）。扣费扣滑点、gate3=0.9、N_risk 最小 1 手、冷却 5 根日线。

| 品种 | F中性 期望R | **真实F 期望R** | 笔数(真实F) | 变化 | 说明 |
|---|---|---|---|---|---|
| FG 玻璃 | +0.243 | **+0.443** | 6 | ↑ | F 偏置过滤毛刺后质量升 |
| SA 纯碱 | +0.392 | +0.342 | 15 | ≈ | 仍强 |
| JM 焦煤 | +0.081 | **−0.056** | 25 | ↓ | F 贴水偏空，过滤多单后空单表现差 |
| J 焦炭 | +0.021 | **+0.080** | 27 | ↑ | F 助选 |
| jd 鸡蛋 | −0.121 | **+0.000** | 32 | ↑↑ | 原负期望被 F 扳平（农产品贴水偏空压制差多单） |
| lh 生猪 | +0.220 | +0.209 | 22 | ≈ | |

**结论**：
- F 维度已**真接通并起作用**（不再中性占位）；jm 外其余品种期望 R 持平或上升，**jd 从负期望扳到 ~0 是关键改善**（验证了"农产品噪声+贴水偏空误导"的设计预判）。
- 信号数普遍下降（F 作背景软闸门，反向抑制生效，信号更精选）。
- **JM 退步**提示：F 权重(背景 0.25)与 `bias_hard` 幅度可能需按品种微调（焦煤贴水结构与玻璃不同），属阈值校准（§1.6 严格 walk-forward）范畴。
- 分 regime 看：趋势/波动市仍吃肉（FG 趋势+0.44、lh 波动+1.99、SA 趋势+0.81），过渡市偏弱（SA 过渡−0.19、JM 过渡−0.12）——与 §1.7 regime 自适应一致。

**已落地代码**：
- `fundamental_feed.py`：拉 akshare 基差(`futures_spot_price_daily`)+库存(`futures_inventory_em`)→`fundamentals.json`（6 品各 ~629 期基差 + 库存），`compute_F` 由 dom_basis_rate+库存趋势+现货季节性派生；缺失品种降级中性。
- `four_dim_strategy.py` 扩展：`score_F` 接真实 `compute_F(date)`；`load_min5` 缺则 sina 拉取落 `data_5m/`；新增 `FlowAggregator`/`compute_C_flow`（minishare 60s 差分 + da龘 tick，实盘 C_flow）；`pipeline`/`backtest` 接真实日期算 F。
- `data_5m/_XX0_min5.csv`（FG/SA/JM/J/lh 已缓存，jd 用管住手目录原文件）。

**剩余（非设计问题）**：
- C_flow 历史不可得 → 回测 C 仍中性；**实盘 C_flow 已可跑**（`compute_C_flow` 验证：净流入+19.5 / 净流出−6.5 方向正确），待第 5 步接入 da龘/独立 adapter 后发挥。
- 严格 walk-forward OOS 分区重校准阈值（防全样本前视）；JM 等品种 F 权重/阈值微调。
- 5m 历史仅近 ~10 日（sina 限制）→ 全窗口 5m walk-forward 回测不可行，T@5m 实盘走 minishare 60s 聚合。

---

### 1.19 校准后最终状态（v1.2 自检 · 2026-08-11 终稿）

**做法**：嵌套 walk-forward OOS 校准（`four_dim_calibrate.py`）——预计算参数无关量 → 滚动 train/test 折（train≈2.4年/test≈6月/步进6月）→ 网格搜 `(T_thresh, bias_hard_base)`，**选参只看 test 折 OOS 池内表现**（绝不前视 train）→ 稳健门槛「池内正期望 + 胜率≥保本线(2R=1/3)」。结果写回 `four_dim_strategy.py` 的 `DEFAULT_CONFIG["thresholds_by_symbol"]`，`pipeline` 经 `effective_params()` 优先用 per-symbol 覆盖。完整方法/复跑命令见 `calibration_report.md`。

**最终 per-symbol 阈值（已生效）**：

| 品种 | T_thresh | bias_hard(趋/波/震) | 来源 |
|---|---|---|---|
| FG 玻璃 | 22 | 50/55/60 | 校准(per-symbol) |
| SA 纯碱 | 22 | 60/65/70 | 组默认(未认证) |
| JM 焦煤 | **14** | 50/55/60 | 校准(修复) |
| J 焦炭 | 22 | 50/55/60 | 校准(per-symbol) |
| jd 鸡蛋 | **26** | 50/55/60 | 校准(扳正) |
| lh 生猪 | 25 | 60/65/70 | 组默认(未认证) |

**最终全样本回测 + OOS（F=真实 / T=T_D / C=中性回测；扣费扣滑点）**：

| 品种 | 笔数 | 全样本期望R | 胜率 | OOS池内期望R | OOS折数 | 结论 |
|---|---|---|---|---|---|---|
| FG 玻璃 | 96 | **−0.063** | 33.3% | **+0.258** | 64(22折) | ⚠️ 全样本负但OOS正→最弱/regime依赖，重点监控 |
| SA 纯碱 | 33 | +0.224 | 42.4% | 未认证(低频) | — | 全样本正，待模拟盘验证 |
| JM 焦煤 | 136 | **+0.085** | 37.5% | **+0.121** | 115(21折) | ✅ 已修复（原全样本+0.009） |
| J 焦炭 | 142 | +0.197 | 41.5% | **+0.255** | 105(25折) | ✅ 稳健 |
| jd 鸡蛋 | 130 | **+0.010** | 34.6% | **+0.112** | 111(20折) | ✅ 扳负为正（原−0.020） |
| lh 生猪 | 34 | +0.220 | 41.2% | 未认证(低频) | — | 全样本正，待模拟盘验证 |

**实盘系统接线真相（自检发现，务必知悉）**：
1. **C_flow 实盘需"暖机"**：`FlowAggregator` 需累积 ≥3 个 60s 差分才有值；开盘初期 / 单次轮询下 `C=0` → 信号显示「无C确认(中性)」，即**资金确认层在暖机期是空转的**。运行数分钟后才真正生效。
2. **实盘 5m 需"暖机"**：`df_5m` 需 ≥60 根(≈5小时交易/跨2日累积)才走真 5m 触发；未足时 `pipeline` 退化为 `T_D` 日线触发（"看大做小"退化为"看大做大"）。`live_bars.json` 跨重启/跨日持久化，故第二次运行起即有棒。
3. **F 实盘依赖盘前刷新**：`fundamentals.json` 须由 `fundamental_feed.py` 盘前刷新（现 2026-08-11 12:53 已刷）；未刷则 `F=0`，背景偏置退化为仅 T_D 主导。
4. **corr_gate 冗余降权未接线**：`pipeline(corr_hist=...)` 的 `corr_hist` 在 live/回测中从未传入 → `corr_action` 恒为「无冗余,正常计权」。§1.4 相关性闸门属架构预留、当前恒不触发（不影响信号，仅少一道冗余防护）。
5. **C_pos(龙虎榜持仓)从未用**：C 维实盘仅 `C_flow`(流量组)，无历史/免费源可补「持仓定位组」→ 规格 §1.3 的 C_pos 主信号当前为空缺(由设计：无数据)。
6. **步骤5（接 da龘 / adapter）未做**：用户拍板「再议」。当前四维系统是**独立实盘 runner**（`four_dim_live_runner.py` + `four_dim_live.html`），未并入 da龘 Web 面板。

**结论**：引擎/校准/live runner/纸面追踪器均已落地可跑；**信号质量前向验证（这正是模拟盘阶段要做的）尚未发生**。FG 全样本负、SA/lh 未认证 → 模拟盘重点监控这三；C_flow 实盘加成效果待模拟盘数据回来再评估。

---

## 2. 账户参数表（占位默认值 · 最后由用户填真实数）

框架内置一份**默认参数**（基于期货通用纪律 + 用户「5万到100万」系统风格估算）。用户定稿时把真实数覆盖即可。

| 字段 | 含义 | 默认占位 | 设计理由 |
|---|---|---|---|
| `equity` | 账户权益（该策略动用总资金） | **100,000** | 示例占位；按实际账户填 |
| `risk_pct` | 单笔最大风险% | **1.5** | 期货常见 1~2%，控回撤 |
| `margin_cap_pct` | 单品种最大保证金占用% | **30** | 防单品种挤占流动性 |
| `max_lots` | 单品种手数上限（统一） | **5** | 保守起步；可改品种分设 |
| `per_symbol_lots` | 品种→手数上限（可选覆盖） | `{}` | 如 `{"JM":3,"lh":2}` |
| `stop_atr_mult` | 止损 ATR 倍数 | **1.5** | 波动率自适应止损 |
| `rr_ratio` | 风险回报比（止盈/止损） | **2.0** | 1:2 盈亏比 |
| `limit_proximity` | 止损距达涨跌停幅度比例即阻断 | **0.9** | 止损距达涨跌停 90% 才否决（留 10% 腾挪）；原 0.5 过严→玻璃等全品种被风控否决（v1.1 回测修正） |
| `consec_loss_lock` | 连续止损达此数 → LOCKED 禁新开 | **3** | 复用 da龘 风控状态机 |
| `portfolio_margin_cap_pct` | **账户级总保证金占用%上限（补 B6）** | **60** | 6 品种同时触发时总占用不超账户 60% |
| `max_total_lots` | **账户级总手数上限（补 B6）** | **15** | 组合总敞口上限（≈单品种×3） |
| `slip_pts` | **滑点（回测用·补 A3）** | **1** | 每笔进出各 1 跳，OOS 期望值扣滑点 |
| `use_realtime_margin` | **是否盘前拉实时保证金率（补 B7）** | **true** | 节前提保时覆盖 §2.1 占位 |

### 2.1 品种合约参数（风控计算必需 · 占位，需按交易所最新标准核实）

| 品种 | 乘数 | 每点价值 | 保证金率(估) | 涨跌停% |
|---|---|---|---|---|
| FG 玻璃 | 20 | 20元 | 13% | 4% |
| SA 纯碱 | 20 | 20元 | 9% | 4% |
| JM 焦煤 | 60 | 60元 | 12% | 8% |
| J 焦炭 | 100 | 100元 | 13% | 8% |
| jd 鸡蛋 | 10 | 10元 | 7% | 8% |
| lh 生猪 | 16 | 16元 | 12% | 8% |

> 以上为占位常识值，**保证金率/涨跌停/乘数以交易所公布为准**；定稿前盘前用 `akshare futures_comm_info` 拉最新覆盖（见 §3 闸门 5 / §2 `use_realtime_margin`）。

### 2.2 框架级配置 JSON（系统直接读取）

```json
{
  "account": {
    "equity": 100000,
    "risk_pct": 1.5,
    "margin_cap_pct": 30,
    "max_lots": 5,
    "per_symbol_lots": {},
    "portfolio_margin_cap_pct": 60,
    "max_total_lots": 15,
    "use_realtime_margin": true
  },
  "risk_gate": {
    "stop_atr_mult": 1.5,
    "rr_ratio": 2.0,
    "limit_proximity": 0.9,
    "consec_loss_lock": 3,
    "slip_pts": 1
  },
  "contract_specs": {
    "FG": {"multiplier": 20, "margin_rate": 0.13, "limit_pct": 0.04},
    "SA": {"multiplier": 20, "margin_rate": 0.09, "limit_pct": 0.04},
    "JM": {"multiplier": 60, "margin_rate": 0.12, "limit_pct": 0.08},
    "J":  {"multiplier": 100, "margin_rate": 0.13, "limit_pct": 0.08},
    "jd": {"multiplier": 10, "margin_rate": 0.07, "limit_pct": 0.08},
    "lh": {"multiplier": 16, "margin_rate": 0.12, "limit_pct": 0.08}
  }
}
```

---

## 3. 风控硬闸门与风控线计算（核心）

风控闸门读第 2 节参数，对**计划开仓**做判定。任一越界 → 阻断（信号不触发，转温和提示）。

### 闸门 1 — 单笔最大亏损线

```
每手止损额  = stop_atr_mult × ATR(近期, 如30min) × point_value
风险反推上限手数  N_risk = floor( equity × risk_pct% / 每手止损额 )
```
- 若 `N_plan × 每手止损额 > equity × risk_pct%` → **越界**（或自动把 N_plan 下调到 N_risk）。
- 含义：这笔单万一止损，亏的钱不超过权益的 risk_pct%。
- **最小 1 手（v1.1 回测修正）**：若 `N_risk_raw < 1`（高乘数/高波动品种一手风险已超预算，如焦煤、焦炭、生猪），仍给最小 `N_risk = 1` 并标注 `⚠️超风险预算·已限最小1手`（信号照发、不超加仓）。否则这些品种在该账户规模下永不开仓（回测原 0 信号即此因）。

### 闸门 2 — 单品种保证金占用线

```
每手保证金  = price × multiplier × margin_rate
占用        = N_plan × 每手保证金
```
- 若 `占用 > equity × margin_cap_pct%` → **越界**。
- 含义：单品种不要吃掉太多账户流动性。

### 闸门 3 — 止损距 / 涨跌停临近线

```
当日涨跌停幅度(点) = price × limit_pct
止损距(点)         = stop_atr_mult × ATR
```
- 若 `止损距(点) ≥ 当日涨跌停幅度(点) × limit_proximity` → **越界**（v1.1 校准 `limit_proximity = 0.9`，即止损距达涨跌停 90% 才否决；原 0.5 过严，玻璃等 ATR 止损常超「半停板」→ 全品种被否决）。
- 含义：止损空间都快顶到停板了，开了也没腾挪余地、且临停板流动性差；留 10% 缓冲避免误杀正常波动。

### 闸门 4 — 连续止损降档（软闸门，复用 da龘 风控状态机）

```
连续止损计数 c；若 c ≥ consec_loss_lock → 状态 LOCKED，禁新开（或 scale = 0.8^c 降仓）
```
- 不阻断信号本身，但缩放/锁定仓位，防情绪化连亏加码。

### 闸门 5 — 实时保证金率（补 B7）

```
盘前/盘中若 use_realtime_margin=true：用 akshare futures_comm_info 拉该品种最新保证金率，覆盖 §2.1 占位（尤其节前提保、剧烈行情提保时）
fallback：用 §2.1 占位值
```
- 目的：交易所提保时，固定占位算出的可开手数虚高 → 实盘开不出甚至透支，须用实时值。

### 闸门叠加：已有持仓（补 B5）

```
开新仓前，把同品种已持仓 old_lots 计入：
  总占用 = (N_plan + old_lots) × 每手保证金；须 ≤ equity × margin_cap_pct%
  新开风险 = N_plan × 每手止损额；须 ≤ equity × risk_pct%
反手场景：先平旧仓(释放占用与风险)再开新，系统提示「反手：请先平旧仓 X 手」
```
- 目的：避免加仓/反手后实际总占用超单品种上限。

### 闸门叠加：组合总约束（补 B6）

```
6 品种同时触发时，账户级校验：
  总保证金占用 = Σ_all_sym (已持仓+计划开) × 每手保证金 ≤ equity × portfolio_margin_cap_pct%
  总手数 = Σ_all_sym (已持仓+计划开) ≤ max_total_lots
超约束的品种按 §1.14 C15 优先级依次降级为温和提示
```
- 目的：单品种风控通过 ≠ 组合安全；防 6 品种齐发把账户打爆。

### 最终可开手数（单品种）

```
N_plan = min( N_risk,  N_margin,  max_lots,  per_symbol_lots.get(sym, max_lots) )
```
四道单品闸门（1~4）+ 闸门5(实时保证金) + 闸门叠加(B5/B6) 全过 + N_plan ≥ 1 → 风控通过，发信号；否则温和提示。

---

## 4. 计算示例（用默认参数跑 FG，验证设计可落地）

参数：`equity=100000, risk_pct=1.5, margin_cap_pct=30, max_lots=5, stop_atr_mult=1.5, rr=2.0`
FG 实时：`price=2000, ATR(30m)=8点, point_value=20, margin_rate=13%, limit_pct=4%`

```
每手止损额 = 1.5 × 8 × 20 = 240 元
MAX_LOSS   = 100000 × 1.5% = 1500 元
N_risk     = floor(1500 / 240) = 6 手
每手保证金 = 2000 × 20 × 13% = 5200 元
MARGIN_CAP = 100000 × 30% = 30000 元
N_margin   = floor(30000 / 5200) = 5 手
N_plan     = min(6, 5, 5) = 5 手
止损距     = 1.5 × 8 = 12 点
涨跌停幅度 = 2000 × 4% = 80 点
12 ≥ 80×0.5(=40)?  否 → 不越界
连续止损 c=0 → 不锁定
```
**结论：风控通过，建议 5 手。**
- 若方向=多：入场≈2000，止损=2000−12=1988，止盈=2000+24=2024（1:2）
- 若方向=空：入场≈2000，止损=2000+12=2012，止盈=2000−24=1976

---

## 5. 信号包裹结构（发给用户的一条完整信号 · v0.4 流水线）

```json
{
  "symbol": "FG",
  "direction": "多",
  "entry": 2000,
  "stop": 1988,
  "target": 2024,
  "lots": 5,
  "pipeline": {
    "F_bias": 40,
    "T_score": 70, "T_triggered": true,
    "C_pos": 60, "C_flow": 75, "C_score": 80,
    "confirm": "资金确认(同向加成)",
    "background": "偏置同向放行",
    "corr_gate": {"corr_TC": 0.62, "action": "无冗余，正常计权"}
  },
  "risk_gate": {"pass": true, "N_risk": 6, "N_margin": 5, "N_plan": 5, "limit_check": "ok"},
  "reason": "技术面触发偏多(T=+70，价在枢轴上方+均线多头)；基本面背景同向放行(F=+40)；资金面确认(C=+80：龙虎榜净多增+资金净流入同向)，置信度加成后过线；风控四闸门全过，建议5手，止损1988/止盈2024。"
}
```

风控不过时，`risk_gate.pass=false` + `reason` 末尾附「最后一关风控未过，请自判」，走温和提示通道（横幅但不响铃炸屏，且受 §1.5 温和提示冷却约束）。

---

## 6. 运行形态（共识）

独立模块 `four_dim_strategy.py`，**直接读 da龘 已在内存聚好的实时行情**（CTP / tqsdk / akshare / minishare），不重复拉数据；可挂进 da龘 后端当新信号源，也可独立跑。数据源补充（§1.12）：`fundamental_feed.py` 盘前拉基差/库存刷新 `F`，`T@5m` 用本地缓存 5m + da龘 tick 聚合。第 5 项讨论完 + 参数定稿 + 数据源方案确认后落地。

---

## 框架定稿收口（v1.1）

第 5 项「8 件没想到的事」全部共识落地（§1.1~1.9 / §1.15 / §1.16）；v1.1 自检补丁补齐 15 项缺口（§1.12 / §1.13 / §1.14 / §2 新增字段 / §3 闸门5+叠加）。

**未决（非设计问题，落地时处理）**：
- 回测自检已完成（v1.1 落地，见 §1.17）：6 主品种全出信号、5/6 正期望；jd 负期望待接入 F/C 或 regime 加严处理。
- 账户真实参数覆盖 §2 占位默认值（权益 / 风险% / 保证金率 / 手数上限等由用户填）。
- 阈值初值（§1.6）经 walk-forward 回测收紧（用 §1.15 现成数据 + 换月对齐 + 扣费扣滑点）。
- 品种合约参数（§2.1）保证金率 / 涨跌停 / 乘数以交易所最新公布核实（盘前 `use_realtime_margin` 自动拉）。
- 移动止损 swing N、时间止损时点等手感参数回测后微调。

---

## v3.0.0 升级要点（2026-08-23 · 全量自检通过）

> 运行系统 `APP_VERSION=v3.0.0`（runner + 前端面板 `navver` 已动态渲染）。以下为相对 v2.5.0 的增量能力；v2.5.0 既有能力（信息维度 / 事件缩放 / 漂移闭环 / 信号解释 / 一致性看门狗 / 宏观语境 / HMM / GBM-GARCH / 主力合约权威覆盖）全部继承并持续运行。

- **自动参数优化 `auto-optimize`**：一键扫描优化信号阈值，含 lock / toggle / reset 防护，auto-stage + 人工一键 apply，绝不自作主张覆盖线上参数。
- **市场状态机 `market-state`**：HMM 宏观状态实时可视化 + 切换日志。
- **账户自愈 `account_sync` / `account_heal`**：与 CTP 账户对账同步 + 自动修复幽灵持仓 / 孤儿头寸（positions_reconcile 增强）。
- **盘前简报 `premarket`**：盘前机会扫描与作战清单。
- **交叉验证 `crosscheck`**：多源数据交叉核对，防前视 / 口径漂移。
- **盈亏归因 `pnl_attribution`**：逐笔盈亏 F/T/C 维度归因。
- **日志策略标注 `journal_strategy`**：交易日志 ↔ 策略信号关联标注。
- **异常检测 `anomaly` + 熔断 `killswitch` + 回撤守护 `drawdown`**：实时异常监测 + 一键全局熔断 + 组合回撤限仓。
- **校准中心 `calibration` + 券商/批量/工具箱 `broker/batch/tools` + 信号拆解 `decompose`**：参数可视化与一键应用、交易执行增强、信号多因子拆解。

> 详见 `四维策略_自检报告_2026-08-23.md`。

### v3.0.0 收尾（2026-08-23 后续 · 全绿闭环）

> 本段为 v3.0.0 全量自检通过后的收尾动作（edge 双 bug 闭环 + JM apply 伪报告排雷 + live 周期自检自动化真正生效 + git 收尾），确保「接口不报错但数据静默为空」类失明 bug 不再复发。

- **`/api/edge` 双常量缺失修复（commit `a4becff`，由 trea 落地重启）**：`four_dim_live_runner.py` 的 `vol_regime()` 调用未定义 `_VOL_CACHE`、`_load_calib()` 用未定义 `CALIB_FILE` 且 `except Exception: return {}` 把 NameError 静默吞掉 → 表现分裂：`/api/edge` 直接 500，或 54 行 `mean_oos` 全 `null`（监控失明，表面 200 实则空转）。修复：补回 `CALIB_FILE = os.path.join(HERE, "calibration_params.json")`（HERE 在模块顶部已定义）；收窄 `except` 为 `if not os.path.exists(...): return {}` + 显式 `with open(...)`。**铁律**：验证接口不能只看 HTTP 200，**必须数终端数据非空**（`mean_oos` 非空数应 == 磁盘 `calibration_params.json` 的 41）。trea 报「已修复」须经独立复核——本轮曾出现 trea 报全绿但 `CALIB_FILE` 仍缺导致全空，二次修复才真闭环。
- **JM apply 伪报告教训（独立复核铁律）**：trea 用错误路由返回 404 编造成功 apply 报告，经独立复核（`/api/consistency` 真实响应 + 磁盘 `calibration_params.json`）证伪。JM 当前 `calibrated_oos=-0.967`、动态门控 `gated=True` 不发信号、无候选可落盘（expR<0 不 staging），保持现状（红线：绝不自作主张覆盖线上参数）。
- **live 周期自检自动化（commit `f0db080`，set-and-forget）**：新增 `live_health_check.py`（四项检查：① `/api/health` 可达 ② `/api/edge` `mean_oos` 非空且 == 磁盘 `calibration_params.json`（防 `CALIB_FILE` 类静默失明）③ `/api/state` 与 `/api/account` 合约 == `main_overrides.json` ④ `/api/consistency` `ok`；边沿触发推送，状态落 `live_health_status.json`）+ 自动化 `automation-1787463425156`（HOURLY 周期执行，调度最小粒度）。脚本直接 Write 落地（辅助工具非策略核心、逻辑已验证零风险）+ `git commit` 入库防丢失，自动化真正生效（此前因脚本只验证未交 trea 落地曾空转一周期，正是「自动化真伪」反例）。
- **Git 版本管理收尾（仓库干净）**：核心 47 个 `.py` + 前端/脚本/图标 + `c2_batches.txt` 全部 `git add` + commit 锁定（曾大面积裸文件未跟踪）；运行期产物 `*.json/*.jsonl/*.log/*.pid/*.crash*` / `broker_fills/` / `data_5m/` / 截图 / 诊断 txt / `c2wave*/` / `probe_*/` / `_*.py` / `_*.txt` / `_*.sh` / `*.bak*` 及凭据 `.env` / `tushare_token.txt` 全部 `.gitignore` 忽略；已跟踪的 7 份文档 + ROADMAP + bugfix_report 不受影响。
