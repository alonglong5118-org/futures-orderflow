# 四维策略盯盘模型 · 自我介绍

> 🏷️ **当前版本 v3.0.0（2026-08-23 全量自检通过）**：v2.5.0 全部能力继承，新增自动参数优化 / 市场状态机 / 账户自愈 / 盘前简报 / 盈亏归因 / 异常·熔断·回撤 / 校准中心 / 信号拆解（详见「功能汇总」v3.0.0 升级要点）。

> 我是「四维策略盯盘模型」——Ken 的日盘 / 夜盘全天候期货信号雷达与复盘助手。
> 半自动：我只出信号和复盘，**不替你下单**；你看信号 → 在交易软件手动开平 → 面板记账对比。
> 非投资建议，所有输出仅供参考，风险自担。

---

## 一、我是谁

我把「下单前四维自检卡」自动化成一套 **信号发生器 + 风控闸门 + 复盘闭环**：

- **F 维度 · 基本面（背景偏置）**：基差 + 库存 + 龙虎榜净持仓，盘前刷新一次，给方向背景。
- **T 维度 · 技术面（主触发）**：日线 8 策略合成 + 实时 5m 触发，配 `regime`（趋势 / 波动 / 震荡）权重。
- **C 维度 · 资金流（确认 / 否决）**：minishare 实时净流差分 + 龙虎榜持仓定位，同向加成、反向打折。**2026-08-15 升级**：CZCE(FG/SA) 现已由独立进程 `backend_tqsdk.py`（端口 8742）生产**交易所逐笔真 tick** 灌入 C 维流量（DCE 的 JM/J/jd/lh 受权限限制走 akshare 分钟线近似，标注 `akshare_min`）；开启 `TICK_FEED_ENABLED=1` 零改动接入。
- **合成 `bias_G`**：多维修正后的综合偏置，过 **4 道硬风控闸**（T 阈值 / 相关性闸门 / 涨跌停距 / 仓位上限）才出结构化信号（方向 + 手数 + 止损 + 止盈 + 理由）。
- **止损止盈自动算（30min ATR 自适应）**：信号 / 开仓的止损基准用 **30 分钟 ATR**（5m 太小会被秒扫、日线太大），夜盘 resample 算、样本不足回退日线 ×0.40、地板 ≥日线 ×0.25；手数仍按日线 ATR。**每次开仓自动算好 stop / t1（平半）/ t2（全平）并记账，无需你手动填。**

---

## 二、我的面板（12 大书签 · 黑金工业玻璃风）

浏览器打开 `four_dim_live.html`（端口 **8741**），顶部一排书签切换：

1. **账户总览**：**动态权益**（= 同步权益 + 浮动盈亏实时推算，上下板块同源同步）/ 资金使用率 / 持仓保证金 / 距组合上限 / 已实现盈亏；**行情状态行**显示实时·同步时间 / 实时·X分钟前数据(琥珀) / 离线(红)；**组合风险热度行**显示计划风险R合计 / 当前未实现R vs 风险预算，超标红字「勿加仓」；当日未同步权益会红字提醒；下方**只显示有持仓的品种**（不堆砌空仓），每品种带**合约代码**（避免开错合约）+ **止损 / t1 / t2 与目标价** + **移动止损状态(保本 / 跟踪 / 达t2)**，实盘价触及任一档时该行高亮 🔥。
2. **信号**：55 品种实时卡，触发高亮 + 方向 / 手数 / 止损 / 止盈；支持**搜索过滤**；卡上显示**合约代码**；触发卡带 **⚡ 按此信号开仓** 按钮（一键按「品种+方向+价格」预填，杜绝手动选错品种/方向）；点品种名可跳**席位态度榜** / **持仓行**。
3. **席位态度**：龙虎榜前 20 会员净持仓排名（C_pos 维度），按交易日回看历史；与信号卡**双向跳转**。
4. **交易记录**：开仓 / 平仓流水，来源标注（信号 / 手动 / 账户同步）；**开仓时止损止盈由系统自动算好并填好，不再手填**；信号来源下拉按「品种+方向」过滤、显示 MM-DD HH:MM，防勾错别的品种。
5. **异动**：全市场涨跌幅 + 振幅排名，领涨 / 领跌榜；点品种名**反向跳回自己的持仓行**。
6. **预警监控（新增）**：五类风险预警集中一页——**自选到价提醒**（非持仓品种也能挂「上破/下破」条件，触发即弹窗+语音+消息，可重置/暂停/删除，最多 60 条）、**换月预警**（持仓合约距交割月天数，正常·留意·紧急三级）、**跳空风险预警**（止损距 vs 历史隔夜跳空 p90，高·中·低三级，隔夜/过周末风险提示）、**分品种 Edge + 波动率 regime**（OOS 期望 R、稳健认证、持仓/关注标记、ATR% 波动分位、低/中/高/极高 regime）、**报警历史**（按类型回看全部报警：触价/移动止损/自选到价/跳空风险/风险超标）。
7. **复盘**：**日 / 周 / 月** 三级纪律复盘闭环（每日 23:20 自动冻结 → 周五 15:10 聚合周卡 → 月末 15:10 聚合月卡），每卡含综合评分（A–D）、C1–C6 六维纪律检查、完整操作流水、当前持仓明细；**回看下拉仅显示 2026-08-11 及以后的复盘（此前无记录）**。
8. **绩效**：净值曲线 + 11 指标（累计净盈亏 / 胜率 / 最大回撤 / 当前回撤% / 回撤天数 / Sharpe(估算) / Calmar / 平均 R / 利润因子 / R 分布 / 累计手续费）+ **CSV 一键导出**（成交 / 绩效 / 复盘）；绩效与 R 倍数均为**净数字**（已扣交易所手续费）；净值曲线标「是否创新高」；**组合相关性矩阵**（持仓品种日收益率相关系数，红=同向集中风险·绿=反向分散）、**价差套利监控**（纯碱 9-1 / 玻璃-纯碱 / 焦煤-焦炭 的当前价差·60 日均值·z-score·趋势(扩大·收窄)）、**情景压力测试**（系统性 -3% / -5% · 黑色板块 -4% · 农产品 -3% · 单品跌停 -4%，估算冲击后动态权益与击穿止损数）；**日盘 / 夜盘绩效分解**（G4：按平仓时段拆盈亏 / 胜率 / 笔数，逐品种 `by_symbol`——鸡蛋/生猪无夜盘、玻璃/纯碱/焦煤/焦炭有夜盘，看清楚钱是哪个时段赚的）。
9. **消息**：💬 逐日聊天流（信号 + 持仓触价报警 + 移动止损状态切换），可回看任意历史日。
10. **健康（新增）**：系统健康与高级工具集中一页——**数据质量监控**（#14 各品种陈旧/冻结/异常/跳变 + 整体健康度）、**事件/数据日历闸门**（#13 未来24h 宏观/产业/交易所事件 + 减仓/禁开建议）、**纪律自动体检**（#12 blunder 检测 + A–D 评分）、**蒙特卡洛权益曲线置信区间**（#11 终值分位带 + 最大回撤分布 + 盈利/破产概率）、**回测可视化**（#17 水下曲线 + 逐笔 R 散点）、**手机推送**（#15 Telegram/Bark/企业微信通道状态 + 测试）。
11. **利润·价差·比价（G1）**：表格展示 7 项产业链指标——玻璃-纯碱加工利润 / 焦煤-焦炭化利润 / 生猪自繁自养利润 / 蛋鸡养殖利润 / 玻璃-纯碱比价 / 焦煤-焦炭比价 / 螺卷差（rb-hc），列：最新值 / 30 日均值 / z-score / 趋势（扩大·收窄），一眼看清黑色 / 农产品产业链利润结构与比价偏离。
12. **我的品种（G5）**：聚焦 6 个关注品种（鸡蛋 jd / 生猪 lh / 玻璃 FG / 纯碱 SA / 焦煤 JM / 焦炭 J）的一屏聚合盯盘，按 5 维关注度打分（信号活跃度 / 波动 / 持仓 / 价差偏离 / 风险热度）高亮排序，附现价 / 方向 / 当日盈亏 / VaR% / ATR% / 信号状态快照。

---

## 三、我的风控与边界

- **仓位状态机**：NORMAL → WARNING → LOCKED。保证金破红线 / 当日亏 5% / 连亏降档 / 回撤过大自动锁死或降档；LOCKED 一律不许新开仓。
- **持仓触价报警（三档）**：实盘价触及 **止损 / t1（平半）/ t2（全平）** 任一档时，立即弹窗 + 语音播报 + 桌面通知，并有防重复守卫（同一档不重复弹）。
- **移动止损自动（t1→保本 / 盈利跟踪 / t2 全平）**：持仓盈利触及 t1(1R) 自动把止损上移至开仓价（保本，后续不再亏本金）；盈利继续扩大按 1R 跟踪上移锁定利润；触及 t2(2R) 标记「达t2」并提示全平。状态实时显示在账户总览「移动止损」列（仅调整面板止损位，**不向经纪商下单**）。
- **组合风险热度 + 风险预算硬上限（A1/B4）**：实时汇总所有持仓的「计划风险R合计 + 当前未实现R」对比账户风险预算（risk_pct×权益）。热度 >80% 警戒（琥珀）、>100% 或单品种占用超限即「超标」红字「勿加仓」。仅提示，**不自动减仓**。
- **手续费净盈亏（A2）**：每笔成交自动估算并扣除交易所手续费，净值 / R 倍数 / 期望值均为**真实净数字**，不再虚高。
- **复盘纪律卡**：把「管住手」从机器约束补成你能看见自己守没守纪律的闭环（日 / 周 / 月评分 + 六维检查）。
- **不自动下单**，下单永远在你手里；**不预测**，只做情景分析 + 风控闸门。
- 屏蔽 OOS 负期望品种，不给信号。所有内容**非投资建议**。
- **OCO 自动下单（第三批）暂缓**：自动下单需经纪商 API 接入，当前模型坚持「不自动下单」原则（移动止损也只调面板止损位、不直连券商），待具备条件再做；所有开平动作仍在你手里。

---

## 四、怎么用我盯盘

1. 启动面板（端口 8741），浏览器打开。
2. 顶栏看风控状态（绿 NORMAL / 黄 WARNING / 红 LOCKED）—— 红色别开新仓。
3. 信号书签看触发卡（带合约代码），异动书签找广度热点。
4. 看中信号 → 点卡上 **⚡ 按此信号开仓**预填（或交易软件手动下单）→ 面板「交易记录」开仓即可，止损止盈已自动算好记录。
5. 持仓价触止损 / t1 / t2 时面板会**弹窗 + 语音**提醒你决策；收盘后到「复盘」书签回看当天 / 当周 / 当月纪律表现。

> 提示：重启后头几分钟 C 资金流在暖机；面板已改为**先行启动**，重启 3 秒内即可打开，信号随后填充。Python 后端改动需 `launchctl kickstart`，HTML 改动刷新即生效。

---

## 五、近期升级（2026-08-13 · 重校准 / 滑点 / 执行质量 / 自适应恢复）

本段为当日 6 项升级摘要（详见「功能汇总」§十七 与「架构梳理」§4.1/§4.10/§4.12）。

1. **真重校准 5 个失效品种（真正重拟合，非只出报告）**
   - 关键陷阱：校准参数有**双源**——`calibration_params.json` 的 `T_thresh` 只给 `/api/edge` 展示用；**真正驱动 live 触发门槛的是 `four_dim_strategy.py` 的 `thresholds_by_symbol`**。本次两处都改了。
   - 结果：JM 12→14、hc 12→14（二者为模型+实盘双确认**真死**，维持屏蔽）；zn 12→34、eb 14→16、al 28→12（后三者近期 walk-forward 转正，可解禁——但需新真实交易走通才自动解禁）。

2. **流动性敏感滑点（成本透明）**
   - 固定 `slip_pts=1` → 改为 `get_slip_pts(symbol)` 按流动性分级：A 档 1.0（jm/rb/FG 等超流动）、B 档 1.5（jd/ni/zn 等中流动）、C 档 2.0（lh/AP/eg 等薄流动）。回测与信号 `cost` 字段均用其双向扣，并透出「滑点占止损距 %」。

3. **执行层成交质量监控**
   - 信号记录 `entry_ref`（信号时刻价）→ `trade_journal` 的 `_fill_quality()` 算：不利滑点 / 超预算滑点 / 滑点占 R% / 成交时机 / 手数偏离比 / 出场质量 / 执行偏差标记；`/api/journal` 的 `compare.fill_quality` 已上线，前端交易记录卡展示。

4. **前端滑点档 + 重校准态展示**
   - Edge 卡片新增「滑点档」（A/B/C 彩色）与「校准态」（🔧已重校准 / ↻自适应恢复 / ✓稳健 / 已屏蔽）两列，每品种成本与状态一目了然。

5. **流动性档位真实成交量复核（修出真 bug）**
   - 跑 `recompute_liquidity_tiers` 用近 60 日日均成交额重排 53 品种，暴露并修复：①**大小写陷阱**——原 `LIQUIDITY_SLIP` 写 `"EB"` 但 `SYMBOLS` 主键小写 `"eb"`，`get_slip_pts("eb")` 静默回退全局 1.0（已改全小写键 + 查 `.lower()`）；②**手工档位失准**——lh 2.0→1.0、jd 1.5→1.0、J 1.0→2.0、FG/SA 1.0→1.5，按真实成交额数据驱动重排。

6. **JM/hc 自适应恢复（硬禁但保留定义，未来自动解禁）**
   - JM/hc 加入 `DISABLED_SYMBOLS`（保留品种定义不删），`AUTO_RECOVER_SYMBOLS={JM,hc}`；每 3600s 跑 `recovery_check`（walk-forward expR≥0 且 胜率≥45% 且 样本≥10）转正则自动从禁用集移除、恢复出信号并发通知；前端显示「↻自适应恢复」徽章。

---

## 六、十七项模型功能缺口全补完（2026-08-13 · #1–#17）

一次性把模型「该解决没解决」的 17 项功能缺口全部补完，分四批：

**风控 / 仓位（#1 #4 #5 #6）**
1. **#1 逐品种止损止盈 + regime 系数联合校准**：按品种真实 walk-forward 数据联合拟合止损/止盈/regime 系数，写回 `stop_rr_overrides.json` / `stop_rr_calibration_full.json`。
2. **#4 fractional-Kelly 仓位缩放**：按期望 R 与盈亏比算 Kelly 分数，再打折缩到纪律手数。
3. **#5 组合级硬熔断 KillSwitch**：账户权益自峰值回撤 ≥15% 或当日亏 ≥8% 或连亏 ≥6 笔 → **全平 + 禁开 + 永不自动恢复**，落盘 `killswitch_state.json` 防重启洗白；与软降档状态机（NORMAL/WARNING/LOCKED 可自动恢复）分工。
4. **#6 板块集中度 + 单边净敞口上限**：组合层四道闸（总风险预算 / 相关性同向桶 / 板块集中度 / 单边净敞口）补齐后两道，单板块风险 ≤权益×1.2%、单边净敞口 ≤权益×1.5%、单板块最多 3 品种。

**执行 / 交易（#7 #8 #9）**
5. **#7 大单拆分 / 冰山 / TWAP**：`execution_planner.py` 按流动性档位×每分钟量给拆片建议（1.0档参与率3%帽40手 / 1.5档2%帽15手 / 2.0档1%帽6手），单笔超 3 片自动切冰山（只露 1 片）。
6. **#8 一键全平 / 一键反手**：只出清单不代下单，反手手数走 risk_gate+scale，风控锁定时开仓腿不记账。
7. **#9 经纪商成交回灌**：读结算单/成交明细 CSV（中文表头归一 + 成交编号去重），apply 才落账到 journal + 账户追踪器，免费路线不依赖专业版行情账户。

**绩效 / 体检（#10 #11 #12 #17）**
8. **#10 绩效多维分解**：`perf_breakdown.py` 按方向/时段/品种/板块/市场状态/出场原因/持仓时长/星期/状态×方向 9 维切 R 统计，给出最赚/最亏/建议砍掉切片。
9. **#11 蒙特卡洛置信区间**：对 R 序列 bootstrap 重抽样 → 权益曲线分位带(5/25/50/75/95) + 终值 p5/p50/p95 + 最大回撤分布 + 盈利/破产概率；样本太薄自动回退 papertrack 160 笔。
10. **#12 纪律自动体检**：扫成交记录揪「逆信号手动开仓/无止损裸奔/超手数/频繁反手/连亏加仓(martingale)/单笔≥3R 大亏」等 blunder，A–D 扣分评级。
11. **#17 回测可视化**：水下曲线(drawdown underwater) + 逐笔 R 散点（红盈绿亏），权益曲线与回撤同图。

**系统可靠性（#13 #14 #15 #16）**
12. **#13 事件/数据日历闸门**：内置宏观(非农/CPI/EIA/FOMC) + 产业(钢联库存/USDA/生猪周度) + 交易所日历，事件前 1h 禁开、2h 减半仓。
13. **#14 数据质量/陈旧监控**：自我追踪各品种行情新鲜度，检测断流(陈旧)/价格卡死(冻结)/错误价(跳变/异常)，给整体健康度。
14. **#15 手机推送**：Telegram / Bark(iOS) / 企业微信三通道，信号、熔断、风控告警推到手机；配置走 `push_config.json` 或环境变量。
15. **#16 进程看门狗**：`watchdog.sh` 崩溃自动重启 + 卡死健康检查(/api/health 90s 无响应强杀重启)；runner 内置停摆自杀线程交看门狗接管。
16. **#2 模型健康分解**：F/T/C 留一消融定位退化维度。
17. **#3 tick 订单流接入**：盘口级订单流(Delta/盘口失衡/吸收)进 live C 维度。

---

## 七、启动修复 + 交易记录↔账户总览打通（2026-08-13 下午 · v2.0.1）

1. **交易记录 ↔ 账户总览 打通**：以前「交易记录」录入只写 `trade_journal.json`（算 R/绩效），「账户总览」持仓只读 `account_state.json`——两套脱钩。现在 `/api/journal` 的 entry/exit **同步写 account_tracker**（entry→open/add、exit→close/reduce），在「交易记录」录开平仓，账户总览持仓表自动更新。
2. **录入即自动填止损止盈**：「交易记录」entry 未带止损时，自动按 30min ATR 算好 `止损 / t1(平半) / t2(全平)` 填进去（与「账户总览」表单同逻辑），杜绝无止损裸奔。
3. **触价通知链路确认**：`check_position_alerts()` 每 60s 遍历持仓，实盘价触及止损/t1/t2 自动弹窗+语音+消息流+报警历史（配了手机推送还会推到手机）。你只报开单，止损止盈自动填、到点位自动通知。
4. **启动脚本修复**：`start.sh` 看门狗模式忽略多余参数（修中文注释被当参数透传的崩溃）；runner 两处 `global` 修复（函数内给模块级变量 `last_recover`/`DAY_OPEN_EQUITY` 赋值必须加 `global`，否则 UnboundLocalError）。

---

*—— 四维策略盯盘模型 v2.0.1 · 2026-08-13（十七项功能缺口 #1–#17 补完 + 交易记录↔账户总览打通）*

---

## 八、全量自检修复 + G1–G5 功能补齐（2026-08-14 · v2.2.0）

在 v2.1.5（#119–#134 增量缺口补全）基础上，做了一次**全量代码自检**并补齐 5 项此前没想到 / 没加入的功能：

**全量自检修复（5 处真实 bug，逐条读源码复核 + 编译验证）**
1. **看门狗心跳失效**：`main` 内给模块级 `LAST_CYCLE_TS` 赋值漏 `global`，只更新了局部变量，看门狗永远读到 0.0、卡死判定永不触发 → 已补 `global`。
2. **部分平仓脱钩**：`record_exit` 忽略 `lots` 参数，半自动 t1 平半 / 减仓时 journal 与账户脱节 → 已实现部分平仓拆分（原 trade 减手数、新增一条已平记录）。
3. **动态权益双计**：`account_tracker` 重复计入同步时刻浮动盈亏 → `set_equity` 存 `float_at_sync`、`snapshot` 扣减。
4. **VaR 分母口径不一致**：`portfolio_var` / `correlation_breakdown_stress` 用静态权益做 VaR 分母 → 改用持仓价算动态权益。
5. **止损 regime 硬编码**：`_auto_levels` 止损档位写死 → 改 `fd.classify_regime` 动态判定（趋势 / 波动 / 震荡）。

**G1–G5 功能补齐**
- **G1 利润·价差·比价**：新建 `fundamental_metrics.py`，7 项产业链指标（玻璃-纯碱加工利润 / 焦煤-焦炭化利润 / 生猪自繁自养 / 蛋鸡养殖 / 玻璃-纯碱比价 / 焦煤-焦炭比价 / 螺卷差），每项 latest / 30 日均值 / z-score / 趋势；前端「利润·价差·比价」书签。
- **G2 交易笔记**：成交记录加 `note` 字段 + `update_trade`（白名单、RLock 防死锁）+ `/api/journal_list` 列表端点；前端成交明细「备注」按钮即时写。
- **G3 重校准自动调度**：`_recalibrate_tick()` 接入主循环（受 `auto_recalibrate` / `recalibrate_interval_h` 控制），不再仅限 CLI 工具箱——面板常驻即自动周期检测校准漂移。
- **G4 日盘/夜盘绩效**：`trade_journal.session_performance()` 按平仓时段拆分；`SYMBOLS` 注入 `night` 字段作单一真值源（消除 `NO_NIGHT` / `_NIGHTLESS` 双集合漂移）；前端绩效页「日盘 / 夜盘绩效分解」卡。
- **G5 关注度盯盘板**：`focus_board()` 5 维打分（信号 / 波动 / 持仓 / 价差偏离 / 热度），前端「我的品种」书签聚焦 6 关注品种一屏聚合。

> 本机实测跑通（2026-08-14）：`import four_dim_live_runner` 无报错；面板启动正常、G3 重校准日志可见触发；12 书签（含 G1/G5 两个新 tab、G2 备注、G4 日盘夜盘）均渲染正常。

---

## 九、面板二次自检修复 + 订单流真实 tick 升级（2026-08-15 · v2.2.6）

这一天做了一次面板二次自检（P0=0、P1/P2-3/P2-4 细节修复）+ 订单流数据层升级（Stream3），全部为**管道 / 前端 / 数据层**修复，未改动任何 F/T/C 权重、regime 过滤、品种启停等策略 / 风控逻辑：

**P1 配色 / 语义收敛（前端）**
- 前端 47 处颜色字面量收口为语义变量：`--red:#ff4757` 仅表盈利 / 上涨 / 多头 / 买入；`--green:#2ed573` 仅表亏损 / 下跌 / 空头 / 卖出；`--amber:#ffa502` 表风险 / 告警。修正了西方配色反向——开仓(entry)=红、平仓(exit)=绿，与你习惯的中国期货「红涨绿跌」一致。

**P2-3 子模块刷新 + 主循环并发（前端）**
- 复盘页 F1 多策略表新增 **+30s 轮询**、报警历史新增 **+15s 轮询**，子模块数据不再依赖整屏刷新才更新。
- 主 `refresh()` 由串行 `await` 逐接口拉取改为 `Promise.allSettled([...])` **并发**拉取，慢接口不再拖累整屏加载。

**P2-4 JM 合约错配修复（后端 `_xs_recent_signal_price`）**
- 根因：信号记录缺合约月份字段 + TTL 上线前的老信号（无 `valid_minutes`）被 `annotate_signal_ttl` 永久判为「未过期」，函数永远返回冻结价 1323 与 JM2609 最新收盘 1515 比对，偏差 −12.7% 持续误报「合约错配」（在 #126 多源交叉校验里反复弹错）。
- 修复：跳过过期 / 历史信号；无 `valid_minutes` 的老信号用 `DEFAULT_SIGNAL_TTL_MIN=120` 作过期基准；重启验收通过，#126 不再误报 JM 合约错。

**Stream3 订单流真实 tick 升级（数据层）**
- 新增 `backend_tqsdk.py`（**独立进程 · 端口 8742**）作为真 tick 生产者：TqSdk 订阅真实 tick 写 `tick_stream.jsonl`，**CZCE(FG/SA) 走交易所逐笔真 tick**，**DCE(JM/J/jd/lh) 因逐笔 tick 权限 / 稳定性受限走 akshare 分钟线近似**（标注 `akshare_min`，分钟级非逐笔），休市日读本地缓存回放。
- 主面板零改动接入：runner 配 `TICK_FEED_ENABLED=1` 即 `tail` 该 jsonl 灌进 `FEED.flow[sym]`，主面板代码一行不动。
- Tailer 断点自愈：读到 `tick_stream.jsonl` 被 truncate 重写（`size < self.last_read`）时归零续读，防整屏订单流冻结。
- 启用 / 停用：`bash start_tqsdk.sh` 启动生产者、`stop_tqsdk.sh` 停止；未启用时自动回退原 minishare 5m 差分代理。
- 末次实测：87899 + 98047 双活 jsonl 共 1140 行，C 维流量组信号质量提升。

---

## 十、组合 VaR 升级 + 风控健壮性修复 + OOS 最终态（2026-08-17 · v2.3.0）

本日对「组合 VaR 风控模块 + 风控健壮性 + 品种启停最终态 + 5m 回测死循环」做了一次集中收口（详见「升级同步记录_2026-08-17」与「架构梳理」§4.1/§4.3/§4.12）。全部改动已落地并通过逻辑自测 + 真实在线验收；live runner 重启链 PID 40277→81016→81954→82305→**82784**（FRESH_OK，模块全部重载）。

**A. 组合 VaR 模块升级（③①②，hy3 / kimi-k3 协作）**
- **③ 阈值全参数化**：`portfolio_risk_pct` / `kelly_max` / `kelly_min` 接入 `trade_config.json`，消除代码内硬编码常量（`four_dim_strategy.py` 新增 `_tc_num` / `_load_tc`，回测路径也能读到配置）。
- **① VaR 历史模拟法 + EVT 尾部压力**：组合 1 日 95%/99% VaR 由参数法正态升级为 **250 日历史模拟法**，并叠加 **EVT（广义帕累托 GPD，矩估计免 scipy）** 尾部外推（取严叠加只加不减）；`var_method=param` 或样本不足自动回退旧参数法。`/api/var` 返回键不变，VaR 闸与逐仓增量闸无感兼容。
- **② 协方差矩阵缓存**：双层缓存（`_VAR_RETS_CACHE` 收益率 + `_VAR_DATA_CACHE` 对齐矩阵/协方差），高频逐仓 VaR 调用只补载新品种。**真实环境加速约 1075×（1611ms→1.5ms）**。
- **验收**：QA 41 断言 ALL PASS；真实在线验收 var_95_pct=**0.85%**、cvar_95_pct=1.24%、var_99_pct=1.48%，cap=3.3% 余量 **+2.45pp**（不会误拦截）。

**B. 全市场 OOS 日线版应用（品种启停最终态）**
- **解禁 JM/ag/bu/ss**（整改后日线转正 + Δ>+0.02 + 样本≥30 + 5m 不退化 + 回撤改善）。
- **加禁 MA/PR**（整改后日线 on 负且较 v12 退化，PR 另 5m 双负）。
- **DISABLED_SYMBOLS 最终态 = 11 个**：`{au, i, eg, m, a, b, rr, RM, hc, MA, PR}`；**AUTO_RECOVER_SYMBOLS = {hc}**（JM 已解禁，仅留 hc）。

**C. 5m 回测死循环修复（fundamental_feed.load 加 `_load_cache`）**
- 根因：`fundamental_feed.load` 无进程内缓存，每 bar 重算/重载基差库存 → SR/TA/al/c/sp/v/y/zn 等 8 品种 `walk_forward_backtest_5m_exit` 卡死。
- 修复：加 `_load_cache` 进程内缓存 → 上述品种 5m 回测 **10–23s 跑通**，OOS 全量报告收敛。

**D. 风控健壮性修复（3 处 P0 / P1）**
1. **风控状态机 P0 修复**：`RiskStateMachine` 补初始化 `daily_loss_locked=False`（原漏初始化，运行期触发 `AttributeError` 风险）。重启验收 FRESH_OK。
2. **非交易时段通知门控**：新增 `_should_suppress(topic)` + `_market_open_now()`，非交易时段静默信号/报警推送（持仓触价、自选到价等高优先级不动）；`_should_push` 单测覆盖。
3. **止损止盈方向异常修复**：根因——止损方向配置入口无校验，多空方向反填。在 `trade_journal.record_entry` + `account_tracker._auto_levels` + 返回前最终方向校验 `_validate_levels` 三层补校验；成功修复时静默配置异常通知（避免重复刷屏）。

*—— 四维策略盯盘模型 v2.3.0 · 2026-08-17（组合 VaR 历史模拟+EVT+全参数化+协方差缓存 · OOS 全市场扫描最终态 11 屏蔽 · 5m 回测死循环修复 · 风控状态机 P0 + 非交易时段门控 + 止损方向校验）*

---

## 十一、认知层五件套 + 波动率动力学 + 合约权威覆盖（2026-08-17夜 ~ 08-18 · v2.4.0）

在 v2.3.0 基础上，为信号增加「认知层」与「波动率动力学」，并落实**主力合约自动切换（用户最高优先级铁律）**：

**A. 认知层五件套（#1/#4/#5/#6/#7，全部 live 实测）**
1. **#1 信息维度**：`info_dimension.py` 读取限产/疫情/USDA 等外部采集（`fetch_info_dimension.py`，系统3.9+akshare）→ `f_override_for()` 算出信息分 F_info **只喂 live 信号**（`pipe["F_source"]=info_override`），**绝不喂回测**（零前视红线）。
2. **#6 跨资产宏观语境**：`macro_context.py` 用沪深300/国债10Y/美元/铜的 20 日变化合成 `macro_bias`（股/债/汇三因子 tanh 归一），对 `bias_G` **温和微调**（±12 分以内，不改方向）；宏观偏多时利多远月偏多品种。
3. **#7 HMM 市场状态**：`regime_hmm.py` 用 GaussianHMM(4) 对日线收益+波动聚类，识别 **trend_up / trend_down / choppy / high_vol** 四态，按态调整触发阈值（震荡/高波动更严、趋势更松）。
4. **#7 GBM/GARCH 波动率动力学**：`gbm_garch.py` 拟合 **GARCH(1,1)** 得当前波动率与持续性，再做 **GBM 蒙特卡洛前向模拟**（5/10/20 日）给出期望收益 / **95% VaR** / 上行概率 / 价格区间；波动率分档（low/normal/high/extreme）→ **`risk_scale`（0.6~1.0）已接入实际手数**——高波动品种（如当前 jd/SA）信号触发时手数自动 ×0.8，与 FSM/回撤/事件取较严者。
5. **#4 信号解释**：`signal_explain.py` 为每条信号生成**结论先行**的解释（技术触发 / 基本面含信息维度 / 资金流 / 风控手数 / 校准漂移），面板和消息流直接可读。
6. **#5 一致性看门狗**：`consistency_watchdog.py` 每 tick 检查「服务中的 T 阈值与基线漂移 / 缺 OOS 校验 / broken 仍服务 / 校准陈旧」四类问题，**只报告不自动改**（如当前 lh/JM broken 仍服务、lh/SA 缺 mean_oos——如实暴露，待人工处理）。

**B. 主力合约自动切换（🔴 用户最高优先级铁律）**
- **面板与 feed 钉合合约永远等于真实主力**（akshare 权威源），换月自动滚动、不可回退旧月、不可假自动。
- 落地：`main_overrides.json` 权威覆盖层（55 品种）→ `minishare_live` 三解析路径优先采用 → `/api/state` **每次请求实时解析**；采集器每 2h 自动重写（单调向前防换月 OI 横跳）；**换月异常自动弹 macOS 通知**（异常旧月 6h / 源失败·临界 7天状态变化推送，不刷屏）。
- 自检口径：**永远以 `/api/state` 的 `symbols[].contract` 实时字段为准**。
- 例外（正确设计）：你的持仓品种（AP/FG/SA/c/rb）钉死开仓合约不滚（滚动会破坏止损/目标跟踪）；SA09/SA01 是固定合约追踪器（fixed 模式）。

## 十二、风控口径修复 + 看门狗语义 + 持仓对账 + 面板版本动态化（2026-08-18 · v2.5.0）

在 v2.4.0 基础上，修复用户实测发现的多个问题并补防再犯机制：

1. **heat_pct 预算口径修复**：`compute_heat` 曾把「单笔风险% 1.5」误当「组合总风险预算」，导致 5 笔计划风险合计被算成「预算使用 91.8%」虚高误报（实际保证金仓位仅 ~10%）。已改用 `PORTFOLIO_RISK_PCT` 4.5% 组合预算，正确值 30.7%。**口径铁律**：`risk_pct`(单笔1.5%) ≠ `portfolio_risk_pct`(组合4.5%) ≠ `usage_rate`(保证金仓位%)。
2. **看门狗语义精确化**：`consistency_watchdog.py` 新增 `RECAL_GRACE_DAYS=7`（近期主动重校的品种不重复报 divergence），并把 `broken_serving` 拆成「真在服务失效模型」与 `broken_gated`（已被动态门控压制、风险已控，仅提示不计 ok）→ 看门狗 `ok=true`。
3. **持仓对账防漂移**：新增 `POST /api/positions_reconcile` 手动对账端点（提交 CTP 真实持仓清单 → 重建 account_state + journal），加 `account_marketsync` 持仓超期>7天提醒——解决「在 CTP 客户端手动平仓后 runner 残留幽灵持仓」的结构性漂移。
4. **面板版本号动态化**：`APP_VERSION` 常量经 `/api/state` 实时下发，前端侧栏 `navver` 动态渲染，杜绝「文档升级漏改面板版本标签」。
5. **P3 数据源暂缓**：Tushare 采集器骨架 `fetch_macro_context_full.py` + `tushare_token.txt` 就位；实测 token 积分不够期货接口（需 2000 积分），且 `macro_bias` 只用三因子（hs300/国债/汇率，akshare 已覆盖），原油/南华/USDA 不进 bias → 暂缓。

*—— 四维策略盯盘模型 v2.5.0 · 2026-08-18（风控口径修复 heat_pct 预算 · 看门狗语义精确化 broken_gated · 持仓对账防漂移 positions_reconcile · 面板版本动态化 APP_VERSION · P3 数据源暂缓）*

---

## v3.0.0 升级要点（2026-08-23 · 全量自检通过）

> 运行系统 `APP_VERSION=v3.0.0`（runner + 前端面板 `navver` 已动态渲染）。以下为相对 v2.5.0 的增量能力；v2.5.0 既有能力（信息维度 / 事件缩放 / 漂移闭环 / 信号解释 / 一致性看门狗 / 宏观语境 / HMM / GBM-GARCH / 主力合约权威覆盖）全部继承并持续运行。

- **自动参数优化 `auto-optimize`**：一键扫描优化信号阈值，含 lock / toggle / reset 防护，auto-stage + 人工一键 apply，绝不自作主张覆盖线上参数。
- **市场状态机 `market-state`**：HMM 宏观状态实时可视化 + 切换日志。
- **账户自愈 `account_sync` / `account_heal`**：与 CTP 账户对账同步 + 自动修复幽灵持仓 / 孤儿头寸（positions_reconcile 增强）。
- **盘前简报 `premarket`**：盘前机会扫描与作战清单。
- **交叉验证 `crosscheck`**：多源数据交叉核对，防前视 / 口径漂移。
- **盈亏归因 `pnl_attribution`**：逐笔盈亏 F/T/C 维度归因。
- **日志策略标注 `journal_strategy`**：交易日志 ↔ 策略信号关联标注。
- **异常检测 `anomaly` + 熔断 `killswitch` + 回撤守护 `drawdown`**：实时异常监测 + 一键全局熔断 + 组合回撤限仓。
- **校准中心 `calibration` + 券商/批量/工具箱 `broker/batch/tools` + 信号拆解 `decompose`**：参数可视化与一键应用、交易执行增强、信号多因子拆解。

> 详见 `四维策略_自检报告_2026-08-23.md`。
