# 四维策略盯盘系统 · 调阅索引（定格快照）

> 🔑 **口令约定**：用户说「**调用四维策略**」或「**四维策略**」→ 立即读本索引 + 调出下列全部相关文件与最新状态。
> 📌 **定格时间**：2026-08-23 · **当前版本 v3.0.0（自动参数优化 auto-optimize + 市场状态机 + 账户自愈 + 盘前简报 + 盈亏归因 + 异常/熔断/回撤 + 校准中心 + 信号拆解；v2.5.0 全部能力继承）**。
> ✅ **最后核对**：2026-08-18 —— v2.5.0 新增：**① heat_pct 预算口径修复**（`compute_heat` 改用 `PORTFOLIO_RISK_PCT` 4.5% 组合预算，替代单笔 `risk_pct` 1.5%，修 91.8% 虚高误报；口径铁律 `risk_pct`≠`portfolio_risk_pct`≠`usage_rate`）；**② 看门狗语义精确化**（`RECAL_GRACE_DAYS=7` + `broken_serving`/`broken_gated` 拆分，门控压制品种归提示不计 ok）；**③ 持仓对账防漂移**（`POST /api/positions_reconcile` 手动对账 + `account_marketsync` 持仓超期>7天提醒，解决 CTP 手动平仓后幽灵持仓）；**④ 面板版本号动态化**（`APP_VERSION` + `/api/state` 下发 version + 前端 navver 实时渲染）；**⑤ P3 数据源暂缓**（Tushare 骨架 `fetch_macro_context_full.py` + `tushare_token.txt` 就位，token 实测积分不够期货接口）。四份模型文档同步 v2.5.0。
> 📁 **项目根目录**：`/Users/ken/WorkBuddy/futures-orderflow/`（独立目录，任意会话直接读写此绝对路径）。

---

## 0. 一句话定位

**半自动期货信号系统**：F（基本面背景）→ T（技术触发，8策略+regime）→ C（资金确认）→ `bias_G=0.6T+0.25F+0.15C` → 4 道风控闸 + 仓位状态机 + 组合级硬熔断 → 出结构化信号（方向+手数+止损+t1/t2）→ 12 书签 Web 面板（含「健康」页 + G1「利润·价差·比价」+ G5「我的品种」）→ 日/周/月复盘闭环 + 蒙特卡洛/纪律体检/回测可视化。**不自动下单，只出信号**，用户在交易软件手动开平、面板记账。

---

## 1. 核心代码（44 个 .py + 2 个 .sh）

> 行数为 `wc -l` 实测值（2026-08-13 17:55 核对）。「已接入」= 被 `four_dim_live_runner.py` import；「工具/诊断」= 独立命令行运行，不进 runner。

### 1.1 主线核心（22 个，全部已接入 runner）

| 文件 | 行数 | runner 别名 | 职责 |
|---|---|---|---|
| `four_dim_live_runner.py` | 5723 | 入口 | **主程序**：HTTP 服务 + 调度主循环 + 信号生成 + 预警 B1/B2/B3/C2/C3/C4 + 触价/移动止损/风险热度 + #1–#17 全部能力 + G1–G5（全量自检修复 5 处 + 利润价差比价/交易备注/重校准调度/日夜盘绩效/关注度盯盘）|
| `fundamental_metrics.py` | 233 | `fm` | **G1** 利润/价差/比价 7 项指标（玻璃-纯碱加工利润/焦煤-焦炭化利润/生猪自繁自养/蛋鸡养殖/玻璃-纯碱比价/焦煤-焦炭比价/螺卷差，latest/mean30/zscore/trend）|
| `four_dim_strategy.py` | 1175 | `fd` | **核心引擎**：SYMBOLS/DEFAULT_CONFIG/F/T/C/pipeline/risk_gate/exit_plan/build_signal/walk_forward 回测 |
| `strategy_layer.py` | 337 | `strat_atr` | 8 策略 + atr/rsi/sma + classify_regime + compute_strategy |
| `risk_state_machine.py` | 438 | `rsm` | 仓位状态机 NORMAL→WARNING→LOCKED + 硬熔断 KillSwitch(#5) |
| `drawdown_guard.py` | 168 | `ddg` | **#119 回撤水位线自动降险**：多档渐变缩放 + 跨重启持久化（峰值/档位落 `drawdown_state.json`） |
| `calibration.py` | 201 | `cal` | **#120 概率校准 + 置信度分层命中率**：信号 4h 方向命中率按 |bias_G| 分桶 + 可靠性图 + Brier |
| `account_tracker.py` | 251 | `at` | 持仓/权益/动态权益快照 |
| `trade_journal.py` | 663 | `tj` | 成交记录 + 手续费净盈亏 + R倍数 + 绩效 |
| `discipline_review.py` | 996 | `dr` | 日/周/月复盘闭环 + 收盘快照落账 |
| `fundamental_feed.py` | 285 | `ff` | 基本面 F（基差+库存+季节性） |
| `long_hu_bang.py` | 404 | 独立跑 | 龙虎榜 C_pos（五交易所前20会员），每日 16:30 自动化 |
| `anomaly_scan.py` | 91 | `asc` | 异动扫描（涨跌幅+振幅） |
| `minishare_live.py` | 333 | `ml` | 实时行情（rt_fut_k 快照 + 5m合成 + C_flow） |
| `four_dim_papertrack.py` | 703 | `pt` | 真实 walk-forward papertrack 评分 + 动态门控（胜率/累计R 驱动自动暂停） |
| `tick_orderflow.py` | 156 | `tof` | **#3 盘口级订单流**：Delta / 吸收 / 盘口失衡 → 分数 Δ∈[-100,100] → `FEED.flow[sym].push_tick()` 混入 C_flow |
| `account_monitor.py` | 199 | `am` | 账户监控（TqSdk 只读，可选；无接口则优雅降级手动记账） |
| `execution_planner.py` | 196 | `exp` | #7 大单拆分/冰山/TWAP 执行建议（按流动性档×委托手数） |
| `broker_import.py` | 358 | `bi` | #9 经纪商成交明细自动回灌（CSV/TXT/XLS 解析 + 去重落账） |
| `montecarlo.py` | 233 | `mc` | #11 蒙特卡洛权益曲线置信区间（R 序列 bootstrap） |
| `blunder_check.py` | 182 | `bc` | #12 纪律自动体检（H/M/L 三级 blunder 检测 + A–D 评级） |
| `event_calendar.py` | 171 | `ec` | #13 事件/数据日历闸门（reduce / no_new_open） |
| `data_quality.py` | 166 | `dq` | #14 数据质量监控（正常/陈旧/冻结/异常） |
| `push_notify.py` | 179 | `pn` | #15 手机推送（Telegram / Bark / 企业微信） |
| `backtest_viz.py` | 157 | `bv` | #17 回测可视化（权益+水下曲线 / 逐笔 R 散点） |
| `info_dimension.py` | — | `idim` | **#1 信息维度**：读 `info_dimension.json`+`info_dimension_manual.json` → `f_override_for()` F_override 喂 live（3.13 stdlib）|
| `fetch_info_dimension.py` | — | 独立跑 | **#1 采集器**：系统3.9+akshare 采集限产/疫情/USDA 等 → `info_dimension.json` |
| `macro_context.py` | — | `mctx` | **#6 跨资产宏观语境**：`compute()` 股/债/汇 20日变化 → `macro_bias`（3.13 stdlib）|
| `fetch_macro_context.py` | — | 独立跑 | **#6 采集器**：系统3.9+akshare 沪深300/国债/美元/铜 → `macro_context.json` |
| `regime_hmm.py` | — | `rhmm` | **#7 HMM 市场状态**：GaussianHMM(4) → trend_up/trend_down/choppy/high_vol（live 专属，回测不要调用）|
| `gbm_garch.py` | — | `gg` | **#7 GBM/GARCH 波动率动力学**：GARCH(1,1) MLE + GBM 前向 VaR/区间 + vol_state→risk_scale |
| `signal_explain.py` | — | `sexp` | **#4 信号解释**：`explain_signal(sig,pipe)` 结论先行解释器（纯 stdlib）|
| `consistency_watchdog.py` | — | `cwd` | **#5 一致性看门狗**：`check_consistency()` 4 类问题报告（只报告不修正）|
| `refresh_main_contracts.py` | — | 独立跑 | **合约权威采集 + 换月告警**：akshare 全市场真实主力 → 单调向前写 `main_overrides.json`；异常 osascript 通知 |

### 1.2 独立工具（命令行运行，**未接入 runner**）

| 文件 | 行数 | 职责 |
|---|---|---|
| `four_dim_calibrate.py` | 232 | 「真重校准」：walk-forward 扫最优 T_thresh + `sweep_stop_rr` 止损/RR 联合扫描（#1），dry-run 提议、不自动改源码。**#121 已接入** `/api/tools?tool=calibrate` |
| `four_dim_recalibrate.py` | 265 | 在线自适应重校准之**漂移检测**（P1-4）：产出 `calibration_drift.json` 候选参数，不直接覆盖线上校准。**#121 已接入** `/api/tools?tool=recalibrate` |
| `perf_breakdown.py` | 251 | #10 绩效多维分解：7 基础维（方向/时段/品种/板块/出场原因/持仓时长/星期）+ `with_regime` 再加「市场状态」「状态×方向」= **共 9 维**切片 + 砍切片建议。⚠️ **纯 CLI，runner 未 import，面板无对应端点** |
| `gen_papertrack_html.py` | 324 | 把 `papertrack_report.json` 渲染成自包含可视化 HTML（零依赖零联网，双击即看）。**#121 已接入** `/api/tools?tool=papertrack_html` |

### 1.3 诊断/校验脚本（只读，不写回任何文件）

| 文件 | 行数 | 职责 |
|---|---|---|
| `diag_p02_gaps.py` | 73 | P0-2 诊断：扫全部已判定信号的前向窗口，统计开盘相对前收的真实跳变幅度，验 `ROLL_GAP_PCT`/`ROLL_GAP_MULT` 阈值是否曾触发 |
| `verify_p02.py` | 67 | P0-2 复核：从固化的 `backtest_bars` 快照重放 `backtest_signal`，验 ①outcome/R 可复现（确定性修复生效）②换月跳空跳过根数稳定为 0 |
| `backend_tqsdk.py` | 8742 进程 | **Stream3 订单流真 tick 生产者**（独立进程，非 runner import）：TqSdk 订阅真实 tick → 写 `tick_stream.jsonl`（CZCE FG/SA 交易所逐笔真 tick；DCE JM/J/jd/lh akshare 分钟线近似，标注 `akshare_min`）；Tailer 断点自愈（`size < last_read` 时归零续读）；休市日读本地缓存回放。runner 设 `TICK_FEED_ENABLED=1` 后 `tail` 其输出灌进 `FEED.flow[sym]`，零改动接入 |

### 1.4 遗留（不属四维主流程，保留不删）

| 文件 | 行数 | 说明 |
|---|---|---|
| `hidden_pivot.py` | 120 | da龘 隐秘枢轴 Hidden Pivot（a-b-c 结构推 p 目标位），未接入四维 |
| `minishare_feed.py` | 361 | da龘 全品种报价，已被 `minishare_live.py` 取代 |
| `preflight_check.py` | 283 | 管住手侧 CTP 开盘前巡检（属管住手产品） |
| `minishare_permission_check.py` | 40 | minishare 端点权限自检（一次性，已完成；换 token 可复跑） |

### 1.5 Shell

| 文件 | 职责 |
|---|---|
| `start.sh` | 启动入口（默认看门狗模式；`direct` 参数走直连调试） |
| `watchdog.sh` | #16 进程看门狗：崩溃自动重启 + `/api/health` 90s 无响应强杀 |
| `start_tqsdk.sh` / `stop_tqsdk.sh` | **Stream3** 启动/停止 `backend_tqsdk.py`（nohup 拉起写 pid + `tqsdk.log`，stop 按 pid 强杀）|

---

## 2. 前端 + 文档

| 文件 | 版本 | 说明 |
|---|---|---|
| `four_dim_live.html` | v2.2.6 | **前端面板**：12 书签（账户/信号/席位/交易/异动/预警监控/复盘/绩效/消息/健康/**利润·价差·比价(G1)**/**我的品种(G5)**），黑金工业玻璃风；账户卡「回撤水位线」阶梯 + 近期信号 TTL 灰显/跟单下拉过期标记 + 绩效页「组合 VaR/CVaR」卡 + 账户页「账户只读同步」卡(minishare 盯市+对账漂移) + 健康页「📋 盘前作战清单」卡(#125) + 健康页「多源数据交叉校验」卡(#126) + 绩效页「实盘盈亏 F/T/C 维度归因」卡(#127) + 绩效页「相关性崩溃/危机趋同 Stress」卡(#128) + 绩效页「板块强弱轮动排序」卡(#129) + 绩效页「波动率目标化头寸」卡(#130) + 绩效页「流动性风险 Liquidity-at-Risk」卡(#131) + 预警页顶部「组合预警规则引擎」卡(#132：总评级+9上下文chip+最高优先级动作+逐条规则分级)  + 复盘页顶部「🎬 交互式历史回放」卡(#133：时间机器·逐日状态帧+滑块/播放回放+当日持仓/事件流+累计盈亏sparkline+单品种K线开平仓标记) + 绩效页顶部「🧭 参数鲁棒性敏感性地图」卡(#134：OAT敏感性·稳健/中等/敏感分级·逐参数期望R柱状)。**2026-08-15 前端升级（P1/P2-3/P2-4）**：配色/语义统一（`--red` 仅表盈利/涨/多/买、`--green` 仅表亏损/跌/空/卖、`--amber` 表风险告警，修正西方配色反向 entry=红/exit=绿）；复盘页 F1 多策略表 +30s 轮询、报警历史 +15s 轮询；主 `refresh()` 由串行 `await` 改 `Promise.allSettled([...])` 并发拉取 |
| `四维策略_规格草案.md` | v1.2 定稿（2026-08-15 第五次修订） | 设计层定稿（四维框架 + 风控闸 + 校准方法） |
| `四维策略模型_自我介绍.md` | v3.0.0 | 系统自我介绍（14 书签 + 风控边界 + #1–#17 补完 + 认知层五件套 + 合约权威覆盖 + 风控口径修复 + 近期升级） |
| `四维策略_思维导图.html` | v3.0.0 | 可视化思维导图（SVG） |
| `四维策略模型_功能汇总.md` | v2.5.0 | 功能汇总 + 版本演进 + 文件职责 + 升级详述（2026-08-18） |
| `四维策略_架构与数据流梳理.md` | v2.5.0 | **逐文件精读收口**（架构图+数据流+模块精读+API+算法+坑 + 认知层 + 合约权威覆盖 + 风控口径修复） |
| `ROADMAP.md` | v1.0（2026-08-18） | 进度与路线图（已完成一览 + 系统快照 + 下一步候选 P1~P4 + 红线铁律） |
| `双系统自测规划.md` | — | 管住手 + 四维 双系统自测规划 |
| `模拟盘回本100万_作战规划.md` | — | 模拟盘作战规划 |

---

## 3. 数据文件（20 个 json/jsonl + data_5m）

| 文件 | 写入方 | 说明 |
|---|---|---|
| `four_dim_live_state.json` | runner 每轮 | 面板主状态（55 品种卡 + 信号流 + 聊天流） |
| `account_state.json` | account_tracker | 权益/持仓/动态权益基准 |
| `trade_journal.json` | trade_journal | 开平仓流水 |
| `four_dim_signals.json` | runner | 信号日志 |
| `cpos_cache.json` | long_hu_bang | 龙虎榜（C_score/net/history） |
| `fundamentals.json` | fundamental_feed | 基差/库存序列 |
| `calibration_params.json` | four_dim_calibrate | 53 品种校准（mean_oos/阈值/robust） |
| `trade_config.json` | 手动 | 账户参数/合约规格 |
| `account_monitor.json` | 手动 | 账户后端配置（tqsdk） |
| `minishare.json` | 手动 | minishare token/interval |
| `minishare_cache.json` | minishare_live | 品种→代码映射缓存 |
| `live_bars.json` | minishare_live | 实时 5m 合成 K 线 |
| `watchlist.json` | watch_add | 自选到价提醒 |
| `alert_history.json` | log_alert | 报警历史（最多300条） |
| `signal_chat_feed.jsonl` + `_YYYY-MM-DD.jsonl` | append_chat | 消息流（按天归档） |
| `signal_dedup_state.json` | evaluate | 信号去重记忆 |
| `discipline_records/weekly/monthly_records.json` | snapshot_* | 复盘日/周/月落账 |
| `discipline_events.json` | discipline_review | 开仓纪律事件 |
| `killswitch_state.json` | KillSwitch(#5) | 硬熔断状态落盘（防重启洗白） |
| `drawdown_state.json` | drawdown_guard(#119) | 回撤水位线状态落盘（峰值权益/回撤%/档位/系数；lazy 创建，防重启洗白峰值） |
| `broker_import_state.json` | broker_import(#9) | 回灌成交去重状态 |
| `push_config.json` | push_notify(#15) | 手机推送通道配置 |
| `tq_config.json` | 手动 | 天勤账号 |
| `data_5m/_*.csv` | sina/本地 | 5 分钟历史缓存（FG/J/JM/SA/lh） |
| `info_dimension.json` / `info_dimension_manual.json` | fetch_info_dimension（系统3.9）/ 手动 | **#1** 信息维度采集 / 人工补录 |
| `macro_context.json` | fetch_macro_context（系统3.9） | **#6** 宏观语境近60日（沪深300/中证500/国债/美元/铜）|
| `main_overrides.json` | refresh_main_contracts（每2h） | **合约权威覆盖**（sym→真实主力 ts_code，55 品种；单调向前）|
| `calibration_drift.json` | four_dim_recalibrate（每6h） | **#3** 漂移检测 + staging 候选 proposed_T |
| `main_contract_refresh.log` / `main_contract_alert_state.json` | refresh_main_contracts | 换月刷新日志 / 告警去重冷却状态 |

---

## 4. 运行方式（关键命令）

| 操作 | 命令 |
|---|---|
| 启动面板（看门狗模式） | `bash start.sh`（崩溃/卡死自动重启，生产推荐） |
| 启动面板（直连调试） | `bash start.sh direct` 或 `/Users/ken/.workbuddy/binaries/python/envs/default/bin/python3 four_dim_live_runner.py --port 8741` |
| 浏览器访问 | `http://localhost:8741` |
| 后端改动生效 | `launchctl kickstart -k gui/$(id -u)/com.ken.futures-orderflow.live`（或重启 start.sh） |
| 前端改动生效 | 刷新浏览器即可（runner 每次 GET / 重读 HTML） |
| 龙虎榜刷新 | `long_hu_bang.py`（每日 16:30 自动化） |
| 基本面刷新 | `fundamental_feed.py`（盘前） |
| 自测 | `/tmp/test_orderflow2.py`(30断言) + `batch2`(22) + `batch3`(15) + `node --check` |

---

## 5. 关键事实速查

- **品种**：SYMBOLS=55（含纯碱逐合约 SA09/SA01）；**DISABLED=11**（OOS 负期望屏蔽：**au/i/eg/m/a/b/rr/RM/MA/PR 硬禁 + hc 自适应恢复候选**）；2026-08-17 全市场 OOS 日线版应用：解禁 JM/ag/bu/ss、加禁 MA/PR，`AUTO_RECOVER_SYMBOLS={hc}`。
- **流动性滑点**：`get_slip_pts(symbol)` 按流动性分 A/B/C 档（1.0/1.5/2.0 点，双向扣），数据驱动（近60日成交额重排），大小写不敏感。
- **自适应恢复**：`AUTO_RECOVER_SYMBOLS={JM,hc}`，每 3600s 跑 `recovery_check`（walk-forward expR≥0 且 胜率≥45% 且 样本≥10）→ 自动从 `RUNTIME_DISABLED` 解禁 + 通知。
- **bias_G 权重**：0.6·T + 0.25·F + 0.15·C。
- **止损基准**：30min ATR + 日线兜底（绝不可改回 5m）。
- **数据源边界**：minishare 仅 `rt_fut_k` 可用；历史日线/基本面走 akshare/sina。
- **C_flow 双来源**：minishare 快照净流（`dP×dOI` 差分，需≥3 根暖机）+ **#3 tick 订单流**（`tick_orderflow` 的 Delta/吸收/失衡 → `push_tick`）混合。
- **HTTP 端点**：共 **54 个** `/api/*`（GET 为主，`/api/watch`、`/api/journal`、`/api/batch`、`/api/push`、`/api/killswitch`、`/api/drawdown`(#119)、`/api/calibration`(#120)、`/api/tools`、`/api/var`(#123)、`/api/account_sync`(#124)、`/api/premarket`(#125)、`/api/crosscheck`(#126)、`/api/pnl_attribution`(#127)、`/api/stress_corr`(#128)、`/api/sector_rotation`(#129)、`/api/vol_target`(#130)、`/api/liquidity_risk`(#131)、`/api/risk_rules`(#132) + `/api/playback`(#133) + `/api/playback_kline`(#133) + `/api/sensitivity`(#134) + **G1/G2/G4/G5 新增** `/api/fund_metrics`(利润价差比价) / `/api/focus`(关注度盯盘) / `/api/journal_session`(日夜盘绩效) / `/api/journal_list`(成交明细含 note) 等支持 POST）。
- **未接入面板的工具**：`perf_breakdown.py`(#10) 仍为**命令行工具**，runner 不 import、面板无端点——要看结果需手动跑（`python perf_breakdown.py`）。其余 `four_dim_calibrate`/`four_dim_recalibrate`/`gen_papertrack_html` 已在 **#121** 接入 `/api/tools` + 健康页「工具箱」卡，可面板一键触发。
- **不自动下单**：移动止损只调面板位，不向券商下单；OCO 自动下单暂缓。
- **#5 硬熔断**：回撤15%/日亏8%/连亏6笔 → 全平+禁开+永不自动恢复（落盘 killswitch_state.json，人工 reset 唯一出口）。
- **#15 手机推送**：Telegram/Bark/企业微信，配置 push_config.json 或环境变量。
- **#16 看门狗**：`bash start.sh` 崩溃重启 + /api/health 90s 无响应强杀。
- **已知坑**：状态机须 RLock；minishare 命名 JM=焦炭/JMM=焦煤；C_flow 需≥3差分暖机；5m 需≥60根才触发。
- **v2.0.1 打通**：`/api/journal` entry/exit 同步写 account_tracker（交易记录↔账户总览）；entry 未带止损自动 `_auto_levels` 填；`start.sh` 看门狗模式忽略多余参数；runner 函数内给模块级变量赋值须加 `global`。
- **v2.2.0 全量自检 + G1–G5（2026-08-14）**：① 5 处真实 bug 修复（看门狗心跳 `LAST_CYCLE_TS` 漏 global / `record_exit` 部分平仓脱钩 / 动态权益双计 / `portfolio_var` 用静态权益做 VaR 分母 / `_auto_levels` regime 硬编码）；② **G1** `fundamental_metrics.py` 利润·价差·比价 7 项指标（`/api/fund_metrics` + 前端「利润·价差·比价」书签）；③ **G2** 成交 `note` 字段 + `update_trade`（RLock）+ `/api/journal_list` + `/api/journal` 的 `note` action（前端备注按钮）；④ **G3** `_recalibrate_tick()` 接入主循环（`auto_recalibrate`/`recalibrate_interval_h` 控制）；⑤ **G4** `session_performance()` 日/夜盘绩效 + `SYMBOLS` 注入 `night` 单一真值源（消 `NO_NIGHT`/`_NIGHTLESS` 双集合）；⑥ **G5** `focus_board()` 5 维打分（`/api/focus` + 前端「我的品种」书签）；⑦ 前端 10→**12 书签**。本机实测跑通。
- **#119 回撤水位线自动降险**：多档渐变缩放（默认 5%→0.7× / 10%→0.5× / 15%→0.0×，与 #5 硬熔断 15% 对齐）+ 跨重启持久化（`drawdown_state.json`）；可在 `trade_config.json` 的 `risk_gate.drawdown_waterlines` 覆盖；每轮喂动态权益、信号与反手手数乘以系数、`/api/drawdown` 查询、账户卡阶梯面板展示。旧 `risk_state_machine` 的「回撤>8%→WARNING→0.5×」二值分支已移除，避免双重惩罚。
- **#120 概率校准 + 置信度分层命中率**：`calibration.py` 取信号日志方向/|bias_G|/触发价/时间，在 `live_bars`(5m) 找 t+4h 后价格判方向命中，按 |bias_G| 分 4 桶（低<8/中8–14/高14–20/极高≥20）对比「名义置信度 vs 经验命中率」+ 整体 Brier；`/api/calibration` 查询（300s 缓存、?force=1/?window_h=N）；绩效页「概率校准」卡逐桶名义蓝条 vs 实际绿/红条。**实测洞察**：中自信档仅 6.9% 命中 vs 名义 62%，暴露「模型在该档严重过度自信」。
- **#121 接入已存在未接线 CLI**：`four_dim_calibrate`(真重校准扫描)/`four_dim_recalibrate`(校准漂移检测)/`gen_papertrack_html`(回测报告→HTML) 三个工具原未被 runner import、面板无入口；现顶部 import + 工具箱基础设施(`_TOOL_DEFS`/`run_tool` 后台线程 `redirect_stdout` 捕获)+`/api/tools?tool=&action=run|status`(结果落 `tools_state.json`)+健康页「工具箱」卡 3 按钮触发、结果框回看。
- **#122 信号时效 TTL 过期机制**：`signal_ttl_minutes()`(默认 120 分钟滚动窗口，读 `trade_config.json` 顶层 `signal_ttl_minutes` 可配) + `annotate_signal_ttl(sig)`(算 `expired`/`expires_at`/`remaining_min`/`age_min`，老信号缺 `valid_minutes`→不判过期向后兼容)；生成信号写 `created_at`+`valid_minutes`，`/api/signals` 返回前补算时效字段；前端「近期信号」过期整条灰显 + `⏱ 已过期/有效剩 X 分钟` 标记、持仓卡「上次信号」过期提示、跟单下拉过期项沉底并标 `🕒已过期`。**注意**：当前 200 条历史信号均 `valid_minutes:0`→全不判过期（正常向后兼容），新信号才带 TTL。
- **#123 组合 VaR / CVaR**：`portfolio_var()`(紧邻 `correlation_matrix`)——读持仓算带符号市值暴露 `X=方向×手数×现价×乘数`，各持仓日线 `pct_change` 对齐求**协方差矩阵 Σ**，组合日 P&L 方差 `σ²=XᵀΣX`；参数法 `VaR_α=z_α·σ`(正态均值=0)、`CVaR(ES)=σ·φ(z_α)/(1−α)`(解析免 scipy)，输出 95%/99% 金额+占权益% + 成分 VaR 分解(求和=总 VaR95，正红加风险/负绿对冲)；60s 缓存、无持仓/样本不足优雅降级；`/api/var`(`?force=1` 强刷) + 绩效页「组合 VaR / CVaR」卡(4 指标+成分条)。与 `compute_heat`「计划风险R(基于止损)」互补——此处是市值盯市统计 VaR。
- **#124 账户只读同步（minishare 盯市 + 对账漂移）**：用户裁定**不接券商实时 API**（太贵/暂无能力），只接 minishare 实时接口、保持半自动——故原「券商账户只读同步」重解读为「minishare 实时价对 journal 已记录持仓逐笔盯市 + 对账漂移」。`account_marketsync(force=False)`(紧邻账户函数区)取 `FEED.price(sym)`→`at.snapshot` 得动态权益/浮动/占用，逐持仓标 `mtm_ok`(实时/未订阅/行情离线/非交易时段缺口)、`drift[]`(行情偏旧>5min / 无价持仓 / 对账不一致)、`_reconcile_journal_vs_account` 用 `tj._load()` 的 `pnl=None` 开仓反推「journal 应持仓」与 `account_state` 持仓比对(账户有但 journal 无 / journal 有但账户漏记 / 手数不一致)；30s 缓存、`read_only:True`；`/api/account_sync`(`?force=1`) + 账户页「账户只读同步」卡(数据时效/只读动态权益/盯市覆盖/每持仓徽标/漂移告警/一致性结论)。与 `broker_import.py`(#9 免费路线)同源，全程不碰券商 API、不代下单。
- **#125 盘前自动作战清单**：`premarket_brief(force=False)`(紧邻 `account_marketsync`)聚合——`account_marketsync()`(只读盯市+漂移) + `compute_heat()`(风险热度) + `ddg.current()`(回撤水位线) + `rsm.RISK_FSM.summary()`/`rsm.is_halted()`(状态机+硬熔断) + `_market_phase()`(当前时段 日盘/夜盘/休市) + `_recent_signals_for_premkt()`(近期信号含 TTL 过期) + `_premkt_focus()`(关注品种关键价位：持仓 `at.load_state()` 取 avg/stop/t1/target 优先，其次未过期信号)。输出 **8 项开盘前检查 `_chk()`**(status: ok/warn/error/info)：账户权益已同步 / 实时行情(休市→info) / 回撤水位线降险(scale<1→warn) / 硬熔断(halted→error) / 组合风险热度(over→error·警戒→warn) / journal↔账户一致性(不一致→error) / 近期信号时效(过期→warn) / 当前持仓盯市(部分无价→warn)；30s 缓存、`read_only:True`；`/api/premarket`(`?force=1`) + 健康页顶部「📋 盘前作战清单」卡(时段+生成时间/权益/风险/熔断/8 项检查✅⚠️🛑ℹ️/漂移告警/关注品种关键价位表/只读来源)。纯只读、不接券商 API，与 #124 半自动定位一致。
- **#126 多源数据交叉校验**：`cross_source_check(force=False)`(紧邻 `premarket_brief`)对 `持仓 ∪ FOCUS_SYMS(6)` 每个品种**四源互相印证**——源A=`FEED.price(sym)`(minishare 实时价)、源B=`load_daily_refreshed(sym)` 末根 `close/high/low`(日线)、源C=`account_state[sym].avg`(持仓均价·journal 手动记录)、源D=`_xs_recent_signal_price(sym)`(该品种最近未过期信号 `price`)。**4 项校验**：① 实时 vs 日线收盘(仅交易时段，`max(6%, 2×当日振幅)` 动态阈值)→⚠️行情卡/合约错/复权错 ② 实时 vs 持仓均价(持仓+交易时段，`|gap|>8%`→🛑移仓漏更/合约错/账户同步错) ③ 持仓均价 vs 日线(实时不可用/休市时替代 ②，互斥防重复) ④ 信号开仓价 vs 日线收盘(`|gap|>8%`→⚠️信号时间错/合约错)。状态：含「持仓均价」矛盾→error(红·影响实盘)、其余→warn(黄)、无→ok(绿)；阈值常量 `RT_DAILY_GAP_WARN=6/RT_AVG_GAP_WARN=8/AVG_DAILY_GAP_WARN=8/SIGNAL_DAILY_GAP_WARN=8` 集中可调；60s 缓存、输出 `symbols[]`(四源数值+各 gap%+flags+status)+`summary`(checked/with_realtime/ok/warn/error/flags)。与 #14 单源质量、#124 对账互补——此处是「多源数值一致性」。不接券商 API、纯只读；`/api/crosscheck`(`?force=1`) + 健康页「多源数据交叉校验」卡(时段/覆盖/结论着色+矛盾告警红框+四源数值表)。
- **#127 实盘盈亏 F/T/C 维度归因**：`pnl_attribution(strong_thresh=20)`(落地于 `trade_journal.py`，紧邻 `session_split`)把每笔已平仓成交**按关联信号的 F(基本面)/T(触发)/C(资金确认) 三维得分**拆解实盘净盈亏来源——`aligned_k=score_k×dir_sign`(多+1/空−1)；① 主导维度分桶(每笔计入 argmax 带符号 aligned 的单一维度：F主导/T主导/C主导，无信号成交计「无信号驱动」) ② 比例贡献分解(`pnl_k=pnl×aligned_k/Σaligned`，三项和严格=该笔盈亏、反向维度得负贡献) + 占比(%) ③ 强信号有效性(`|score|≥20` 时该维度胜率/盈亏)。runner 包装 `pnl_attribution(force=False)`(30s 缓存 `_ATTR_CACHE`)、`/api/pnl_attribution`(`?force=1`)、绩效页「实盘盈亏 F/T/C 维度归因」卡(主导分桶+比例贡献条+强信号格)。只读，不接券商 API；自测 `/tmp/test_pnl_attr.py` 21 断言全过。
- **#128 相关性崩溃 / 危机趋同 stress 专项**：`correlation_breakdown_stress(rho_crisis=0.7, rho_tail=0.9, z_crisis=3.0)`(紧邻 `stress_test`，与 #123 同源：持仓日收益率协方差+带符号暴露 X)在 #123 VaR 之外专门量化「相关性维度崩溃」——**关键校正**：危机相关按每对持仓买卖方向取最不利符号 `corr_ij=ρ·sign(X_i·X_j)`(同侧+ρ/对冲−ρ)，保证危机 VaR≥正常 VaR、代表「所有腿同时向不利方向趋同」(无脑+ρ会让对冲对反而抵消更多、方差变小)。**Lens A** 危机 VaR=`var_95/99_crisis`(ρ=0.7)/`var_95_tail`(ρ=0.9)、`amplify_95/99=σ_crisis/σ_normal`(放大倍数)、`div_benefit_normal/crisis`、`div_erosion=正常−危机`(保护蒸发pp)、`avg_corr_obs`(现状平均两两相关)；**Lens B** 危机趋同情景=`loss_convergence`(每腿 z×自身波动不利、对冲全失效) vs `loss_hedged_normal`(同样 z 但用组合波动、对冲抵消)、`hedge_unwind=二者差`(对冲消失多亏) + `per_leg_adverse`；`contrib_crisis_var95`(危机边际 VaR、求和=危机VaR95)；30s 缓存(`_CORRBRK_CACHE`)。与 #92 `stress_test`(均匀百分比冲击) 互补——此处是「相关性维度」的崩溃；只读，不接券商 API；`/api/stress_corr`(`?force=1`) + 绩效页「相关性崩溃 / 危机趋同 Stress」卡(危机VaR+放大倍数+保护蒸发+对冲消失多亏+危机边际VaR条)；自测 `/tmp/test_corrbrk.py` 15 断言全过。
- **#129 板块强弱轮动排序**：`sector_rotation(cache_sec=180)`(紧邻 `spread_monitor`，与 #123 同源：`load_daily_refreshed(sym)` 取日线)把全市场品种按 `SYMBOLS[sym]['group']` 分板块做横截面强弱扫描——逐品种 `_symbol_momentum()` 算 `ret5/ret20/ret60`(5/20/60日收益) + `vol20`(20日波动) → 风险调整相对强度 `RS=ret20/vol20`；板块聚合 `mean_rs=成员RS均值/mean_ret5-20-60/up(成员ret20>0数)`；**轮动象限**按板块级 `gr20/gr60`：`accel=gr20−gr60`(中期动能−长期动能)，`gr20>0且accel>0`→**领涨加速**、`gr20>0且accel≤0`→**高位钝化**、`gr20≤0且accel>0`→**筑底回升**、`gr20≤0且accel≤0`→**领跌走弱**；板块按 `mean_rs` 降序排名，输出 `breadth_up/breadth_pct`(全市场广度)、`leading`(领涨加速板块)/`lagging`(领跌走弱板块)/`strongest`/`weakest`/`focus_sectors`(含关注品种 jd/lh/FG/SA/JM/J 的板块，加★)；剔除交割合约(SA09/SA01 等带数字品种)防板块内重复计数；180s 缓存(`_SECTOR_CACHE`)。只读，不接券商 API；`/api/sector_rotation`(`?force=1`) + 绩效页「板块强弱轮动排序」卡(顶部领涨/领跌/加速板块/走弱板块 chip + ★聚焦板块提示 + 板块排名行含强度条/RS/涨跌↑/象限徽标，黑系农产品加★高亮)；自测 `/tmp/test_sector.py` 21 断言全过。
- **#130 波动率目标化头寸**：`vol_target_position(vol_target_pct=1.0, force=False, cache_sec=120)`(紧邻 `sector_rotation`，只读 `at.snapshot()`/`load_daily_refreshed`/`_spec_mult`，不接券商 API、不代下单)给定「单品种日度盈亏波动=权益×vol_target_pct%」→ 反推目标手数 `target_lots=round(权益×vol_target_pct%/100 ÷ (现价×σ×乘数))`(σ=最后20日日收益率波动)，与当前持仓对比给 **加仓/减仓/建仓(无持仓)/持平** 建议；标的=持仓∪聚焦品种(后者显「建仓建议」)。组合层面给 `cur_portfolio_vol_indep`=√Σ(σ·lots)²(独立上界) / `cur_portfolio_vol_corr`=√(XᵀΣX)(日收益率协方差·最后20日·含对冲更低) / `target_portfolio_vol_indep`(目标独立口径)；**关键校正**：相关口径协方差须与逐品种σ同用「最后20日」窗口(初版误用全样本致口径不一致、对冲对反算大变异；修正后完美负相关对冲对 cur_corr<cur_indep、单持仓退化相等)。端点 `/api/vol_target`(`?vol_target_pct=N`默认1.0 & `?force=1`) + 绩效页「波动率目标化头寸」卡(顶部可调目标%输入框 + 组合波动三栏[相关/独立上界/目标] + 品种行：方向/σ%/现手·占比→标手·占比/加仓减仓徽标)，`refreshVolTarget()` 60s 轮询；自测 `/tmp/test_voltgt.py` 29 断言全过。
- **#131 流动性风险 Liquidity-at-Risk**：`liquidity_at_risk(force=False, cache_sec=180)`(紧邻 `vol_target_position`，只读 `at.snapshot()`/`at.load_config()['contract_specs'][sym].multiplier`/`load_daily_refreshed(sym)` 的 `volume`/`oi`/`close`，不接券商 API、不代下单)基于**平方根冲击模型**量化「清仓的隐性成本与可行性」——逐持仓 `ADV=mean(volume[-20])`(20日均量)、`σ=std(close.pct_change()[-20])`(日波动)、`参与率=平仓手数/ADV`；**冲击成本占比=`β×√(参与率)×σ`**(β=0.15 经验冲击常数、上限50%)、`LaR=名义金额(手数×现价×乘数)×成本占比`；**清仓天数=`ceil(手数 / max(ADV×20%, 1))`**(单日最多交易 20% ADV 以避免冲击)；**流动性分层**：参与率>0.5 或 清仓天数>3 → 低流动性⚠️ / >0.15 → 中等 / 否则 高流动性；参与率>20% 标 `impact_warn`(单日冲击超阈)。组合层面：`total_laR=Σ腿LaR`(独立加总，非相关抵消)、`total_laR_pct_equity`、`n_illiquid`(低流动性品种数)、`n_impact_warn`(单日冲击超阈数)、`worst_symbol`(参与率最高)、`max_leg_symbol`(LaR占权益最高)；180s 缓存(`_LAR_CACHE`)。与 #123 VaR(市场波动风险)、#130(目标波动) 互补——此处是「退出时的流动性冲击成本与能否快速平仓」。端点 `/api/liquidity_risk`(`?force=1`) + 绩效页「流动性风险 Liquidity-at-Risk」卡(组合LaR+低流动性品种数+单日冲击超阈数+最差参与率 四 chip + 品种行：方向/手数/ADV/冲击成本·元/清仓天数/流动性分层徽标)，`refreshLiqRisk()` 60s 轮询；自测 `/tmp/test_lar.py` 29 断言全过。**附带修复**：`vol_target_position` 与 `liquidity_at_risk` 原缓存赋值误用 `_X_CACHE={...}` 局部重绑定，二次 `force=False` 命中会 `UnboundLocalError`；改为 `_X_CACHE["t"]=...` 原地修改（与 `_XSRC_CACHE` 模式一致）
- **#132 组合预警规则引擎**：`evaluate_risk_rules(force=False, prices=None, cache_sec=30)`(`_RISK_RULES_CACHE` 30s 缓存，紧邻报警历史区，复用 `at.snapshot`/`compute_heat`/`rsm.RISK_FSM.summary`/`ddg.current`/`rsm.KILL.summary` 口径，不接券商 API、不代下单)把散落各模块的 12 类风险信号**统一收口为「一套可配置规则」**：逐条 `_eval_rule()` 评估 → 按 `RULE_SEVERITY_RANK` 定级 `正常(绿)/提示(蓝)/关注(黄)/警告(橙)/危险(红)`，给出最高优先级动作建议。内置 12 条（`heat_over`>100%·`heat_watch`>80%·`kill_halted`·`fsm_locked`·`fsm_warning`·`daily_loss`>5%·`daily_loss_warn`>3%·`consec_losses`≥4·`drawdown`≥5%·`single_concentration`>30%·`usage_high`>60%·`position_count`≥8）；**可配置**：`trade_config.json` 的 `risk_rules` 段 `overrides`(按 id 改 `on/threshold/severity/action/notify`)+`extra`(自定义规则)。**节流报警** `_risk_rules_notify()` 仅在「未触发→触发」且 `notify=True` 时写一次报警历史(`log_alert(kind="规则引擎")`，`ALERT_KINDS` 已加筛选项)，致命规则(heat_over/kill_halted/fsm_locked/daily_loss)因已有专用播报设 `notify=False` 不重复；并接入 `_update_aux` 每轮评估(成本极低)，面板不打开也能把软预警写入「预警」页历史。端点 `/api/risk_rules`(`?force=1`) + 预警页顶部「组合预警规则引擎」卡(总评级徽标+9上下文chip：动态权益/资金占用率/风险热度/状态机/回撤/当日亏损/连亏/持仓数/单品占比+最高优先级动作条+逐条规则：严重度色点·名称·当前值 op 阈值·触发/正常+触发展开建议)，`refreshRules()` 30s 轮询；自测 `/tmp/test_risk_rules.py` 32 断言全过。**实测修复**：初版 `_eval_rule` 未带 `notify` 标志致 `_risk_rules_notify` 对全部规则默认 `notify=True`、致命规则被错误写历史；已在评估 dict 补 `"notify": rule.get("notify", True)`。。
- **#133 交互式历史回放（时间机器）**：`build_playback_timeline(force=False, cache_sec=60)`(`_PLAYBACK_CACHE` 60s 缓存，紧邻规则引擎，只读 `tj.JOURNAL_FILE`/`SIGNAL_LOG`/`ALERT_HISTORY_FILE`/`load_daily_refreshed`/`at.load_config()['contract_specs'][sym].multiplier`，不接券商 API、不代下单)把**交易记录+信号历史+报警历史**按时间合并，重建 [最早事件,最晚事件] 区间内**每一自然日的组合状态帧**：逐帧算 `realized_pnl_to_date`(已平累计净盈亏) + `open_positions`(当日未平持仓，日线收盘市值重估 `running_pnl=(收-开)×乘数×手数×方向`，离线仅已实现) + `equity_to_date=base+已实现+浮盈`(base=500000 或 `trade_config.json` 的 `playback_base_equity`) + `events`(当日开/平仓/信号/报警明细) + 各计数。另 `playback_kline(symbol, asof)` 返回某品种截至 asof 的日线 OHLC + 开/平仓标记(best-effort，离线 ok=False)。端点 `/api/playback`(`?force=1`) + `/api/playback_kline`(`?symbol=&asof=`) + 复盘页顶部「🎬 交互式历史回放」卡：⏮◀▶⏭ 步进 + ▶/⏸ 播放(1.2s/天) + 日期滑块；四统计(累计已实现盈亏·盈红亏绿/估算权益/持仓市值浮盈/当日未平仓) + 当日持仓表 + 当日事件流(🔔/🟢/🔴/⚠️) + 累计盈亏 sparkline + 单品种 K 线 canvas(截至当日含开平仓圆点)；`refreshPlayback()` 60s 轮询。自测 `/tmp/test_playback.py` 40 断言全过（帧序列/逐日已实现累加/持仓重建/市值重估/权益/per-day 事件计数/K线裁剪/开平仓标记/空历史 note/离线仅已实现/缓存命中）。
- **#134 参数鲁棒性敏感性地图（OAT）**：`parameter_sensitivity(force=False, symbols=None, tail=200, cache_sec=1800)`(`_SENS_CACHE` 30min 缓存 + `_SENS_LOCK`，复用 `fd.walk_forward_backtest` 评估口径，不接券商 API、不代下单)对 `SENS_PARAMS` 4 关键参数做「一次一变」扫描：`risk_gate.stop_atr_mult`(止损ATR 1.0–2.5)/`risk_gate.rr_ratio`(盈亏比 1.0–3.0)/`account.risk_pct`(单笔风险% 0.5–3.0)/`risk_gate.kelly_slope`(Kelly斜率 1.0–3.0) 各 5 档，基准配置=`fd.DEFAULT_CONFIG`+`at.load_config()`；每档对聚焦品种(默认 FG/SA/JM/jd，`?symbols=` 可覆盖)跑回测聚合 `expR/total_R/n_trades/win_rate`。**敏感性** `sens=(max(expR)-min(expR))/max(0.01,|base_expR|)`，分级 `稳健<0.3/中等<0.8/敏感`，按敏感性降序。回测较重→daemon 线程执行，先返回 `status=running` 再 `done`。端点 `/api/sensitivity`(`?force=1&symbols=FG,SA`) + 绩效页顶部「🧭 参数鲁棒性敏感性地图」卡(🔄重新计算+基准指标行+逐参数：稳健/中等/敏感徽标+敏感度+5档期望R柱状，基准档蓝高亮/非基准盈红亏绿)，`refreshSensitivity()` 自动触发+4s 轮询；自测 `/tmp/test_sensitivity.py` 36 断言全过（running→done/基线指标/敏感性降序 stop最敏感·kelly最稳健/每参数5值恰一is_base/极值点/walk_forward调用=84/无效品种→error/缓存命中）。
- **v2.4.0 认知层 + 波动率 + 合约权威（2026-08-17夜~08-18）**：**#1 信息维度** `info_dimension.f_override_for()` 喂 live F（`pipe["F_source"]=info_override`，不喂回测）；**#6 宏观语境** `macro_bias`（股/债/汇 20日变化 tanh 归一）→ `bias_G += mb×12.0` 温和微调；**#7 HMM 市场状态** `regime_hmm.compute_label` 4 态 → T_thresh_eff ×0.90~1.25；**#7 GBM/GARCH** `gbm_garch.compute` GARCH(1,1) MLE + GBM 前向 5/10/20日 VaR/P_up/区间，vol_state→risk_scale(0.6~1.0) **已接入实际手数** `_combined=min(scale,dd_scale,ev_scale,_gbm_scale)` 取严者，`sig["gbm_risk_scale"]` 独立暴露；**#4 信号解释** `signal_explain.explain_signal` → `sig["explanation"]`；**#5 一致性看门狗** `/api/consistency` 4 类问题（只报告不修正）；**🔴 主力合约权威覆盖（用户最高优先级铁律）** `main_overrides.json` + 三路径优先采用 + `/api/state` 实时解析 + 单调向前 + SA09/SA01 fixed + 持仓钉开仓合约 + **换月异常告警**（ABNOLD 6h / FETCHFAIL·临界 7天状态变化推送，osascript）。自检口径永远以 `/api/state` 的 `symbols[].contract` 实时字段为准。
- **v2.5.0 风控口径修复 + 看门狗语义 + 持仓对账 + 面板版本动态化（2026-08-18）**：① **heat_pct 预算口径修复** `compute_heat` 改用 `PORTFOLIO_RISK_PCT`(4.5% 组合预算) 替代单笔 `risk_pct`(1.5%)，修「91.8% 虚高误报」；口径铁律 `risk_pct`≠`portfolio_risk_pct`≠`usage_rate`；② **看门狗语义精确化** `consistency_watchdog.py` 加 `RECAL_GRACE_DAYS=7`（近期重校不报 divergence）+ `broken_serving`/`broken_gated` 拆分（门控压制归提示不计 ok）；③ **持仓对账防漂移** 新增 `POST /api/positions_reconcile` 手动对账端点 + `account_marketsync` 持仓超期>7天提醒（解决 CTP 客户端手动平仓后 runner 残留幽灵持仓，踩坑：POST 端点须加在 do_POST 非 do_GET）；④ **面板版本号动态化** `APP_VERSION` + `/api/state` 下发 version + 前端 `navver` 实时渲染；⑤ **P3 数据源暂缓** Tushare 骨架 `fetch_macro_context_full.py` + `tushare_token.txt` 就位（token 实测积分不够期货接口，macro_bias 只用三因子故暂缓）。
- **P1 配色/语义收敛（2026-08-15 面板二次自检）**：前端 47 处颜色字面量收口为语义变量——`--red:#ff4757` 仅表盈利/上涨/多头/买入；`--green:#2ed573` 仅表亏损/下跌/空头/卖出；`--amber:#ffa502` 表风险/告警。修正西方配色反向：entry(开仓)=红、exit(平仓)=绿（与中国期货红涨绿跌一致）。不改动任何策略/风控逻辑。
- **P2-3 子模块刷新 + 主循环并发（2026-08-15）**：复盘页 F1 多策略表 **+30s 轮询**、报警历史 **+15s 轮询**；前端主 `refresh()` 由串行 `await` 逐接口改 `Promise.allSettled([...])` **并发**拉取，慢接口不再拖累整屏。不改动任何策略/风控逻辑。
- **P2-4 JM 合约错配修复（2026-08-15 · `four_dim_live_runner.py` `_xs_recent_signal_price`）**：原老信号（`valid_minutes` 缺省）被 `annotate_signal_ttl` 永久判「未过期」，永远返回冻结价 1323 与 JM2609 最新收盘 1515 比对，偏差 −12.7% 持续误报合约错配。修复：跳过过期/历史信号；无 `valid_minutes` 老信号用 `DEFAULT_SIGNAL_TTL_MIN=120` 作过期基准；重启验收通过（#126 多源交叉校验不再误报 JM 合约错）。
- **Stream3 订单流真实 tick 升级（2026-08-15）**：新增 `backend_tqsdk.py`（**独立进程·端口 8742**）作为真 tick 生产者——TqSdk 订阅真实 tick 写 `tick_stream.jsonl`，**CZCE(FG/SA) 走交易所逐笔真 tick**，**DCE(JM/J/jd/lh) 走 akshare 分钟线近似**（标注 `akshare_min`，分钟级非逐笔），休市日读本地缓存回放。runner 设 `TICK_FEED_ENABLED=1` 后 `tail` 其输出灌进 `FEED.flow[sym]`，**零改动接入**（主面板代码不碰）。启用：`bash start_tqsdk.sh` 启动生产者 + runner 配置 `TICK_FEED_ENABLED=1` 重启；`stop_tqsdk.sh` 停止。Tailer 断点自愈（`size < self.last_read` 归零续读），防 `tick_stream.jsonl` 被 truncate 重写后整屏订单流冻结。未启用时回退原 minishare 5m 差分代理。

---

## 6. 调阅动作模板（说「调用四维策略」后执行）

1. 读本索引文件。
2. 列核心文件 + 报当前版本（v2.3.0）+ 运行状态（curl 8741；订单流真 tick 另 curl 8742 看 TqSdk 生产者）。
3. 给出一句话架构概览（§0）。
4. 按用户后续指令深入具体文件（核心引擎/前端/复盘/预警/健康工具等）。

*—— 四维策略 调阅索引 · 定格 v2.5.0 · 2026-08-18（风控口径修复 heat_pct 预算 · 看门狗语义精确化 broken_gated · 持仓对账防漂移 positions_reconcile · 面板版本动态化 APP_VERSION · P3 数据源暂缓）· 文件清单末次实测核对 2026-08-18（新增 fetch_macro_context_full.py/tushare_token.txt/tushare_cache.json 三产物；四份模型文档版本号对齐 v2.5.0）*

---

## v3.0.0 升级要点（2026-08-23 · 全量自检通过）

> 运行系统 `APP_VERSION=v3.0.0`（runner + 前端面板 `navver` 已动态渲染）。以下为相对 v2.5.0 的增量能力；v2.5.0 既有能力（信息维度 / 事件缩放 / 漂移闭环 / 信号解释 / 一致性看门狗 / 宏观语境 / HMM / GBM-GARCH / 主力合约权威覆盖）全部继承并持续运行。

- **自动参数优化 `auto-optimize`**：一键扫描优化信号阈值，含 lock / toggle / reset 防护，auto-stage + 人工一键 apply，绝不自作主张覆盖线上参数。
- **市场状态机 `market-state`**：HMM 宏观状态实时可视化 + 切换日志。
- **账户自愈 `account_sync` / `account_heal`**：与 CTP 账户对账同步 + 自动修复幽灵持仓 / 孤儿头寸（positions_reconcile 增强）。
- **盘前简报 `premarket`**：盘前机会扫描与作战清单。
- **交叉验证 `crosscheck`**：多源数据交叉核对，防前视 / 口径漂移。
- **盈亏归因 `pnl_attribution`**：逐笔盈亏 F/T/C 维度归因。
- **日志策略标注 `journal_strategy`**：交易日志 ↔ 策略信号关联标注。
- **异常检测 `anomaly` + 熔断 `killswitch` + 回撤守护 `drawdown`**：实时异常监测 + 一键全局熔断 + 组合回撤限仓。
- **校准中心 `calibration` + 券商/批量/工具箱 `broker/batch/tools` + 信号拆解 `decompose`**：参数可视化与一键应用、交易执行增强、信号多因子拆解。

> 详见 `四维策略_自检报告_2026-08-23.md`。
