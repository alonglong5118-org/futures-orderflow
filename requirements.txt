# 核心依赖（生产/回测必需）
# 版本对齐生产运行时实测（ken env, Python 3.13.12, pip freeze 2026-09-14）：
#   numpy 2.5.1 / pandas 3.0.5 / scipy 1.18.0 / hmmlearn 0.3.3 / tqdm 4.69.0 / setuptools 83.0.0
# 注意：numpy 2.5.1 / scipy 1.18.0 要求 Python >=3.12，pandas 3.0.5 要求 >=3.11。
numpy==2.5.1
pandas==3.0.5
scipy==1.18.0
hmmlearn==0.3.3
plotly>=5.0
tqdm==4.69.0

# 可选数据源（按需安装，CI 不装）
# akshare>=1.10
# tqsdk>=2.0

# 遗传算法优化（可选）
# deap>=1.3

# pip-audit 安全扫描要求（升级到 80.9+ 修复 PYSEC-2026-3447 高危漏洞）
setuptools==83.0.0
