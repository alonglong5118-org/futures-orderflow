# 四维策略模型 功能汇总（v3.0.0 · 2026-08-23 · 自动参数优化 + 市场状态机 + 账户自愈 + 盘前简报 + 盈亏归因 + 异常/熔断/回撤 + 校准中心 + 信号拆解）

> 定位：半自动信号系统（**不自动下单**，出信号 → 你在交易软件手动开平 → 面板记账复盘）。
> 运行：独立实盘面板 `four_dim_live_runner.py` + `four_dim_live.html`，端口 **8741**。
> 覆盖：**55 品种**（含纯碱逐合约 SA09 / SA01），**44 激活 / 11 屏蔽**。
> 行情：minishare 实时（rt_fut_k 快照差分），回退 akshare 日线。
> 主题：黑金工业玻璃风；前端 14 书签导航（含总览驾驶舱首页 + 模拟盘沙盒）；复盘页「交互式历史回放」已高级化重构。

---

## 一、版本演进（关键节点）

| 版本 | 时间 | 关键更新 |
|---|---|---|
| **v3.0.0** | 08-23 | **自动参数优化 auto-optimize（lock/toggle/reset 防护）· 市场状态机 market-state · 账户自愈 account_sync/account_heal · 盘前简报 premarket · 交叉验证 crosscheck · 盈亏归因 pnl_attribution · 日志策略标注 journal_strategy · 异常检测 anomaly + 熔断 killswitch + 回撤守护 drawdown · 校准中心 calibration · 券商/批量/工具箱 broker/batch/tools · 信号拆解 decompose**（v2.5.0 全部能力继承并持续运行） |
| v1.3 | 08-11 夜 | 从 da龘 合并 4 能力（仓位状态机 / 异动扫描 / 账户监控 / 可选通知）|
| v1.5.2 | 08-11 夜 | 龙虎榜 C_pos 真正对接（`long_hu_bang.py` → `cpos_cache.json`）|
| v1.5.3–1.5.8 | 08-12 | 席位态度榜↔信号卡、持仓卡↔信号卡 双向跳转；逐合约持仓拆分；合约代码显示（持仓卡 + 信号卡）|
| v1.6.0 | 08-12 | UI 重做：6 书签标签导航 + 高级深色主题 |
| v1.6.1 | 08-12 | 黑金工业玻璃风重配色 |
| v1.6.2 | 08-12 | 复盘每日 23:20 自动冻结落账 |
| v1.6.3 | 08-12 | 复盘 日→周→月 三级闭环 + 复盘页重排版 |
| v1.7.0 | 08-12 | 复盘记录完整化与详细展示（合约 / 来源 / 持仓时长 / 盈亏）；生猪平仓补记；面板先行启动（秒开）；账户总览只显持仓 |
| v1.7.1~1.7.13 | 08-12 夜 | 30min ATR 自动算止损/t1/t2、动态权益上下同步、触价三档报警、⚡一键开仓+防误选、复盘回看 cutoff 8/11、去手填止损止盈、第一批高性价比（净值曲线+11指标+R倍数追踪+行情健康指示+CSV 导出） |
| v1.8.0 | 08-12 夜 | 第二批：组合相关性矩阵、价差套利监控、情景压力测试、移动止损自动（t1→保本/跟踪/达t2）；OCO 自动下单暂缓 |
| v1.8.1 | 08-13 | 组合风险热度 + 风险预算硬上限（A1/B4）、手续费净盈亏（A2）、当前回撤%/持续天数/新高（A4） |
| v1.9.0 | 08-13 | 预警监控标签页：自选到价提醒(B1)、换月预警(B2)、跳空风险预警(B3)、分品种 Edge+波动率 regime(C2/C3)、报警历史(C4)；9 书签 |
| v1.9.1 | 08-13 | **六大升级**：① 真重校准 5 品种(T阈值双源落盘·JM/hc真死) ② 流动性敏感滑点 get_slip_pts A/B/C ③ 执行层成交质量监控(entry_ref+fill_quality) ④ 前端滑点档+重校准态双列 ⑤ 流动性档位真实复核(修大小写bug+数据驱动重排) ⑥ JM/hc 自适应恢复(转正自动解禁) |
| v2.0.0 | 08-13 | **十七项功能缺口 #1–#17 全部补完**：风控(#1止损止盈联合校准/#4 fractional-Kelly/#5 硬熔断KillSwitch/#6 板块集中度+净敞口)、执行(#7 大单拆分冰山TWAP/#8 一键全平反手/#9 经纪商回灌)、绩效(#10 多维分解/#11 蒙特卡洛/#12 纪律体检/#17 回测可视化)、可靠性(#13 事件日历闸门/#14 数据质量/#15 手机推送/#16 进程看门狗)；新增「健康」书签（10 书签） |
| v2.0.1 | 08-13 | **交易记录↔账户总览打通**：`/api/journal` entry/exit 同步写 account_tracker（entry→open/add、exit→close/reduce）；entry 未带止损自动 `_auto_levels` 填止损止盈；`start.sh` 看门狗模式忽略多余参数；runner 两处 `global` 修复（last_recover/DAY_OPEN_EQUITY UnboundLocalError） |
| v2.0.2 | 08-13 | **增量缺口补全启动**（16 项头脑风暴缺口，逐一带号实现）：`#119` 回撤水位线自动降险（graduated 多档 + 跨重启持久化，`drawdown_guard.py` + `/api/drawdown` + 账户卡阶梯面板）；后续 #120–#134 依次推进 |
| v2.0.3 | 08-13 | 增量缺口补全推进：`#122` 信号时效 TTL 过期机制（`signal_ttl_minutes` + `annotate_signal_ttl` + `/api/signals` 补算 + 近期信号灰显/跟单下拉过期标记）；#119–#122 已落地 |
| v2.0.4 | 08-13 | 增量缺口补全推进：`#123` 组合 VaR / CVaR（参数法 1 日，`portfolio_var()` + `/api/var` + 绩效页「组合 VaR / CVaR」卡 + 成分 VaR 分解）；#119–#123 已落地 |
| v2.0.5 | 08-13 | 增量缺口补全推进：`#124` 账户只读同步（**minishare 实时盯市 + 对账漂移检测**，不接券商 API、不改写约束）= `account_marketsync()` + `_reconcile_journal_vs_account()` + `/api/account_sync` + 账户页「账户只读同步」卡；#119–#124 已落地 |
| v2.0.6 | 08-13 | 增量缺口补全推进：`#125` 盘前自动作战清单（聚合只读账户盯市/风险热度/回撤水位线/状态机/硬熔断/行情时效/信号过期 → 8 项开盘前检查 + 关注品种关键价位）= `premarket_brief()` + `_market_phase()`/`_chk()`/`_premkt_focus()` + `/api/premarket` + 健康页「盘前作战清单」卡；#119–#125 已落地 |
| v2.0.7 | 08-13 | 增量缺口补全推进：`#126` 多源数据交叉校验（**四源互相印证**=minishare 实时价/日线收盘/持仓均价(journal)/信号开仓价，检测移仓漏更·合约错·行情卡·复权错）= `cross_source_check()` + `_xs_recent_signal_price()` + `/api/crosscheck` + 健康页「多源数据交叉校验」卡；#119–#126 已落地 |
| v2.0.8 | 08-13 | 增量缺口补全推进：`#127` 实盘盈亏 F/T/C 维度归因（每笔已平仓成交按关联信号 F(基本面)/T(触发)/C(资金确认) 三维得分，拆实盘净盈亏来源）= `pnl_attribution()`(trade_journal.py) + `pnl_attribution()` runner 包装(30s 缓存) + `/api/pnl_attribution` + 绩效页「实盘盈亏 F/T/C 维度归因」卡（主导维度分桶 + 比例贡献分解 + 强信号有效性）；#119–#127 已落地 |
| v2.0.9 | 08-13 | 增量缺口补全推进：`#128` 相关性崩溃 / 危机趋同 stress 专项（在 #123 VaR 之外，把全部非对角相关抬至危机相关 ρ 并**按每对持仓买卖方向取最不利符号**(同侧+ρ/对冲−ρ)→「所有腿同时向不利方向趋同」，重算 VaR 量化分散化保护蒸发；并给波动率放大+对冲失效的危机趋同情景(美元)）= `correlation_breakdown_stress()` + `/api/stress_corr` + 绩效页「相关性崩溃 / 危机趋同 Stress」卡(危机VaR+放大倍数+保护蒸发+对冲消失多亏+危机边际VaR)；#119–#128 已落地 |
| v2.1.0 | 08-13 | 增量缺口补全推进：`#129` 板块强弱轮动排序（全市场品种按 `SYMBOLS[sym]['group']` 分板块，逐品种算多周期动量 5/20/60 日→风险调整相对强度 `RS=20日收益/20日波动`→板块 RS 均值排名；轮动象限按「中期动能(20d)正负 × 加速(20d−60d)正负」分 4 类：领涨加速/高位钝化/筑底回升/领跌走弱；聚焦板块(含关注的黑系/农产品)加★）= `sector_rotation()` + `/api/sector_rotation` + 绩效页「板块强弱轮动排序」卡(顶部领涨/领跌/加速/走弱+★聚焦 + 板块排名行含强度条/涨跌/象限)；#119–#129 已落地 |
| v2.1.1 | 08-13 | 增量缺口补全推进：`#130` 波动率目标化头寸（给定「单品种日度盈亏波动=权益×vol_target_pct%」反推每品种目标手数，与当前持仓对比给加仓/减仓/建仓建议；组合层面给当前组合日波动=相关口径(含对冲更低)/独立上界 + 目标组合日波动）= `vol_target_position()` + `/api/vol_target`(`?vol_target_pct=N`) + 绩效页「波动率目标化头寸」卡(可调目标% + 顶部组合波动三栏 + 品种现手/目标手/占比/调仓建议行)；#119–#130 已落地 |
| v2.1.2 | 08-13 | 增量缺口补全推进：`#131` 流动性风险 Liquidity-at-Risk（基于**平方根冲击模型**：清仓冲击成本 ≈ 名义金额 × β×√(平仓量/ADV)×日波动，ADV=20日均量；并给出按 ≤20%×ADV/日 减仓的「清仓所需天数」与低流动性预警）= `liquidity_at_risk()` + `/api/liquidity_risk` + 绩效页「流动性风险 Liquidity-at-Risk」卡(组合LaR+低流动性品种数+单日冲击超阈数+最差参与率 + 品种行：方向/手数/ADV/冲击成本·清仓天数/流动性分层)；#119–#131 已落地 |
| v2.2.0 | 08-14 | **全量代码自检 + G1–G5 功能补齐**：5 处真实 bug 修复（看门狗心跳 `LAST_CYCLE_TS` 漏 global / `record_exit` 部分平仓脱钩 / 动态权益双计 / `portfolio_var` 用静态权益做 VaR 分母 / `_auto_levels` regime 硬编码）；**G1** 利润·价差·比价指标（`fundamental_metrics.py` 7 项 + `/api/fund_metrics`）· **G2** 交易笔记（`trade_journal` 加 `note`+`update_trade` + `/api/journal_list` + `/api/journal` 的 note 动作 + 前端备注按钮）· **G3** 重校准自动调度（`_recalibrate_tick()` 接入主循环，受 `auto_recalibrate`/`recalibrate_interval_h` 控制）· **G4** 日盘/夜盘分时段绩效（`session_performance()` + `SYMBOLS` 注入 `night` 字段作单一真值源，消 `NO_NIGHT`/`_NIGHTLESS` 双集合漂移 + `/api/journal_session` + 前端日盘夜盘卡）· **G5** 关注度盯盘板（`focus_board()` 5 维打分 + `/api/focus` + 前端「我的品种」书签聚焦 6 关注品种）；前端 10→**12 书签**（新增「利润·价差·比价」「我的品种」） |
| v2.2.1 | 08-14 | **合约代码全页覆盖（硬性要求）**：所有书签页「商品名后强制带当前生效合约代码」，随换月自动滚动（动态主力/次主力，由 `minishare_live.contract_of` + `/api/symbols_meta` 解析）。补齐 3 处此前漏带的商品名——信号流水、对话推送（警报/信号标题）、硬熔断一键全平方案每条腿；对话推送 `append_chat` keep 元组补 `symbol` 字段（历史条目由前端 NAME→SYM 反查兜底）。根源修复「rb 写死 RB2609(2986) 实为 RB2610(3016)」的换月错月问题（见 v2.2.0 动态主力合约解析）。 |
| v2.2.2 | 08-14 | **面板排版/结构高级化重构（一目了然）**：① 导航由扁平 12 标签改为按功能域分组的左侧分类侧边栏（总览 / 行情研判 / 风险预警 / 交易执行 / 复盘绩效 / 我的聚焦，配色跟随分类）；② 每个书签页内部按子功能加「分区色块标题」（带分类竖条+编号+副标题，共 30 处），结构一目了然；③ 新增「总览驾驶舱」首页书签（端口 8741 · 13 书签），一屏聚合各功能域实时指标（权益 / 浮动盈亏 / 持仓 / 风控等级 / 活跃信号 / 触发规则 / 日亏·回撤 / 今日成交 / 总盈亏·胜率 / 关注品种）+ 信号速览与关注品种列表，点「进入 →」直达对应页；④ 文档就地更新（不新建文件），版本 v2.2.1→v2.2.2。 |
| v2.2.3 | 08-14 | **复盘页「交互式历史回放」高级美化**：将原先粗糙的按钮/4 等分 KPI/两列裸表/堆叠 canvas 重构为高级 dashboard 风格。① 播放控制统一为深色玻璃胶囊工具栏，播放按钮金色高亮；② 进度条改为金色滑块；③ KPI 改为 4 张悬浮光晕卡（带图标/副标题/涨跌色数字）；④ 当日持仓/当日事件各自包裹为独立卡片，列表项改为三栏卡片（方向图标 + 商品名+合约代码 + 开收价元信息 + 盈亏）；⑤ K 线与累计盈亏曲线分别独立为带标题栏的图表卡片；⑥ 持仓列表继续沿用 `cc()` 动态合约代码。 |
| v2.2.4 | 08-14 | **8 大体验增强 F1–F8（按用户要求逐个落地）**：**F1 多策略/多账户视图（已落地）**——`trade_journal.record_entry` 加 `strategy`/`account` 标签字段，`update_trade` 支持改标签；新增 `by_strategy(group_by)` 聚合（笔数/胜率/盈亏/手续费）；后端 `/api/journal_strategy` 暴露；前端开仓表单加「策略/账户」下拉，复盘页新增「多策略·多账户表现」分区（按策略/账户拆分盈亏条形对比，随切页加载）。后续 F2 导入导出 / F3 日报周报 / F4 语音播报 / F5 移动端PWA / F6 纪律打分卡 / F7 事件标记 / F8 模拟盘沙盒。 |
| v2.2.5 | 08-14 | **8 大体验增强 F1–F8 全部完成（就地更新，无新文档文件）**：**F2 导入导出**——`trade_journal.import_trades(rows)` 按 id 合并（新增/更新/跳过统计），后端 `/api/journal_import`（接收 csv 文本或 rows），导出 CSV 扩列含 strategy/account/note，交易记录页加「导入CSV」控件；**F3 日报周报**——`generate_report(kind)` 聚合账户/时段/风控/纪律/聚焦合成 Markdown，后端 `/api/report_daily`、`/api/report_weekly`，复盘页加弹窗（复制/下载.md）；**F4 语音播报**——顶栏 🔊 开关 + Web Speech API，`voiceScanSignals` 在信号触发/自选到价时防重复播报；**F5 移动端PWA**——响应式侧栏(窄屏横滑)+ manifest.webmanifest + sw.js(离线壳) + 图标(192/512)，后端静态路由；**F6 交易纪律打分卡**——复用既有 `/api/discipline`+复盘页「交易纪律打分卡」(日/周/月维度评分、C1–C6 检查项)；**F7 事件驱动标记**——`playback_kline` 增 `events`(消息流/异动/信号)，K 线叠加事件竖线+三角标记+图例；**F8 模拟盘沙盒**——`paper_open/close/state`(虚拟资金100万、逐合约单仓、独立盈亏)，后端 `/api/paper`，侧栏新增「模拟盘」书签(开/平/持仓/流水)。书签 13→14，版本 v2.2.4→v2.2.5。 |
| v2.2.6 | 08-15 | **面板二次自检 + 订单流真实 tick 升级（Stream3）**：① **P1 配色/语义收敛**（前端 47 行颜色字面量统一：红=#ff4757 仅表盈利/上涨/多头/买入、绿=#2ed573 表亏损/下跌/空头/卖出、琥珀=#ffa502 表一切风险/告警，消除"红既表盈利又表风险"误读；修正西方配色反向 entry=绿/exit=红 → 改为 entry=红/exit=绿）；② **P2-3 子模块刷新 + 主循环并发**（复盘页 F1 多策略表 +30s 轮询、报警历史 +15s 轮询，主 `refresh()` 由 6 行串行 `await` 改为 `Promise.allSettled([...])` 并发）；③ **P2-4 JM 合约错配修复**（后端 `_xs_recent_signal_price` 跳过过期/历史信号，无 `valid_minutes` 老信号用默认 `DEFAULT_SIGNAL_TTL_MIN=120` 作过期基准，消除 JM「信号价1323@08-12 vs 日线1516」持续性误报"合约错"）；④ **Stream3 订单流真实 tick 升级**：新建 `backend_tqsdk.py`（独立 TqSdk 进程·端口 8742·主线程跑行情循环）把真 tick 写 `tick_stream.jsonl`、主面板靠既有 `TICK_FEED_ENABLED=1` 零改动接入灌进 C_flow、`start_tqsdk.sh`/`stop_tqsdk.sh` 启停、修 Tailer 断点（文件 truncate 后 `last_read` 卡死）、DCE 品(JM/J/jd/lh)免费源无 tick → akshare 分钟线近似(标注 `akshare_min`)、CZCE(FG/SA)真 tick。面板 P0/P1/P2-3/P2-4 全部清零。 |
| v2.3.0 | 08-17 | **组合 VaR 升级 + 风控健壮性修复 + OOS 最终态 + 5m 死循环修复（升至 v2.3.0）**：组合 VaR 模块① 250 日历史模拟法+EVT 尾部压力（取严叠加只加不减）② 协方差矩阵双层缓存（真实环境加速 ~1075×）③ 阈值全参数化（`portfolio_risk_pct`/`kelly_max`/`kelly_min` 接 `trade_config.json`）；全市场 OOS 日线版应用（解禁 JM/ag/bu/ss、加禁 MA/PR，`DISABLED_SYMBOLS` 最终 11 个 `{au,i,eg,m,a,b,rr,RM,hc,MA,PR}`、`AUTO_RECOVER={hc}`）；5m 回测死循环修复（`fundamental_feed.load` 加 `_load_cache`，8 品种 10–23s 跑通）；风控状态机 P0（`daily_loss_locked` 初始化）+ 非交易时段通知门控（`_should_suppress`/`_market_open_now`）+ 止损止盈方向校验（`_validate_levels`/`record_entry`/`_auto_levels` 三层）。live runner 重启链 PID→82784（FRESH_OK）。 |
| v2.4.0 | 08-17夜~08-18 | **认知层五件套 + 波动率动力学 + 合约权威覆盖（升至 v2.4.0，live 全部实测）**：**#1 信息维度**（`info_dimension.py` + `fetch_info_dimension.py`，F_override 喂 live F 不喂回测）；**#6 跨资产宏观语境**（`macro_context.py` + `fetch_macro_context.py`，股/债/汇 → `macro_bias` 温和调制 bias_G，回测三处零前视）；**#7 HMM 市场状态**（`regime_hmm.py` 4 态调制 T_thresh_eff 0.90~1.25）+ **GBM/GARCH 波动率动力学**（`gbm_garch.py` GARCH(1,1) MLE + GBM 前向 VaR/区间 + vol_state→risk_scale 0.6~1.0 **接入实际手数缩放**（`min(scale,dd_scale,ev_scale,_gbm_scale)` 取严者，jd/SA high→0.8 真实减仓）；**#4 信号解释**（`signal_explain.py` 结论先行解释器挂 signal）；**#5 一致性看门狗**（`consistency_watchdog.py` `/api/consistency` 只报告不修正）；**主力合约权威覆盖**（`main_overrides.json` 权威层 + 三条解析路径优先采用 + `/api/state` 实时解析 + 单调向前防 OI 横跳 + SA09/SA01 fixed + 持仓钉死开仓合约）——**用户最高优先级铁律「以后一直自动切换」**；**换月异常告警**（ABNOLD 6h / FETCHFAIL·临界 7天状态变化推送，osascript macOS 通知）。 |
| v2.5.0 | 08-18 | **风控口径修复 + 看门狗语义精确化 + 持仓对账防漂移 + 面板版本动态化（升至 v2.5.0，live 实测）**：① **heat_pct 预算口径修复**（`compute_heat` 改用 `PORTFOLIO_RISK_PCT` 4.5% 组合总预算，替代单笔 `risk_pct` 1.5%，修「91.8% 虚高误报」而实际保证金仓位仅 ~10%；三个口径铁律 `risk_pct`≠`portfolio_risk_pct`≠`usage_rate`）；② **看门狗语义精确化**（`RECAL_GRACE_DAYS=7` 近期主动重校不报 divergence + `broken_serving`/`broken_gated` 拆分，门控压制品种归提示项不计 ok）；③ **持仓对账防漂移**（新增 `POST /api/positions_reconcile` 手动对账端点 + `account_marketsync` 持仓超期>7天提醒，解决 CTP 客户端手动平仓后 runner 残留幽灵持仓）；④ **面板版本号动态化**（`APP_VERSION` + `/api/state` 下发 version + 前端 navver 实时渲染，杜绝文档升级漏改面板标签）；⑤ **P3 数据源暂缓**（Tushare 骨架 `fetch_macro_context_full.py` + `tushare_token.txt` 就位，token 实测积分不够期货接口、macro_bias 只用三因子故暂缓）。 |

---

## 二、四维核心引擎（F + T + C → 合成）

### F 维度 · 基本面（背景偏置）
- 入口：`score_F` + `fundamental_feed.py` + `fundamentals.json`
- 输入：基差、库存（盘前刷新）；缺数据 → 中性不报错
- 作用：方向背景偏置
- **G1 利润 / 价差 / 比价指标（F 维度补强）**：新增 `fundamental_metrics.py`，7 项产业链指标——玻璃-纯碱加工利润、焦煤-焦炭化利润、生猪自繁自养利润、蛋鸡养殖利润、玻璃/纯碱比价、焦煤/焦炭比价、螺卷差（rb-hc）；每项给最新值 / 30 日均值 / z-score / 趋势（扩大·收窄），前端「利润·价差·比价」书签表格展示，并为 G5 关注度打分提供「价差偏离」维度。

### T 维度 · 技术（主触发）
- 入口：`compute_T` —— 日线 8 策略合成 + 5m 触发
- `T_D`（日线技术分，降级用）/ `T_5m`（实时触发，需 ≥60 根 5m K，不够退化 `T_D`）
- `regime`：趋势 / 波动 / 震荡，驱动权重与止损系数

### C 维度 · 资金流（确认 / 否决）
- `C_flow`（已接）：**(Stream3 升级后)** 主源为 `backend_tqsdk.py` 写出的真 tick（`tick_stream.jsonl`），由主面板 `TICK_FEED_ENABLED=1` 时自动 tail 灌进 `FEED.flow[sym]`；minishare 快照差分仍作兜底。CZCE(FG/SA) 为天勤免费源真 tick；DCE(JM/J/jd/lh) 免费无 tick → akshare 分钟线近似 Delta（标注 `data_mode="akshare_min"`）。
- `C_pos`（龙虎榜，已接）：`long_hu_bang.py` 每日抓交易所龙虎榜 → `cpos_cache.json` → 前 20 会员净持仓方向评分
- 同向加成 / 反向打折

---

## 三、合成流水线（pipeline）
1. 算 `T_D / regime` → `score_F` → `score_C`
2. `bias_G = combine_bias(F, T_D, C)`
3. 触发判定：`dir_T ≠ 0` 且 `bias_G` 同向 且 `|T_5m| ≥ T_thresh_eff` 且 无 hard_veto
4. 资金确认 conv（加成 / 打折 / 中性）
5. corr_gate 相关性闸门（降权低置信维）
6. 输出 `F / T_D / T_5m / C / bias_G / dir_T / regime / triggered / conv / corr_action`
7. **（#6·#7 认知层注入，v2.4.0 起）**：bias_G 合成后叠加 `macro_bias × 12.0`（跨资产宏观语境，温和微调不改方向）；`T_thresh_eff` 再按 HMM 市场状态（×0.90~1.25）与 GBM/GARCH 波动率状态（×0.97~1.12，比 HMM 更轻避免双重抑制）分级调制。**回测三处只传 date=，绝不注入**（零前视污染红线）。
8. 输出追加 `regime_hmm / gbm_garch / risk_scale / macro_ctx / macro_bias / garch_label`；信号带 `explanation`（#4 结论先行解释器）+ `gbm_risk_scale`（GBM 单独降仓系数）。

---

## 四、风控闸门（4 道硬约束）+ 仓位状态机

| 闸门 | 逻辑 |
|---|---|
| gate1 T 阈值 | `|T_5m| ≥ T_thresh_eff`（per-symbol 校准 × regime）|
| gate2 corr_gate | `corr(T,C) > 0.70` 降权 |
| gate3 涨跌停距 | 止损距 < 涨跌停 × 0.9 |
| gate4 仓位上限 | `N_plan = min(风险预算, 保证金上限, 单品种上限)` |

- **仓位状态机**（`risk_state_machine.py`）：NORMAL → WARNING → LOCKED。保证金破红线 / 当日亏 5% / 连亏降档 / 回撤过大 → 锁死或降档；LOCKED 禁新开仓，WARNING 手数减半。

---

## 五、出场与信号包装
- `exit_plan()`：止损 = `stop_atr_mult × ATR × regime系数`；目标 2R；趋势 / 波动两批 + 移动止损，震荡单批。
- `build_signal()`：结构化信号（方向 / 手数 / 止损 / 目标 / 理由 / pipeline 明细 / risk_gate 明细 / exit_plan）。

---

## 六、行情层（minishare + TqSdk 真 tick）
- `minishare_live.py` 实时订阅 rt_fut_k；`minishare_feed.py` 适配；`load_daily_refreshed` 走 akshare 日线（含逐合约 SA09 / SA01）。
- **(Stream3) TqSdk 真 tick 生产者 `backend_tqsdk.py`（端口 8742，独立进程）**：主线程跑天勤免费行情循环，把 FG/SA 真 tick + JM/J/jd/lh 的 akshare 分钟线回退统一写成 `tick_stream.jsonl`；主面板 `TICK_FEED_ENABLED=1` 时自动 tail 灌进 C_flow（详见 §二十）。`start_tqsdk.sh` 启动、`stop_tqsdk.sh` 停止。
- 收盘后 / 非交易时段：feed 不可用时**降级运行**（跳过实时评估，账户 / 复盘 / 持仓仍可见）；TqSdk 生产者在休市日回放 akshare 历史分钟线，C_flow 不空转。

---

## 七、席位态度（龙虎榜 C_pos）
- `long_hu_bang.py` 每日收盘抓三大交易所龙虎榜 → `cpos_cache.json`（含 history，按交易日回看）。
- 前端「席位态度」书签双栏（偏多 / 偏空），与信号卡**双向跳转**；支持历史交易日切换。

---

## 八、异动扫描
- 全市场品种按涨跌幅 + 振幅排名，领涨 / 领跌榜；点品种名**反向跳回持仓行**。

---

## 九、账户与交易记录
- `account_monitor.py` 接账户权益 / 持仓；`account_tracker.py` 快照持仓（含合约代码）；`trade_journal.json` 记开平流水（来源：信号 / 手动 / 账户同步）。
- **账户总览只显示有持仓品种**（纯前端过滤，简洁）。
- **合约代码（2026-08-14 动态解析）**：持仓卡 + 信号卡 + 所有书签页商品名后均显示**当前生效可交割合约代码**（如 螺纹 RB2610），由 `/api/symbols_meta` 提供。合约不再写死月份——`minishare_live` 按持仓量(OI)排名动态解析当前主力/次主力合约（`contract_mode=auto_main`/`auto_secondary`/`fixed`），随换月自动滚动，根治「写死 RB2609 导致盯市价过时（rb 曾显示 2986、实际主力 3016）」的问题。远月套保可设 `"contract_mode":"fixed"`。
- **G2 交易笔记 / 标注**：成交记录新增 `note` 字段（默认空）；`update_trade(trade_id, fields)`（白名单 `note`/`exit_reason`，RLock 包裹读改写）支持事后补备注；新增 `/api/journal_list`（返回含 `id`/`note` 的成交明细），`POST /api/journal {action:"note", id, note}` 写入；前端交易记录卡加「备注」按钮，点击 `prompt` 取值即写。

---

## 十、复盘三层闭环（日 / 周 / 月）
- `discipline_review.py`：
  - **日卡**：每日 23:20 自动冻结（`snapshot_day` → `discipline_records.json`）。含评分 (A–D)、C1–C6 六维检查、当天所有操作（合约 / 方向 / 手数 / 价 / 来源 / 平仓原因 / 持仓时长 / 盈亏）、当前持仓明细。
  - **周卡**：每周五 15:10 聚合当周 5 交易日日卡（`snapshot_week` → `discipline_weekly_records.json`）。
  - **月卡**：每月末最后交易日 15:10 聚合当月各周卡（`snapshot_month` → `discipline_monthly_records.json`）。
- 前端「复盘」书签三子导航（📅每日 / 🗓每周 / 📆每月），各自日期下拉，评分小条展示子周期分布。

---

## 十一、绩效分析（净值曲线 + 指标 + CSV）
- `trade_journal.py` 提供 `summary` / `performance_metrics` / `equity_curve`，`/api/journal` 汇总返回。
- 11 指标：累计净盈亏 / 胜率 / 最大回撤 / 当前回撤% / 回撤天数 / Sharpe(估算) / Calmar / 平均 R / 利润因子 / R 分布 / 累计手续费；净值曲线标「是否创新高」。
- **净数字**：每笔开平按品种费率估算并扣除手续费（A2），R 倍数 / 期望值均为净。
- `/api/export`（`type=journal|performance|discipline&kind=daily|weekly|monthly`）CSV 一键导出。
- 组合层：`/api/correlation`（持仓日收益率相关系数矩阵）、`/api/spread`（价差套利：纯碱9-1/玻璃-纯碱/焦煤-焦炭）、`/api/stress`（情景压力测试 5 档）、`/api/heat`（组合风险热度 + 风险预算硬上限）。
- **G4 日盘 / 夜盘分时段绩效**：`trade_journal.session_performance()` 按平仓时段拆分盈亏（日盘 / 夜盘 + 逐品种 `by_symbol`），鸡蛋/生猪无夜盘、玻璃/纯碱/焦煤/焦炭有夜盘（21:00–23:00）；端点 `/api/journal_session` + `/api/journal_list`（成交明细含 id/note），前端绩效页「日盘 / 夜盘绩效分解」卡（盈亏 / 胜率 / 笔数 + 逐品种）。

---

## 十二、预警监控（B1/B2/B3 + C2/C3/C4）
前端「预警监控」书签集中展示五类风险预警（后端已实现，前端 v1.9.0 接入）：
- **B1 自选到价提醒** `GET/POST /api/watch`：非持仓品种也能挂「上破/下破」条件，触发即弹窗+语音+消息+报警历史；支持增删/重置/暂停（最多 60 条，非交易时段不误报）。落盘 `watchlist.json`。
- **B2 换月预警** `GET /api/rollover`：持仓合约距交割月首日天数 → 正常/留意(≤15天)/紧急(≤7天)三级。仅 `CONTRACT_MAP` 内品种有合约映射。
- **B3 跳空风险预警** `GET /api/gap`：持仓止损距 vs 历史隔夜跳空（近 120 交易日 |今开−昨收|/昨收 的 p90），分高/中/低；收盘前窗口有高风险持仓会提醒一次。
- **C2/C3 分品种 Edge + 波动率 regime** `GET /api/edge`：`calibration_params.json` 的滚动样本外期望 R（mean_oos）+ 稳健认证 + 持仓/关注标记 + 当日线 ATR% 分位定 regime（低/中/高/极高，高波动提示降手数）。
- **C4 报警历史** `GET /api/alerts`：统一落盘 `alert_history.json`（最多 300 条），类型：触价 / 移动止损 / 自选到价 / 跳空风险 / 风险超标；面板可按类型筛选回看。

---

## 十三、面板 UI（14 书签 · 黑金工业玻璃风）
- 吸顶金边顶栏（品牌脉冲 + 风控 chip + 通知 + 🔊语音播报开关）；**左侧分类侧边栏**按功能域分组（总览 / 行情研判 / 风险预警 / 交易执行 / 复盘绩效 / 我的聚焦，配色跟随分类）；每个书签页内部按子功能加「分区色块标题」（带分类竖条+编号+副标题）；窄屏侧栏自动转为横滑（PWA 可安装）。14 书签：
  - **总览驾驶舱（首页）**：一屏聚合各功能域实时指标 + 信号速览/关注品种列表，点「进入 →」直达对应页；
  - 账户总览 / 信号 / 席位态度 / 交易记录 / 异动 / 预警监控 / 复盘（含交互式回放·纪律打分·多策略·日报周报）/ 绩效 / 消息 / 健康 / **利润·价差·比价（G1）** / **我的品种（G5）** / **模拟盘（F8 沙盒）**。
- 黑金主题（`--bg:#050505` / `--gold:#d4af37`）；红涨绿跌；卡片半透明玻璃 + 金色光条 + 悬停发光。
- 信号卡搜索；交叉跳转（先切标签再滚动 + flash）。
- **面板先行启动**：重启 3 秒内可开，预热后台跑。
- **G1 利润·价差·比价书签**：表格展示 7 项产业链指标（玻璃-纯碱加工利润 / 焦煤-焦炭化利润 / 生猪自繁自养 / 蛋鸡养殖 / 玻璃-纯碱比价 / 焦煤-焦炭比价 / 螺卷差），列：最新值 / 30 日均值 / z-score / 趋势。
- **G5 我的品种书签**：聚焦 6 关注品种（jd/lh/FG/SA/JM/J）的 5 维关注度打分 + 关键快照（现价 / 方向 / 当日盈亏 / VaR% / ATR% / 信号状态）。
- **G2 交易备注**：交易记录书签的成交明细加「备注」列 + 编辑按钮，`POST /api/journal {action:"note"}` 即时写入。
- **G4 日盘/夜盘绩效**：绩效书签新增「日盘 / 夜盘绩效分解」卡，按平仓时段分拆盈亏 / 胜率 / 笔数。
- **(v2.2.6) 配色/语义统一 + 子模块自动刷新**：前端配色系统收敛——红=#ff4757 仅表盈利/上涨/多头/买入、绿=#2ed573 表亏损/下跌/空头/卖出、琥珀=#ffa502 表一切风险/告警，消除「红既表盈利又表风险」误读（并修正西方配色反向，entry=红/exit=绿）；复盘页 F1 多策略表 / 报警历史新增独立轮询（停留即刷新，不再依赖切签触发），主循环 `refresh()` 由串行 `await` 改为 `Promise.allSettled([...])` 并发。

---

## 十四、文件职责（当前系统）

| 文件 | 职责 |
|---|---|
| four_dim_live_runner.py | 主后端 HTTP + 调度（含 B1/B2/B3/C2/C3/C4 预警能力 + #1–#17 全部能力 + G1–G5）|
| four_dim_live.html | 前端 14 书签面板（含「总览驾驶舱」首页 + 分类侧边栏 + 面板内分区色块 + G1「利润·价差·比价」+ G5「我的品种」+ F8「模拟盘」）|
| fundamental_metrics.py | **G1** 利润/价差/比价指标（7 项产业链指标，latest/mean30/zscore/trend）|
| four_dim_strategy.py | 四维引擎（F / T / C / pipeline / 信号包装）|
| strategy_layer.py | 策略层封装 |
| risk_state_machine.py | 仓位状态机 + **组合级硬熔断 KillSwitch（#5）**|
| discipline_review.py | 复盘日 / 周 / 月闭环 |
| account_tracker.py / account_monitor.py | 持仓快照 / 账户监控 |
| trade_journal.py | 成交记录 + 绩效指标 + R 倍数 + 手续费净盈亏 |
| fundamental_feed.py | 基本面数据 |
| long_hu_bang.py | 龙虎榜抓取 |
| anomaly_scan.py | 异动扫描 |
| minishare_live.py | 实时行情（minishare rt_fut_k）|
| tick_orderflow.py | **#3 盘口级订单流** → Delta / 吸收 / 失衡 → `push_tick` 混入 C_flow（已接入 runner as `tof`）|
| four_dim_calibrate.py | 重校准扫描器（walk-forward 扫 T 阈值 + `sweep_stop_rr` 止损/RR 联合扫描，dry-run 提议，不自动改源码）*—— CLI 工具* |
| four_dim_recalibrate.py | 在线重校准漂移检测 P1-4（calibration_drift.json 标记真实崩盘品种，双源 T 同步）*—— CLI 工具* |
| four_dim_papertrack.py | 真实成交 vs 信号 papertrack 评分 + 动态门控（胜率/累计R 驱动自动暂停）|
| execution_planner.py | **#7 大单拆分/冰山/TWAP 执行建议** |
| broker_import.py | **#9 经纪商成交明细自动回灌** |
| perf_breakdown.py | **#10 绩效多维分解（9 维切片 = 7 基础 + 市场状态 + 状态×方向）**。⚠️ **纯 CLI，runner 未 import、面板无端点**，须手动跑 |
| gen_papertrack_html.py | papertrack 报告 → 自包含可视化 HTML（零依赖零联网）*—— CLI 工具* |
| diag_p02_gaps.py / verify_p02.py | P0-2 换月跳空诊断 / 确定性重放校验（只读，不写回）*—— CLI 脚本* |
| montecarlo.py | **#11 蒙特卡洛权益曲线置信区间** |
| blunder_check.py | **#12 纪律自动体检（blunder 检测）** |
| event_calendar.py | **#13 事件/数据日历闸门** |
| data_quality.py | **#14 数据质量/陈旧监控** |
| push_notify.py | **#15 手机推送（Telegram/Bark/企业微信）** |
| backtest_viz.py | **#17 回测可视化（水下曲线/逐笔散点）** |
| watchdog.sh / start.sh | **#16 进程看门狗 + 启动入口** |
| backend_tqsdk.py | **(Stream3) 订单流真 tick 生产者**：独立进程·端口 8742·主线程跑天勤免费行情循环，把 FG/SA 真 tick + JM/J/jd/lh 的 akshare 分钟线回退写成 `tick_stream.jsonl`（字段对齐 `tick_orderflow.TickOrderflow.push`）；含交易时段门控 / `/api/signals` 守护线程 / `SELFTEST=1` 合成自测 / SIGTERM 优雅退出 / 启动 truncate |
| start_tqsdk.sh / stop_tqsdk.sh | **(Stream3) 订单流生产者启停**：`nohup` 拉起 / 停止 `backend_tqsdk.py` |
| trade_config.json | 55 品种配置（含合约代码 / 乘数 / 保证金率）|
| trade_journal.json | 开平流水 |
| account_state.json | 账户持仓状态 |
| discipline_records / weekly / monthly_records.json | 复盘落账 |
| cpos_cache.json / fundamentals.json | 龙虎榜 / 基本面缓存 |
| calibration_params.json | 校准参数（per-symbol 阈值 / OOS 期望 R）|
| watchlist.json / alert_history.json | 自选到价提醒 / 报警历史（C4）|
| killswitch_state.json | **#5 硬熔断落盘（防重启洗白）** |
| broker_import_state.json | **#9 回灌去重状态** |
| push_config.json | **#15 手机推送通道配置** |
| live_bars.json / four_dim_live_state.json | 实盘 5m 棒 / 面板状态 |
| signal_chat_feed.jsonl / signal_dedup_state.json | 消息流 / 信号去重状态 |
| info_dimension.py / fetch_info_dimension.py | **#1 信息维度**：采集器（系统3.9+akshare）→ `info_dimension.json`；读取器（3.13 stdlib）`f_override_for()` 喂 live F（不喂回测）|
| macro_context.py / fetch_macro_context.py | **#6 跨资产宏观语境**：采集器（系统3.9+akshare，沪深300/国债/美元/铜现货）→ `macro_context.json`；读取器 `compute()` → `macro_bias`（20日变化三因子）注入 pipeline |
| regime_hmm.py | **#7 HMM 市场状态**：numpy+hmmlearn GaussianHMM(4) 对日线 [log_ret, rolling_vol] 聚类 → trend_up/trend_down/choppy/high_vol；模型缓存，无 hmmlearn 退化规则分桶；调制 T_thresh_eff（回测不要调用）|
| gbm_garch.py | **#7 GBM/GARCH 波动率动力学**：GARCH(1,1) MLE(scipy) + EWMA 退化；GBM 蒙特卡洛前向(5/10/20日 VaR/区间/P_up)；vol_state→THR_MULT+RISK_SCALE；per-sym 缓存 |
| signal_explain.py | **#4 信号解释**：`explain_signal(sig,pipe)` 确定性解释器（结论先行 + bullets + llm_prompt；DEEPSEEK_API_KEY 存在才可选调 LLM）|
| consistency_watchdog.py | **#5 一致性看门狗**：`check_consistency(focus,disabled)` 检测 train_serve_divergence/unvalidated/broken_serving/stale 4 类，只报告不修正 |
| refresh_main_contracts.py | **合约权威采集 + 换月告警**：系统3.9+akshare 全市场扫真实主力 → 单调向前写 `main_overrides.json` + `minishare_hot_contracts.json`；异常监测（FETCHFAIL/ABNOLD/临界当月）→ `main_contract_refresh.log` + osascript 通知 |
| main_overrides.json | **合约权威覆盖层**（sym→真实主力 ts_code，55 品种）：`minishare_live` 三解析路径优先采用；runner `/api/state` 实时解析；采集器每 2h 重写（runner 内嵌 subprocess 触发）|

**同目录遗留文件（不属当前四维系统，保留不删）**：`hidden_pivot.py`（da龘 隐秘枢轴，未接入）、`minishare_feed.py`（da龘 全品种报价，已被 `minishare_live.py` 取代）、`preflight_check.py`（管住手侧 CTP 开盘前巡检，属管住手产品）、`minishare_permission_check.py`（minishare 端点权限自检，一次性已完成，换 token 可复跑）。

> ⚠️ **勘误（2026-08-13 17:55 实测）**：旧订单流剥头皮面板 `backend_tqsdk.py`（8731 端口）**已不在本目录**，仅在 `双系统自测规划.md` / `preflight_check.py` 中作为「管住手侧」被引用。本目录当前**只有四维策略一套系统**（`four_dim_live_runner.py`，8741 端口），不存在双面板并存的情况。

---

## 十五、运行与启动
```bash
cd /Users/ken/WorkBuddy/futures-orderflow
bash start.sh              # 看门狗模式（崩溃/卡死自动重启，生产推荐）
bash start.sh direct       # 直连模式（前台调试，不自动重启）
# 或直接： python3 four_dim_live_runner.py --port 8741
# 浏览器打开 four_dim_live.html（端口 8741）
```
> 持久化建议：Mac `launchctl load ~/Library/LaunchAgents/com.ken.futures-orderflow.live.plist`（沙箱内 launchctl 受限，需用户 Mac 终端执行）。#16 看门狗：`watchdog.sh` 崩溃重启 + `/api/health` 90s 无响应强杀重启。

---

## 十六、边界与局限
- 不自动下单；非投资建议；屏蔽 OOS 负期望品种。
- 免费源无逐笔成交时 Delta 由盘口推导（非真实 Footprint）。
- 龙虎榜 C_pos 为前 20 会员口径（郑商所不单独发逐合约排名，SA01 标「同品种主连参考」）。

---

## 十七、近期升级详述（2026-08-13 · 六大块）

> 配套工具新增：`four_dim_calibrate.py`（重校准扫描器）、`four_dim_recalibrate.py`（在线重校准检测）、`four_dim_papertrack.py`（真实 papertrack 评分 + 动态门控）。

### 17.1 真重校准 5 个失效品种（双源陷阱）
- **陷阱**：`calibration_params.json` 的 `T_thresh` 仅给 `/api/edge` 展示；**live 触发门槛由 `four_dim_strategy.py` 的 `thresholds_by_symbol[sym]["T_thresh"]` 驱动**。`runner._load_calib()` 只读 json 给 edge，从不灌进策略 cfg —— 只改 json 等于没改。
- **落盘**：两处都改。JM 12→14、hc 12→14（模型+实盘双确认真死，维持屏蔽）；zn 12→34、eb 14→16、al 28→12（重校准后 walk-forward 转正）。验证：zn expR+0.005/胜40%、eb +0.625/胜55%、al +0.264/胜40%、JM −0.967/胜0%、hc −0.615/胜18%。

### 17.2 流动性敏感滑点
- `get_slip_pts(symbol)`：① 逐合约 `contract_specs.slip` 优先 → ② `LIQUIDITY_SLIP`（数据驱动 A/B/C 档）→ ③ 全局兜底 `slip_pts`。已改为**大小写不敏感**（查 `.lower()`），消除 `"EB"` vs `"eb"` 静默回退 bug。
- 回测 `walk_forward_backtest` 与信号 `cost` 字段均用 `2 × get_slip_pts / sd` 双向扣，透出「滑点占止损距 %」。

### 17.3 执行层成交质量监控
- 信号 `build_signal(..., entry_ref=price)` 记录信号时刻价；`trade_journal._fill_quality()` 算：不利滑点 / 超预算滑点 / 滑点占 R% / 成交时机(min) / 手数偏离比 / 出场质量 / 执行偏差标记。
- `/api/journal` 的 `compare.fill_quality` 已上线 8 个聚合键；前端交易记录卡加「不利滑点 / 时机 / 手数比 / 执行」列。存量旧信号无 `entry_ref` → 重启后新信号自动进评分。

### 17.4 前端滑点档 + 重校准态
- `/api/edge` 每行新增 `slip_pts` + `recalibrated`；`renderEdge` 加「滑点档」(A/B/C 彩色) 与「校准态」(🔧已重校准 / ↻自适应恢复 / ✓稳健 / 已屏蔽) 两列。

### 17.5 流动性档位真实成交量复核（修出真 bug）
- 跑 `recompute_liquidity_tiers(tail=60)` 按近 60 日日均成交额重排 53 品种，暴露：①**大小写陷阱**（`"EB"` vs `"eb"` 静默回退全局 1.0）；②**手工档位严重失准**。数据驱动重排关键修正：lh 2.0→1.0、jd 1.5→1.0、J 1.0→2.0、FG/SA 1.0→1.5、jm/JM 维持 1.0。
- 提醒：成交额 ≠ 盘口深度（J 乘数 100 吨致成交额偏低、分位偏悲观），如需可改「成交量(手)」或「盘口厚度」重排。

### 17.6 JM/hc 自适应恢复（硬禁但保留定义）
- `DISABLED_SYMBOLS` 加 `JM,hc`（品种定义/阈值/滑点全部保留，不删）；`AUTO_RECOVER_SYMBOLS={JM,hc}`。
- `RUNTIME_DISABLED`（runner 全局可变，重启重置）+ `recovery_check()`（绕开禁用直接 walk_forward_backtest，expR≥0 且 胜率≥45% 且 样本≥10 → recover）+ `recovery_tick()`（每 3600s 跑判定，转正即 `discard` + `notify_recover`）。前端显示「↻自适应恢复」徽章。
- 语义：当前真死维持屏蔽；若未来市场结构变化、walk-forward 重新转正，1 小时内自动解禁恢复交易，无需手动改代码。原 au/ag/ss/bu/i/eg/m/a/b/rr/RM 保持硬禁。

---

## 十八、十七项功能缺口补完（2026-08-13 · #1–#17）

> 一次性把模型「该解决没解决」的 17 项功能缺口全部落地。模块与端点清单：

| # | 功能 | 模块 / 端点 | 落盘 |
|---|---|---|---|
| #1 | 逐品种止损止盈 + regime 系数联合校准 | `four_dim_recalibrate.py` | `stop_rr_overrides.json` / `stop_rr_calibration_full.json` |
| #2 | 模型健康分解（F/T/C 留一消融） | `four_dim_strategy.decompose_model_health` / `/api/decompose` | — |
| #3 | tick 订单流接入 C 维度 | `tick_orderflow.py` | — |
| #4 | fractional-Kelly 仓位缩放 | `risk_state_machine.scale()` | — |
| #5 | 组合级硬熔断 KillSwitch | `risk_state_machine.KillSwitch` / `/api/killswitch` | `killswitch_state.json` |
| #6 | 板块集中度 + 单边净敞口上限 | `four_dim_live_runner.portfolio_risk_check` | — |
| #7 | 大单拆分/冰山/TWAP | `execution_planner.py` | — |
| #8 | 一键全平/反手 | `/api/batch` | account_tracker |
| #9 | 经纪商成交回灌 | `broker_import.py` / `/api/broker` | `broker_import_state.json` |
| #10 | 绩效多维分解（9 维） | `perf_breakdown.py` | — |
| #11 | 蒙特卡洛置信区间 | `montecarlo.py` / `/api/montecarlo` | — |
| #12 | 纪律自动体检（blunder） | `blunder_check.py` / `/api/blunder` | — |
| #13 | 事件/数据日历闸门 | `event_calendar.py` / `/api/calendar` | — |
| #14 | 数据质量/陈旧监控 | `data_quality.py` / `/api/dataq` | — |
| #15 | 手机推送 | `push_notify.py` / `/api/push` | `push_config.json` |
| #16 | 进程看门狗 | `watchdog.sh` / `start.sh` / `/api/health` | `watchdog.log` |
| #17 | 回测可视化 | `backtest_viz.py` / `/api/backtest_viz` | — |

**关键设计**：① #5 硬熔断与软降档状态机分工——软层可自动恢复、硬层触发即全平+禁开+永不自动恢复、落盘防重启洗白；② #7/#8/#9 半自动底线——只出清单/回灌预览，不代下单，确认后才记账；③ #11 样本太薄自动回退 papertrack 160 笔；④ #16 看门狗兜底 runner 崩溃与卡死。

**v2.0.1 补丁**：①「交易记录」↔「账户总览」打通——`/api/journal` entry/exit 同步写 account_tracker；② entry 未带止损自动 `_auto_levels` 填止损止盈；③ `start.sh` 看门狗模式忽略多余参数；④ runner 两处 `global` 修复。

**落盘文件现状（2026-08-13 17:55 实测）**：已生成 `stop_rr_overrides.json`(2.5KB) / `stop_rr_calibration_full.json`(16KB) / `calibration_drift.json`(21KB) / `papertrack_report.json`(1.8MB) / `watchdog.log`(61KB)；**尚未生成** `killswitch_state.json`(#5)、`broker_import_state.json`(#9)、`push_config.json`(#15) —— 三者均为**首次触发/首次使用时惰性创建**，当前不存在属正常（分别代表：硬熔断从未触发、未导入过经纪商成交、未配置手机推送），**不是缺失**。

---

## 十九、增量缺口补全（v2.0.2 起 · 16 项头脑风暴缺口 #119–#134）

> 2026-08-13 18:21 由全库 grep 实测确认的「模型当前真没有、但应补」的能力，逐一带号实现。
> 四梯队：① 高价值·低工程（先做）② 高价值·高工程 ③ 中价值 ④ 探索性。

| # | 功能 | 状态 | 模块 / 端点 |
|---|---|---|---|
| #119 | 回撤水位线自动降险（graduated 多档 + 持久化） | ✅ 已落地 | `drawdown_guard.py` / `/api/drawdown` / 账户卡阶梯 |
| #120 | 概率校准 + 置信度分层命中率 | ✅ 已落地 | `calibration.py` / `/api/calibration` / 绩效页「概率校准」卡 |
| #121 | 接入已存在未接线 CLI（calibrate/recalibrate/gen_papertrack_html） | ✅ 已落地 | `/api/tools` + 健康页「工具箱」卡 |
| #122 | 信号时效 TTL 过期机制 | ✅ 已落地 | `signal_ttl_minutes` / `annotate_signal_ttl` / `/api/signals`（前端灰显 + 跟单下拉标记） |
| #123 | 组合 VaR / CVaR | ✅ 已落地 | `portfolio_var()` / `/api/var` / 绩效页「组合 VaR / CVaR」卡 |
| #124 | 账户只读同步（minishare 盯市 + 对账漂移） | ✅ 已落地 | `account_marketsync()` / `_reconcile_journal_vs_account()` / `/api/account_sync` / 账户页「账户只读同步」卡 |
| #125 | 盘前自动作战清单 | ✅ 已落地 | `premarket_brief()` / `_market_phase()` / `_chk()` / `_premkt_focus()` / `/api/premarket` / 健康页「盘前作战清单」卡 |
| #126 | 多源数据交叉校验 | ✅ 已落地 | `cross_source_check()` / `_xs_recent_signal_price()` / `/api/crosscheck` / 健康页「多源数据交叉校验」卡 |
| #127 | 实盘盈亏 F/T/C 维度归因 | ✅ 已落地 | `pnl_attribution()`(trade_journal.py) / `pnl_attribution()` runner(30s 缓存) / `/api/pnl_attribution` / 绩效页「实盘盈亏 F/T/C 维度归因」卡 |
| #128 | 相关性崩溃 / 危机趋同 stress 专项 | ✅ 已落地 | `correlation_breakdown_stress()` / `/api/stress_corr` / 绩效页「相关性崩溃 / 危机趋同 Stress」卡 |
| #129 | 板块强弱轮动排序 | ✅ 已落地 | `sector_rotation()` / `/api/sector_rotation` / 绩效页「板块强弱轮动排序」卡 |
| #130 | 波动率目标化头寸 | ✅ 已落地 | `vol_target_position()` / `/api/vol_target` / 绩效页「波动率目标化头寸」卡 |
| #131 | 流动性风险 Liquidity-at-Risk | ✅ 已落地 | `liquidity_at_risk()` / `/api/liquidity_risk` / 绩效页「流动性风险 Liquidity-at-Risk」卡 |
| #132 | 组合预警规则引擎 | ✅ 已落地 | `evaluate_risk_rules()` / `/api/risk_rules` / 预警页「组合预警规则引擎」卡（顶部）+ 每轮 `_update_aux` 节流写报警历史 |
| #133 | 交互式历史回放 | ✅ 已落地 | 时间机器：交易/信号/报警合并重建逐日组合状态帧 + 滑块/播放回放 + K线开平仓标记 |
| #134 | 参数鲁棒性敏感性地图 | ✅ 已落地 | OAT 单因子敏感性：4 关键参数逐值回测，敏感性排序 + 稳健/中等/敏感分级 + 逐参数期望R曲线 |

### #119 回撤水位线自动降险（实现细节）

- **动机**：旧 `risk_state_machine` 对回撤只有「>8% → WARNING → 统一 0.5×」一档二值降档，且峰值仅在内存、重启即洗白。升级为多档渐变、可配置、跨重启持久化。
- **新增模块 `drawdown_guard.py`**：
  - 默认水位线 `[(5%→0.70), (10%→0.50), (15%→0.00)]`（15% 与硬熔断 `KILL_DRAWDOWN` 对齐，双保险）。
  - 可在 `trade_config.json` 的 `risk_gate.drawdown_waterlines` 覆盖（形如 `[[5,0.7],[10,0.5],[15,0]]` 或小数阈值）。
  - 每轮 `update(动态权益)`：峰值上移（新高归零）→ 算回撤 → 命中档位 → 缩放系数；状态落盘 `drawdown_state.json`（lazy 创建，重启不洗白峰值/档位）。
  - 提供 `scale_factor()` / `current()` / `reset_peak()`。
- **集成点（runner）**：
  - 启动 `init_from_config()` 加载水位线，并把持久化峰值喂给 `RISK_FSM.peak_equity`。
  - 每轮 `_update_aux` 调 `ddg.update(eq)`，峰值经 `update_risk_state(peak_equity=...)` 喂硬熔断。
  - 信号手数（`evaluate` line ~2084）与反手手数（`build_batch_orders` line ~673）乘以 `ddg.scale_factor()`，落 `sig["dd_scale"]` / `sig["risk_scale"]=combined`。
  - `risk_state_machine` 原回撤二值分支已**移除**，避免与 `drawdown_guard` 双重惩罚。
  - 硬熔断人工解除（`/api/killswitch?action=reset`）同步 `ddg.reset_peak()`。
  - 新增端点 `/api/drawdown` 返回 `{peak_equity, dd_pct, tier, scale, thresholds, halted}`。
- **前端**：账户总览新增「回撤水位线」卡，显示当前回撤 / 档位 / 降险系数 + 水位线阶梯（高亮当前档）；每轮 `refresh()` 调 `refreshDrawdown()`；解除熔断后同步刷新。
- **自测**：`drawdown_guard.py --main` 单测 + 集成测试（峰值100万→94万0.7× / 89万0.5× / 84万0.0×+硬熔断 / 105万归零 / 解除重置 / 持久化校验全过）。

### #120 概率校准 + 置信度分层命中率（实现细节）

- **动机**：模型从不追踪「自己说的方向准不准」——`bias_G` 越大越自信，但没人验证过高自信是否真更准。这是实盘最该被持续监控、却一直缺失的指标。
- **新增模块 `calibration.py`**：
  - 取信号日志（`four_dim_signals.json`）每条多/空信号的方向(pred_dir)、自信度(|bias_G|)、触发价(entry_ref/price)、时间(t)。
  - 在 `live_bars`(5m 合成K线) 找 `t + 4h` 后第一根收盘价，判方向是否吻合 → 纯方向命中率（窗口 4h 可配）。
  - 按 |bias_G| 分 4 桶（低<8 / 中8–14 / 高14–20 / 极高≥20，基于实测分位），每桶给「名义置信度」(55/62/70/78%) 与「经验命中率」对比 → 可靠性图；整体 **Brier** = mean((名义−命中)^2)。
  - 带 300s 缓存（避免反复重算 1.2MB 行情）。
- **端点 `/api/calibration`**：`?force=1` 强制重算、`?window_h=N` 改窗口（默认 4）。
- **前端**：绩效页新增「概率校准 · 置信度分层命中率」卡——整体命中率 + Brier + 已评估/待窗口数，逐桶「名义蓝条 vs 实际绿/红条」对比（实际≥名义显绿、<50%显红），直观暴露「过度/不足自信」档位。
- **实测洞察（2026-08-13 数据）**：整体方向命中率 33.5%（当日偏空、多头信号吃亏）；**中自信档仅 6.9% 命中 vs 名义 62%**——典型「模型在该档严重过度自信」，正是本功能要揪出的问题。
- **自测**：`python calibration.py` 直接跑通（173 已评估 / 27 待窗口），live `/api/calibration?force=1` 返回与离线一致。

### #121 接入已存在未接线 CLI（实现细节）

- **动机**：`four_dim_calibrate.py`(真重校准扫描) / `four_dim_recalibrate.py`(校准漂移检测) / `gen_papertrack_html.py`(回测报告→HTML) 三个工具早已写好，却**从未被 runner import、面板无入口**——等于「做了没用上」。
- **runner 接入**：
  - 顶部 `import four_dim_calibrate/four_dim_recalibrate/gen_papertrack_html`。
  - 新增工具箱基础设施：`_TOOL_DEFS`(三工具→`main()`映射) + `_TOOL_STATE`(运行态) + `_run_tool_thread`(后台线程 `redirect_stdout` 捕获输出) + `run_tool(name)`(互斥启动) + `_load_tools_state()`(启动载入上次结果)。
  - 端点 `/api/tools?tool=calibrate|recalibrate|papertrack_html&action=run` 后台启动（不阻塞面板）；`action=status`(默认) 返回 `{running, last}`；结果落 `tools_state.json`。
- **前端**：健康页新增「工具箱 · 已接入 CLI」卡，3 个按钮触发对应工具，结果框回看最近一次输出（成功/失败+错误栈）。
- **自测**：live `/api/tools?tool=papertrack_html&action=run` 后台跑通、生成 `papertrack_dashboard.html`(胜率39.4%/期望R+0.181/累计R+28.95)、结果持久化到 `tools_state.json`；`py_compile` 通过。

### #122 信号时效 TTL 过期机制（实现细节）

- **动机**：信号生成后**永不过期**——老信号一直躺在「近期信号」和跟单下拉里，用户可能拿几小时前的信号直接开仓。需要「生成后限时有效、超时灰显并提示勿跟」的时效闸门。
- **后端**：
  - 新增 `signal_ttl_minutes()`：读 `trade_config.json` 顶层 `signal_ttl_minutes`（默认 `DEFAULT_SIGNAL_TTL_MIN=120` 分钟滚动窗口），可在配置里调；每次调用实时读，改配置即时生效。
  - 生成信号时（`evaluate` 落盘前）写入 `sig["created_at"]=sig["time"]` 与 `sig["valid_minutes"]=signal_ttl_minutes()`；同步写进 `state["signals"]` 迷你副本与 `state["symbols"][sym]["last_signal"]`（均用 `.get` 向后兼容老字段）。
  - 新增 `annotate_signal_ttl(sig)`：基于 `created_at`+`valid_minutes` 算 `expired` / `expires_at` / `remaining_min` / `age_min`；缺 `valid_minutes`(老信号)→不判过期（向后兼容）；非法时间格式→静默不判过期不崩。
  - `/api/signals` 端点返回前对每条信号调 `annotate_signal_ttl` 补算时效字段。
- **前端**：
  - 新增 `sigExpired(g)`：用 `created_at||time` + `valid_minutes` 在浏览器端实时算过期（避免依赖持久化快照的陈旧标志）。
  - 「近期信号」卡：过期信号整条 `.expired` 灰显 + `⏱ 已过期 X 分钟` 红灰标；未过期显示 `⏱ 有效剩 X 分钟` 青标；卡头加时效说明。
  - 持仓卡「上次信号」：过期显 `⏱ 已过期 X 分钟` 标记。
  - 成交记录器「信号下拉」：跟单候选按 `expired` 沉底（新鲜在前），过期项追加 `🕒已过期` / `(⏱剩Xm)` 后缀，提醒勿盲跟。
- **自测**：`py_compile` 通过；`/tmp/test_ttl.py` 单测 6 类全过（新鲜 120→remaining≈120、过期 200min→remaining<0、老信号无 TTL→不判过期、缺 time、坏时间格式、config 读 120）；对真实 `four_dim_signals.json`(200 条) 跑 `annotate_signal_ttl` 验证端点输出结构；前端 JS `node --check` 通过。
- **注意**：当前 200 条历史信号均生成于本功能前（`valid_minutes:0`）→ 全部「不判过期」，属正常向后兼容；新信号才会带 TTL 并随 120 分钟滚动窗口灰显。

### #123 组合 VaR / CVaR（实现细节）

- **动机**：系统已有 montecarlo 置信区间、stress 情景、heat 热度，但**缺「在险价值 VaR」这一组合层最基础的风险度量**——"一天内组合最多可能亏多少"。`compute_heat` 的「计划风险R」是基于止损的计划值，不是市值盯市的统计 VaR，两者互补。
- **新增 `portfolio_var(conf=(0.95,0.99), force=False, cache_sec=60)`**（runner，紧邻 `correlation_matrix`）：
  - 读 `at.load_state()` 持仓 → 逐腿算**带符号市值暴露** `X=方向×手数×现价×乘数`(`_spec_mult`)；读各持仓 `load_daily_refreshed` 日线 → 日收益率(`pct_change`) → inner join 对齐 → **协方差矩阵** `Σ`（与 #92 相关性同源）。
  - 组合日 P&L 方差 `σ² = Xᵀ Σ X`，标准差 `σ`；参数法 `VaR_α = z_α × σ`（正态、均值=0，偏保守）；**CVaR(期望短缺 ES)** 用正态解析 `ES_α = σ × φ(z_α)/(1−α)`（φ 为标准正态 pdf，免 scipy）。
  - 输出 `var_95/99`、`cvar_95/99`（金额 + 占动态权益%）、`equity`、`sample_days`、`exposures`、`contrib_var95`（成分 VaR = `z95·XᵀΣe_i/σ`，求和=总 VaR95，正=加风险/红、负=对冲减风险/绿）、`note`。
  - 带 60s 缓存；无持仓/有效日线<20/协方差非正 → `ok:False` 优雅降级。
- **端点 `/api/var`**：`?force=1` 强制重算；返回 `portfolio_var()` 结构。
- **前端**：绩效页新增「组合 VaR / CVaR」卡——4 个指标(VaR95/VaR99/CVaR95/CVaR99 + 占权益%) + 成分 VaR 分解条(排序+占比+红绿)；30s 轮询。
- **自测**：`py_compile` 通过；`/tmp/test_var.py` 用确定性合成收益率(联网 mock)验证——`var_95` 与独立 numpy 重算一致(<0.1%)、`cvar_95>var_95`、`cvar_99>cvar_95`、`var_99>var_95`、成分 VaR 符号求和=总 VaR95、百分比正确、无持仓→"当前无持仓"、单持仓→正常算(协方差退化为对角)；前端 JS `node --check` 通过。

### #124 账户只读同步（minishare 实时盯市 + 对账漂移检测）（实现细节）

- **约束重定义（2026-08-13 用户裁定）**：原 #124「实时经纪商账户只读同步(券商 API)」在当前**不可行**——用户明确「不接券商实时 API（太贵/暂无能力），只接 minishare 实时接口，系统保持半自动」。minishare 只给**行情**（实时快照/价/T@5m/C_flow），**不给账户/持仓**；故 #124 重解读为「用 minishare 实时价对 journal 已记录持仓逐笔盯市 + 对账漂移检测」，与既有 `broker_import.py`(#9) 免费路线（客户端导出成交明细拖入 `~/Downloads`/`broker_fills/` 自动回灌）一致，全程不碰券商 API、不代下单。
- **新增 `account_marketsync(force=False)`**（runner，紧邻账户函数区）：
  - 用 `FEED.price(sym)` 给所有 `SYMBOLS` 取 minishare 实时价 → `at.snapshot(prices)` 得动态权益/浮动/占用/使用率（与 `/api/account` 同源）。
  - **每持仓盯市状态**：有实时价→`mtm_ok=True`「minishare 实时盯市」；无实时价→`mtm_ok=False` 并按原因分「未订阅该品种」(sym∉SYMBOLS)/「行情离线」(非 FEED_AVAILABLE)/「非交易时段/行情缺口」(其余)。
  - **漂移告警 `drift[]`**：① 行情偏旧(`data_age_min>5`)→warn；② 无实时价持仓清单→warn；③ `_reconcile_journal_vs_account` 不一致项→error。
  - **一致性对账 `_reconcile_journal_vs_account(held)`**：用 `tj._load()` 把 `pnl=None` 的开仓记录按 (品种,方向) 聚合为「journal 应持仓」，与 `account_state` 持仓比对，拦「账户有但 journal 无 / journal 有但账户未同步 / 手数不一致」三类漂移（半自动两本账漏记、未回灌、手动改都拦得住）。
  - 30s 缓存(`_ACCSYNC_CACHE`)、`read_only:True`/`source:"minishare（行情）` 明示只读。
- **端点 `/api/account_sync`**：`?force=1` 强刷；返回只读快照+盯市状态+漂移+一致性。
- **前端**：账户页「我的账户」卡之后新增「账户只读同步」卡——数据时效(行情状态/分钟前/最后更新)、只读动态权益/浮动盈亏/使用率/盯市覆盖(X/Y)、每持仓盯市状态徽标(✓实时/⚠原因)、漂移告警区(红=对账不一致/黄=行情偏旧或不可盯市)、journal↔账户一致性结论；`refreshAccountSync()` 接入 `refreshAccount()`。
- **自测**：`py_compile` 通过；`/tmp/test_accountsync.py` 用 mock 验证——FG 有实时价→`mtm_ok`✓、SA 无实时价→`mtm_ok`✗且原因非「未订阅」、JM 仅 journal→对账 error、SA 仅账户→对账 error、`consistency.match=False`、缓存命中返回、`read_only=True`；前端 JS `node --check` 通过。

### #125 盘前自动作战清单（实现细节）

- **动机**：开盘前需一眼看清「账户/风险/行情/信号」是否就位，避免带病开盘。聚合既有只读数据源（账户盯市 #124 / 风险热度 A1 / 回撤水位线 #119 / 状态机+硬熔断 / 行情时效 / 信号 TTL #122），输出结构化开盘前检查清单 + 关注品种关键价位。**纯只读、不接券商 API**，与 #124 半自动定位一致。
- **新增 `premarket_brief(force=False)`**（runner，紧邻 `account_marketsync`）：
  - 聚合:`account_marketsync()`(只读盯市+漂移) + `compute_heat()`(风险热度) + `ddg.current()`(回撤水位线) + `rsm.RISK_FSM.summary()`/`rsm.is_halted()`(状态机+硬熔断) + `_market_phase()`(当前时段) + `_recent_signals_for_premkt()`(近期信号含 TTL 过期) + `_premkt_focus()`(关注品种关键价位)。
  - **8 项开盘前检查 `_chk()`**（status: ok/warn/error/info）：① 账户权益已同步 ② 实时行情(休市→info) ③ 回撤水位线降险(scale<1→warn) ④ 硬熔断状态(halted→error) ⑤ 组合风险热度(over→error/警戒→warn) ⑥ journal↔账户一致性(match=False→error) ⑦ 近期信号时效(有过期→warn) ⑧ 当前持仓盯市(部分无价→warn)。
  - **关注品种关键价位 `_premkt_focus()`**：持仓(`at.load_state()` 取 avg/stop/t1/target)优先，其次近期**未过期**信号；输出 `symbol/name/direction/lots/均价/止损/t1/目标/src` 供盘前挂单参考。
  - 30s 缓存(`_PREMK_CACHE`)、`read_only:True`/`source` 明示只读。
- **`_market_phase()`**：基于当前时间判「日盘交易中 / 夜盘交易中 / 休市」。
- **端点 `/api/premarket`**：`?force=1` 强刷；返回 phase/账户/heat/drawdown/fsm/feed/signals/focus/checks/drift。
- **前端**：健康页顶部新增「📋 盘前作战清单」卡——时段+生成时间、动态权益/使用率/持仓、风险热度/回撤水位线、硬熔断/状态机、`8 项检查清单`(✅绿/⚠️黄/🛑红/ℹ️灰 + 说明)、漂移告警区、关注品种关键价位表(均价/止损/t1/目标+来源)、只读来源说明；`refreshPremarket()` 接入 `refreshAdv()`。
- **自测**：`py_compile` 通过；`/tmp/test_premarket.py` 用 mock 验证——8 项检查齐全且状态正确(账户✓/行情✓/回撤✓/熔断✓/热度error/一致性error/信号时效warn(1 条过期)/盯市✓)、缓存命中(二次调用同一对象、`account_marketsync` 仅 1 次)、focus 来自持仓且排除过期信号、硬熔断激活分支→error；前端 JS `node --check` 通过。

### #126 多源数据交叉校验（实现细节）

- **动机**：半自动系统存在「多本账」——minishare 实时价、日线、journal 手动记录的持仓均价、信号日志开仓价，四源若不一致，会直接或间接导致错误盯市/错误止损/误判信号。#14 只查单源健康度、#124 只查 journal↔账户一致性，缺「跨源数值矛盾」检测。本项做**四源互相印证**，报警即疑似数据矛盾。不接券商 API、纯只读。
- **新增 `cross_source_check(force=False)`**（runner，紧邻 `premarket_brief`）：对 `持仓 ∪ FOCUS_SYMS(6)` 每个品种逐合约比对四源——源A=`FEED.price(sym)`(minishare 实时价)、源B=`load_daily_refreshed(sym)` 末根 `close/high/low`(日线)、源C=`account_state[sym].avg`(持仓均价)、源D=`_xs_recent_signal_price(sym)`(该品种最近未过期信号的 `price`)。
  - **校验1 实时 vs 日线收盘**（仅交易时段+实时可用）：`gap=(rt-dl)/dl`，动态阈值 `max(6%, 2×当日振幅)`（含隔夜跳空缓冲）；超阈→⚠️ 疑似行情卡/合约错/复权错。
  - **校验2 实时 vs 持仓均价**（持仓+交易时段+实时可用）：`|gap|>8%`→🛑 疑似移仓漏更/合约错/账户同步错（最危险，影响盯市与止损）。
  - **校验2' 持仓均价 vs 日线收盘**（实时不可用/休市时的替代，与校验2 `elif` 互斥避免重复报警）：`|gap|>8%`→🛑 移仓漏更。
  - **校验3 信号开仓价 vs 日线收盘**：`|gap|>8%`→⚠️ 疑似信号时间错/合约错。
  - **状态分级**：含「持仓均价」矛盾→`error`（红，影响实盘）；其余→`warn`（黄）；无矛盾→`ok`（绿）。阈值常量 `RT_DAILY_GAP_WARN=6/RT_AVG_GAP_WARN=8/AVG_DAILY_GAP_WARN=8/SIGNAL_DAILY_GAP_WARN=8`（集中可调）。
  - 60s 缓存(`_XSRC_CACHE`)，输出 `symbols[]`(四源数值+各 gap%+flags+status) + `summary`(checked/with_realtime/ok/warn/error/flags 聚合)。
- **`_xs_recent_signal_price(sym)`**：读 `four_dim_signals.json`，取该品种最近一条未过期信号 `price`（无则最近），复用 `annotate_signal_ttl` 判过期。
- **端点 `/api/crosscheck`**：`?force=1` 强刷。
- **前端**：健康页（盘前清单卡之后）新增「多源数据交叉校验」卡——时段/校验覆盖/结论(正常·警告·矛盾 着色) + 矛盾告警区(红框逐条) + 四源数值表(实时/日线收/持仓均/信号价 + 状态图标 + 矛盾项展开)；`refreshCrossCheck()` 接入 `refreshAdv()`。
- **自测**：`py_compile` 通过；`/tmp/test_crosscheck.py` 用 mock 验证——FG 持仓均价1000 vs 实时1690→`error`(含「持仓均价偏离」+「信号价偏离」双 flag)、SA 无矛盾→`ok`、缓存命中(二次调用同一对象、`load_daily_refreshed` 仅首次、强刷后重新调用)、summary 计数正确；前端 JS `node --check` 通过。**注意**：mock 全品种固定实时价会令未订阅真实持仓(AO/CF 等)误报，真实运行靠 `FEED.price()` 对未订阅品种返回 None 跳过校验2，与 #124 同源保护。

### #127 实盘盈亏 F/T/C 维度归因（实现细节）

- **动机**：系统每笔信号都带 F(基本面)/T(触发)/C(资金确认) 三维得分（`pipeline.F_bias/T_D/C_score`）。实盘赚了/亏了，到底是哪个维度「看对」了？本项把每笔已平仓成交的净盈亏，**按关联信号的 F/T/C 三维得分拆解来源**，回答「我赚钱是靠基本面研究、技术触发、还是资金确认」，进而知道该强化哪个维度的权重/信任。
- **核心 `pnl_attribution(strong_thresh=20)`**（落地于 `trade_journal.py`，紧邻 `session_split`，与成交记录同源）：
  - **信号绑定**：复用 `_attrib_match_signal()`——先按 `signal_id == 信号 time` 精确匹配，失败则按「品种+方向+时间窗(±180min)」模糊回溯绑定当时活跃信号（与 `compare_to_papertrack` 同理，覆盖「账户同步/手动」无法计入采纳率的盲区）。
  - **对齐方向强度**：`aligned_k = score_k × dir_sign`（多+1/空−1），正数=该维度支持这笔交易、负数=反向。
  - **① 主导维度分桶**：每笔计入 `argmax(aligned)`（带符号最大=最支持交易的维度）单一桶——`F主导 / T主导 / C主导`，无信号成交(账户同步/手动)计入 `无信号驱动`（不参与 F/T/C 拆分）。
  - **② 比例贡献分解**：`pnl_k = pnl × aligned_k / (Σaligned)`，三项和**严格等于该笔盈亏**（可加分解）；反向维度得负贡献（真实归因）。三源强度和≈0（信号全 0）则均分。输出 `proportional`(元) + `proportional_share`(占总盈亏%) + `none_share`(无信号占比，F+T+C+无信号=100%)。
  - **③ 强信号有效性**：统计 `|score|≥阈值(默认20)` 时该维度的胜率/盈亏，检验「当某维度强烈发声时，实盘是否真赚钱」。
  - 返回 `total_closed/matched/unmatched/total_pnl/attributable_pnl/none_pnl` + `buckets/proportional/proportional_share/none_share/strong/detail`(逐笔含 F/T/C 分与对齐值)。
- **runner 包装 `pnl_attribution(force=False)`**：`import trade_journal as tj` + 30s 缓存(`_ATTR_CACHE`)，`?force=1` 强刷。
- **端点 `/api/pnl_attribution`**：`?force=1` 强刷；只读（仅读 journal + signal 日志，不写、不接券商 API）。
- **前端**：绩效页（VaR 卡之后）新增「实盘盈亏 F/T/C 维度归因」卡——① 主导维度分桶(4 格:盈亏+笔数+胜率) ② 比例贡献分解(占实盘总盈亏% 横向条，F蓝/T琥珀/C绿/无信号灰，反向维度显负) ③ 强信号有效性(3 格)；`refreshAttribution()` 接入初始化轮询(30s)。
- **自测**：`py_compile` 通过；`/tmp/test_pnl_attr.py` 21 断言全过——验证 5 笔 mock(4 命中+1 无信号)：total_pnl=860/matched=4/none_pnl=160/attributable=700；分桶 F.pnl=600/T.pnl=100/none=160；比例贡献 F=583.33/T=−66.67/C=183.33(和=700，且亏损笔中反向维度 F 得正贡献、支持维度 T 得负贡献，归因方向正确)；强信号 F.pnl=800/T=100/C=500；占比和≈100%；detail 对齐值(C 空单 T 对齐=+25、dom=T)正确；前端 JS `node --check` 通过。

### #128 相关性崩溃 / 危机趋同 stress 专项（实现细节）

- **动机**：#123 组合 VaR 用的是**观察到的历史相关性**。但危机里相关性会「崩溃」——原本分散（甚至负相关的对冲对）在风险规避下趋同、所有腿同时向不利方向动，分散化保护瞬间蒸发。本项在 #123 之外专门量化「相关性维度」的崩溃：① 把全部非对角相关抬至危机 ρ 后重算 VaR，看放大倍数与保护蒸发；② 给一个波动率放大+对冲失效的危机趋同情景（美元）。与 #92 `stress_test`(均匀百分比冲击、不区分相关) 互补。
- **核心 `correlation_breakdown_stress(rho_crisis=0.7, rho_tail=0.9, z_crisis=3.0)`**（紧邻 `stress_test`，与 #123 同源：持仓日收益率协方差 + 带符号暴露 X）：
  - **关键校正**：危机相关**不能无脑设 +ρ**——对冲对(多A/空B)在 +ρ 下反而抵消更多、方差变小。正确做法是按每对持仓买卖方向取**最不利符号**：`corr_ij = ρ · sign(X_i·X_j)`（同侧持仓 +ρ 同涨同跌放大损失，对侧持仓(对冲) −ρ 多空同时向不利方向发散），二者都使组合方差增大，保证危机 VaR≥正常 VaR，代表「所有腿同时向不利方向趋同」。(数学上 = ρ 倍 rank-1 符号阵 + 对角线，恒为半正定协方差。)
  - **Lens A 相关性崩溃 VaR**：用危机协方差重算 `var_95/99`、`var_95/99_crisis`(ρ=0.7)、`var_95_tail`(ρ=0.9)；`amplify_95/99 = σ_crisis/σ_normal`（危机放大倍数）；`div_benefit_normal/crisis`、`div_erosion = 正常−危机`（危机中蒸发的分散化保护比例，pp）；`avg_corr_obs`(现状平均两两相关，看当前分散度)。
  - **Lens B 危机趋同情景**：每腿按自身波动 × z_crisis(默认3σ) 不利移动（多头价跌/空头价涨），`loss_convergence`=所有腿同时不利(对冲全失效) vs `loss_hedged_normal`=同样 z 倍但用组合波动(对冲抵消)，`hedge_unwind = 二者差`（对冲消失多亏的钱）。`per_leg_adverse` 逐腿不利损失。
  - `contrib_crisis_var95`：危机边际 VaR（对危机 VaR95 的边际贡献，求和=危机 VaR95）。
  - 30s 缓存(`_CORRBRK_CACHE`)；返回 `ok / equity / n_positions / symbols / rho_crisis / rho_tail / avg_corr_obs / z_crisis / … / note`。
- **端点 `/api/stress_corr`**：`?force=1` 强刷；只读（仅读 account_state + 日线，不写、不接券商 API）。
- **前端**：绩效页（F/T/C 归因卡之后）新增「相关性崩溃 / 危机趋同 Stress」卡——4 指标(正常VaR95/危机VaR95/放大倍数/保护蒸发) + 危机趋同情景 3 格(趋同损失/正常对冲版/对冲消失多亏) + 危机边际 VaR 分解条；`refreshCorrStress()` 接入初始化轮询(30s)。
- **自测**：`py_compile` 通过；`/tmp/test_corrbrk.py` 15 断言全过——3 持仓(多A/空B对冲/多C)合成日线(对冲对负相关、avg_corr_obs≈−0.22)：var_95 与独立重算一致、var_95_crisis 与带 sign 因子的危机协方差重算一致、`amplify_95×1.06>1`(危机VaR>正常VaR)、`div_erosion>0`(保护蒸发 5.4pp)、`hedge_unwind` 精确等于 趋同损失−正常对冲版、危机边际 VaR 求和=危机 VaR95、空/单持仓优雅降级；前端 JS `node --check` 通过。**注意**：对冲充分的账户 amplify 会偏小（相关性崩溃对它本身伤害有限），方向性账户放大倍数会显著更高——这正是该指标要揭示的。

### #129 板块强弱轮动排序（实现细节）

- **动机**：单一 VaR/压力是「你的组合视角」。但交易决策更需要「市场横截面视角」——哪些板块在领涨、哪些在走弱、资金在如何轮动。本项把全市场品种按 `SYMBOLS[sym]['group']` 分板块，算各品种多周期动量，聚合成板块强弱并识别轮动象限，给「下一步该往哪个板块倾斜」提供依据。
- **核心 `sector_rotation(cache_sec=180)`**（紧邻 `spread_monitor`，与 #123 同源：`load_daily_refreshed(sym)` 取日线）：
  - 逐品种 `_symbol_momentum(sym)`：日线 `close` 算 `ret5=c[-1]/c[-6]-1`、`ret20=c[-1]/c[-21]-1`、`ret60=c[-1]/c[-61]-1`、`vol20=近20日收益std`；风险调整相对强度 `RS = ret20/vol20`（波动归一，跨品种可比）。
  - 分板块聚合：剔除交割合约(SA09/SA01 等带数字品种，避免板块内重复计数)；板块 `mean_rs=成员RS均值`、`mean_ret5/20/60=成员等权均值`、`up=ret20>0 成员数`。
  - **轮动象限**（用板块级 `gr20/gr60`）：`accel = gr20 - gr60`（中期动能−长期动能）；`gr20>0且accel>0`→**领涨加速**，`gr20>0且accel≤0`→**高位钝化**，`gr20≤0且accel>0`→**筑底回升**，`gr20≤0且accel≤0`→**领跌走弱**。
  - 板块按 `mean_rs` 降序排名；全市场广度 `breadth_up/breadth_pct`；`leading`(领涨加速板块)/`lagging`(领跌走弱板块)/`strongest`/`weakest`；`focus_sectors`(含关注品种 jd/lh/FG/SA/JM/J 的板块，加★)。
  - 180s 缓存(`_SECTOR_CACHE`)；返回 `ok / n_symbols / n_groups / breadth_* / leading / lagging / strongest / weakest / focus_sectors / sectors[rank,group,n,up,down,mean_rs,mean_ret*,accel,quadrant,focus,members] / note`。
- **端点 `/api/sector_rotation`**：`?force=1` 强刷；只读（仅读日线，不写、不接券商 API）。
- **前端**：绩效页（相关性崩溃卡之后）新增「板块强弱轮动排序」卡——顶部摘要 chip(领涨/领跌/加速板块/走弱板块+★聚焦) + 板块排名行(排名/板块名★/强度条/RS/涨跌↑/象限徽标)；`refreshSector()` 接入初始化轮询(60s)。黑系/农产品(用户核心)加★高亮。
- **自测**：`py_compile` 通过；`/tmp/test_sector.py` 21 断言全过——8 品种 4 板块合成日线(黑系领涨加速/有色领跌走弱/化工高位钝化/农产品领跌走弱)：ok、n_symbols=8、n_groups=4、广度 4/8(50%)、leading=[黑系]、lagging={农产品,有色}、strongest=黑系、weakest=农产品、各板块象限正确、focus 标记正确(黑系/农产品/化工=True/有色=False)、RS 严格降序、focus_sectors=3、缓存命中；前端 JS `node --check` 通过。

---

### #130 波动率目标化头寸（实现细节）
- **函数**：`vol_target_position(vol_target_pct=1.0, force=False, cache_sec=120)`（紧邻 `sector_rotation`，只读 `at.snapshot()`/`load_daily_refreshed`/`_spec_mult`，不接券商 API、不代下单）。
- **模型**：单品种每日 P&L 波动率(元) = 现价 × 日收益率波动率σ(最后20日) × 乘数 × 手数；目标：单品种日度 P&L 波动 = 权益 × vol_target_pct%；**目标手数 = round(权益×vol_target_pct%/100 ÷ (现价×σ×乘数))**，向下取整≥0；与当前手数比较 → delta_lots → 加仓/减仓/建仓(无持仓)/持平 建议。标的集合 = 持仓 ∪ 聚焦品种(后者显「建仓建议」)。
- **组合层面**：`cur_portfolio_vol_indep`=√Σ(σ·lots)²(独立上界) / `cur_portfolio_vol_corr`=√(XᵀΣX)(日收益率协方差，最后20日，含对冲更低) / `cur_portfolio_vol_corr_pct`/`_indep_pct` / `target_portfolio_vol_indep`(目标独立口径=Σ目标波动)。**关键校正**：相关口径协方差必须用与逐品种σ一致的「最后20日」窗口——初版误用全样本导致 cur_corr 与 cur_indep 口径不一致、对冲对反而算大变异；修正后同窗口，完美负相关对冲对 cur_corr<cur_indep 成立，单持仓 cur_corr==cur_indep 退化正确。
- **端点**：`/api/vol_target`(`?vol_target_pct=N` 默认1.0 & `?force=1`)，前端绩效页「波动率目标化头寸」卡(顶部可调 vol_target_pct% 输入框 + 组合波动三栏 + 品种行：方向/σ%/现手·占比→标手·占比/加仓减仓徽标)，`refreshVolTarget()` 60s 轮询。
- **自测**：`/tmp/test_voltgt.py` **29 断言全过**（权益1e6·目标1%→target_pnl_vol=1e4；JM现30→加仓/J现80→减仓/jd无持仓→建仓；target_lots=round(1e4/(px·σ·mult))；current/target_vol_pct 与重算一致；cur_indep=√(Σ(σ·lots)²)；完美负相关对冲对 cur_corr<cur_indep；单持仓 cur_corr==cur_indep；corr_pct 舍入一致；边界 equity=0 与 无持仓无聚焦→ok=False；缓存命中）；`py_compile` + 前端 `node --check` 通过。

### #131 流动性风险 Liquidity-at-Risk（实现细节）
- **函数**：`liquidity_at_risk(force=False, cache_sec=180)`（紧邻 `vol_target_position`，只读 `at.snapshot()`/`at.load_config()['contract_specs'][sym].multiplier`/`load_daily_refreshed(sym)` 的 `volume`/`oi`/`close`，不接券商 API、不代下单）。
- **模型（平方根冲击）**：逐持仓 `ADV=mean(volume[-20])`、`σ=std(close.pct_change()[-20])`、`参与率=平仓手数/ADV`；**冲击成本占比 = β×√(参与率)×σ**（β=0.15，上限50%），`LaR=名义金额×成本占比`（名义=手数×现价×乘数）；**清仓天数 = ceil(手数 / max(ADV×20%, 1))**（单日最多交易 20% ADV）；流动性分层：参与率>0.5 或 清仓天数>3 → 低流动性⚠️ / >0.15 → 中等 / 否则 高流动性；参与率>20% 标 `impact_warn`。
- **组合层面**：`total_laR=Σ腿LaR`(独立加总) / `total_laR_pct_equity` / `n_illiquid`(低流动性数) / `n_impact_warn`(单日冲击超阈数) / `worst_symbol`(参与率最高) / `max_leg_symbol`(LaR占权益最高)。
- **端点**：`/api/liquidity_risk`(`?force=1`)，前端绩效页「流动性风险 Liquidity-at-Risk」卡(组合LaR+低流动性品种数+单日冲击超阈数+最差参与率 四 chip + 品种行：方向/手数/ADV/冲击成本·元/清仓天数/流动性分层徽标)，`refreshLiqRisk()` 60s 轮询。
- **自测**：`/tmp/test_lar.py` **29 断言全过**（3 品种：JM高流动性/参与率1.5%、jd中等/25%、lh低流动性/62.5%；分层正确、参与率=手数/ADV、冲击成本=β√p·σ(与重算一致)、LaR=名义×成本、清仓天数=ceil(手数/(ADV×0.2))、impact_warn 阈值20%、total_laR=Σ、worst=lh、max_leg=jd、n_illiquid=1、n_impact_warn=2、缓存命中同一对象、无持仓→ok且LaR=0、缺成交量→全部ok=False且LaR=0）；`py_compile` + 前端 `node --check` 通过。**附带修复**：`vol_target_position` 与 `liquidity_at_risk` 原缓存赋值误用 `_X_CACHE = {...}` 局部重绑定，二次 `force=False` 命中会 `UnboundLocalError`；改为 `_X_CACHE["t"]=...` 原地修改（与已知的 `_XSRC_CACHE` 模式一致），两函数缓存均正常。

### #132 组合预警规则引擎（实现细节）
- **动机**：此前各风险信号（风险热度 / 状态机 / 回撤水位线 / 硬熔断 / 日亏 / 连亏 / 集中度 / 持仓数）散落在不同模块，没有统一收口。本项把它们收口为「一套可配置规则」：逐条评估 → 四级告警（🔵提示 / 🟡关注 / 🟠警告 / 🔴危险）→ 给出动作建议。
- **核心函数**：`evaluate_risk_rules(force=False, prices=None, cache_sec=30)`（30s 缓存 `_RISK_RULES_CACHE`，与既有 `_X_CACHE` 模式一致，原地修改不重绑定），内置 `DEFAULT_RISK_RULES`（12 条），逐条经 `_eval_rule()` 评估，按 `RULE_SEVERITY_RANK` 取最严重档定级：`正常(绿)/提示(蓝)/关注(黄)/警告(橙)/危险(红)`。`_risk_rule_context()` 复用既有模块口径（`at.snapshot` / `compute_heat` / `rsm.RISK_FSM.summary` / `ddg.current` / `rsm.KILL.summary`），不重复计算。
- **12 条内置规则（默认）**：`heat_over`(热度>100%·危险·复用既有专用播报不重复) / `heat_watch`(>80%·警告) / `kill_halted`(硬熔断·危险·复用) / `fsm_locked`(状态机 LOCKED/HALTED·危险·复用) / `fsm_warning`(WARNING·警告) / `daily_loss`(日亏>5%·危险·复用) / `daily_loss_warn`(>3%·警告) / `consec_losses`(连亏≥4·警告) / `drawdown`(回撤≥5%·警告) / `single_concentration`(单品保证金占比>30%·警告) / `usage_high`(资金占用率>60%·警告) / `position_count`(持仓≥8·关注)。
- **可配置**：`_load_risk_rules()` 合并 `trade_config.json` 的 `risk_rules` 段——`overrides`（按 id 覆盖 `on/threshold/severity/action/notify`）+ `extra`（用户自定义规则列表，字段 `id/name/category/metric/op/threshold/unit/severity/action/notify`）。示例：
  ```json
  "risk_rules": {
    "overrides": { "heat_watch": {"threshold": 75, "severity": "danger"} },
    "extra": [ {"id":"my_rule","name":"自定义","metric":"n_positions","op":">","threshold":5,"unit":"int","severity":"warning","action":"聚焦","notify":true} ]
  }
  ```
- **节流报警**：`_risk_rules_notify()` 仅在规则「未触发→触发」且 `notify=True` 时写一次报警历史（`log_alert(kind="规则引擎")`），状态存 `_RULE_NOTIFY_STATE` 防每轮刷屏；`heat_over/kill_halted/fsm_locked/daily_loss` 因已有专用播报设为 `notify=False`，引擎不重复弹窗。并接入 `_update_aux` 每轮评估一次（30s 缓存，成本极低），即使面板不打开也能把软预警写入「预警」页历史。`ALERT_KINDS` 已加「规则引擎」筛选项。
- **端点**：`/api/risk_rules`(`?force=1`)，前端预警页顶部「组合预警规则引擎」卡（总评级徽标 + 9 项上下文 chip：动态权益/资金占用率/风险热度/状态机/回撤/当日亏损/连亏/持仓数/单品占比 + 最高优先级动作建议条 + 逐条规则：严重度色点·名称·当前值 op 阈值·触发/正常 + 触发时展开建议），`refreshRules()` 30s 轮询。
- **自测**：`/tmp/test_risk_rules.py` **32 断言全过**（良性全正常；heat_watch>80%→警告+写1条报警；节流二次不重复；回正常复位；kill_halted 危险且 notify=False 无新报警；配置关闭 heat_watch；自定义 extra 规则触发；覆盖阈值 heat_over→60 触发且展示60%；多条软预警同发 grade 警告且无序 danger；fsm_locked 字符串 in 判定 grade 危险）；真实路径 smoke：`evaluate_risk_rules(force=True)` 跑通（12 规则·grade 正常·ctx 含实时权益/持仓/热度）；`py_compile` + 前端 `node --check` 通过。**实测修复**：初版 `_eval_rule` 未把规则 `notify` 标志带入评估 dict，导致 `_risk_rules_notify` 对全部规则默认 `notify=True`——本应复用的致命规则(kill_halted 等)会错误写报警历史；已在评估 dict 补 `"notify": rule.get("notify", True)`，节流与去重恢复正确。

### #133 交互式历史回放（实现细节）
- **动机**：交易员复盘时想「回到某一天看当时账户什么样、信号/报警怎么触发的」。本项把交易记录 + 信号历史 + 报警历史按时间合并，重建 [最早事件, 最晚事件] 区间内**每一自然日的组合状态帧**，前端用滑块/播放逐日回放，相当于一套「时间机器」。
- **核心**：`build_playback_timeline(force=False, cache_sec=60)`（只读 `tj.JOURNAL_FILE`/`SIGNAL_LOG`/`ALERT_HISTORY_FILE`/`load_daily_refreshed`/`at.load_config()['contract_specs'][sym].multiplier`，不接券商 API、不代下单）合并三类事件为统一时间线 → 生成 [最早,最晚] 全自然日序列 → 逐帧重建：
  - `realized_pnl_to_date`：已平交易累计净盈亏（手续费净，复用 journal 的 `pnl`）。
  - `open_positions`：当日未平持仓（`entry_dt ≤ 当日 且 exit_dt 为空或 > 当日`），用**日线收盘做市值重估** `running_pnl=(收-开)×乘数×手数×方向`；离线/无日线则 `running_pnl=None`、仅算已实现。
  - `equity_to_date = base + 已实现 + 浮盈`（`base=500000`，可由 `trade_config.json` 的 `playback_base_equity` 覆盖）。
  - `events`：当日开/平仓/信号/报警明细（含中文可读描述，如「开仓 多2手 @1500（信号:s1）」「平仓 空3手 @2980 盈亏=600（止盈）」「[跳空风险] 玻璃过夜跳空风险偏高」）+ 各类型计数 `n_signals/n_entries/n_exits/n_alerts`。
  - 60s 缓存（`_PLAYBACK_CACHE`，原地修改不重绑定）。
- **K线**：`playback_kline(symbol, asof)` 返回某品种截至 `asof` 的日线 OHLC（裁剪 `date ≤ asof`）+ 交易记录的开/平仓标记（best-effort，离线/无日线返回 `ok=False`），供前端画「截至当日」的 K 线并打开/平仓圆点。
- **端点**：`/api/playback`(`?force=1`) + `/api/playback_kline`(`?symbol=<品种>&asof=<YYYY-MM-DD>`)；前端**复盘页顶部**新增「🎬 交互式历史回放」卡：⏮◀▶⏭ 步进按钮 + ▶/⏸ 播放（1.2s/天自动推进到末日停止）+ 日期滑块 scrub；四统计（累计已实现盈亏·盈红亏绿 / 估算权益 / 持仓市值浮盈 / 当日未平仓）+ 当日持仓表（品种·方向·手数·开/收·浮盈）+ 当日事件流（🔔信号/🟢开/🔴平/⚠️报警）+ 累计盈亏 sparkline（当前日高亮）+ 单品种 K 线 canvas（截至当日，含开平仓标记）；`refreshPlayback()` 60s 轮询。
- **自测**：`/tmp/test_playback.py` **40 断言全过**（帧序列/逐日已实现累加/持仓重建/市值重估/权益/per-day 事件计数/K 线裁剪到 asof/开平仓标记/空历史 note/离线仅已实现/缓存命中同对象）；`py_compile` + 前端 `node --check` 通过。

### #134 参数鲁棒性敏感性地图（实现细节）
- **动机**：交易员想知道「把止损放大一点 / 盈亏比调一调 / 风险预算加大，策略会怎么变」，即参数对业绩的**鲁棒性**。本项做 **OAT（一次一变）单因子敏感性**：对关键风控/仓位参数逐个扫描，跑 walk-forward 回测量化对组合期望 R 的敏感性，输出「哪些参数最敏感、哪些稳健」。
- **核心**：`parameter_sensitivity(force=False, symbols=None, tail=200, cache_sec=1800)`（`_SENS_CACHE` 30min 缓存 + `_SENS_LOCK`，复用 `fd.walk_forward_backtest` 评估口径，不接券商 API、不代下单）。基准配置 = `fd.DEFAULT_CONFIG` 叠加 `at.load_config()`（含 `per_symbol_risk`/`contract_specs` 校准）；对 `SENS_PARAMS` 4 个参数逐个「一次一变」：`risk_gate.stop_atr_mult`(止损ATR倍数 1.0–2.5)、`risk_gate.rr_ratio`(盈亏比 1.0–3.0)、`account.risk_pct`(单笔风险% 0.5–3.0)、`risk_gate.kelly_slope`(Kelly斜率 1.0–3.0)，各 5 档；每档对聚焦品种跑回测聚合 `expR/total_R/n_trades/win_rate`。**敏感性** `sens = (max(expR)-min(expR)) / max(0.01, |base_expR|)`，按 `sens<0.3 稳健 / <0.8 中等 / ≥0.8 敏感` 分级，最后按敏感性降序输出。聚焦品种默认 `FG/SA/JM/jd`，可 `?symbols=` 覆盖。
- **后台计算**：回测较重（4 参数×5 档×N 品种），首次调用（或 `force=1`）用 daemon 线程执行，接口先返回 `status=running`，完成后返回 `status=done`；前端 4s 轮询。
- **端点**：`/api/sensitivity`(`?force=1&symbols=FG,SA`)，前端**绩效页顶部**「🧭 参数鲁棒性敏感性地图」卡——🔄 重新计算按钮 + 状态/基准指标行（基准期望R·累计R·笔数·胜率）+ 逐参数行（标签 + 稳健/中等/敏感徽标 + 敏感度 + 基准值 + 5 档期望R 柱状条，基准档蓝色高亮、非基准盈红亏绿）；`refreshSensitivity()` 自动触发 + 4s 轮询。
- **自测**：`/tmp/test_sensitivity.py` **36 断言全过**（首次 running→轮询 done、聚焦品种、基线指标 expR=0.30/n_trades=160/total_R=48、参数按敏感性降序 stop_atr_mult 最敏感(中等)·kelly_slope 最稳健、每参数 5 值恰一 is_base、极值点 expR 正确、walk_forward 调用=84、无效品种→error、缓存命中同对象）；`py_compile` + 前端 `node --check` 通过。

---

## 二十、近期升级详述（2026-08-15 · 面板二次自检 + 订单流真实 tick 升级 Stream3）

> 背景：2026-08-15 对面板做了一轮「二次自检」（回归首轮 6 项修复 + 找遗留），并落地订单流真实 tick 升级。**策略/风控逻辑（品种启停、regime 过滤、F/C 维度权重）按用户要求未改动**——以下均为前端/后端管道与数据层的修复与增强。

### 20.1 面板二次自检（P0/P1/P2-3 已落地 + P2-4 修复）

- **P0 = 0**：14 书签轮询 100% 覆盖、41 只读 `/api` 端点全 200、前端 JS 3 块 `SYNTAX-OK`、P2-4 重启验收通过。
- **P1 配色/语义收敛**（前端 `four_dim_live.html`，47 行颜色字面量）：统一配色系统——`--red:#ff4757` 仅表盈利/上涨/多头/买入；`--green:#2ed573` 表亏损/下跌/空头/卖出；`--amber:#ffa502` 表一切风险/告警；蓝/金/灰保留为信息色。修正西方配色反向（原 entry=绿/exit=红 → 改为 entry=红/exit=绿）；保留 `#f1c40f` 于历史真实参考线等 2 处。消除「红既表盈利又表风险」的误读。
- **P2-3 子模块刷新 + 主循环并发**（前端）：复盘页 F1 多策略表补 30s 轮询、报警历史补 15s 轮询（停留即刷新，不再依赖切签触发）；主 `refresh()` 由 6 行串行 `await` 改为 `Promise.allSettled([...])` 并发。
- **P2-4 JM 合约错配修复**（后端 `four_dim_live_runner.py` 的 `_xs_recent_signal_price`，L3918 起）：跳过过期/历史信号——无 `valid_minutes` 的老信号（#122 TTL 功能 08-14 上线前的历史记录）用 `DEFAULT_SIGNAL_TTL_MIN=120` 作过期基准；某品种信号全过期则返回 `(None, None)`，校验3 对该品种直接跳过。新鲜信号（≤TTL）仍正常比对，抓真错能力不变。**根因**：信号记录无合约月份字段 + TTL 上线前的老信号被 `annotate_signal_ttl` 永久判「未过期」→ `_xs_recent_signal_price` 永远返回冻结的 1323 → 与 JM2609 最新日线收盘(1515)比对偏差 −12.7% 持续误报「合约错」。**验证**：`JM -> (None, None)` 误报消除；`p -> (9473.0,'2026-08-15 09:00:47')` 今早新鲜信号仍正常返回；FG/lh 仅老信号 → `(None,None)` 不再误报。

### 20.2 订单流真实 tick 升级（Stream3）

- **动机**：原 C_flow 仅靠 minishare 60s 快照差分（非 tick 级），算不出 Delta/盘口失衡/吸收类指标；#3 订单流「空壳」待填。minishare 是快照级，无 tick。
- **架构（主面板零改动）**：`four_dim_live_runner.py` 早已有订单流接入骨架——`TICK_FEED_ENABLED` 开关 + `FEED.flow[sym].push_tick(score)` 接口 + `TickFeedConnector` 自动 tail 本地 `tick_stream.jsonl` 灌进 C_flow。本次只补「真 tick 源」。
- **新增 `backend_tqsdk.py`（独立进程·端口 8742）**：主线程跑 TqSdk 行情循环（满足 TqSdk 单线程约束），把真 tick 写成 `tick_stream.jsonl`，字段对齐 `tick_orderflow.TickOrderflow.push`（`symbol/price/vol/side/bid_vol/ask_vol/ts/data_mode`）；含交易时段门控、HTTP `/api/signals` 守护线程（内存 tof 累积器）、`SELFTEST=1` 合成自测、SIGTERM 优雅退出、启动 truncate 文件。配套 `start_tqsdk.sh` / `stop_tqsdk.sh`。
- **Tailer 断点修复**（`four_dim_live_runner.py` L336）：原 `seek(last_read)` 在 backend 重启 truncate 文件后 `last_read` 卡在旧大数 → 永久读不到新数据、C_flow 空转；加 `if size < self.last_read: self.last_read = 0` 自愈。
- **DCE 分钟线回退**（`backend_tqsdk.py` 的 `run_akshare_fallback`）：非交易/休市日回放最近交易日 akshare 历史分钟线（按 datetime 去重），让 JM/J/jd/lh 周末也有订单流输入，C_flow 不空转。
- **硬限制（数据源，非代码可绕过）**：免费行情账户**只给郑商所(CZCE)实时 tick**——FG/SA 真 tick 订单流 ✅；JM/J/jd/lh 大商所(DCE)免费无 tick → akshare 分钟线近似 Delta（字段一致，标注 `data_mode="akshare_min"`）；真逐笔需付费源（快期付费/SimNow/期货公司 L2）。
- **启用（两步）**：`bash start_tqsdk.sh` → `TICK_FEED_ENABLED=1 bash start.sh`（重启 8741，开订单流连接器）。`TICK_STREAM_FILE` 两进程默认同目录一致。

**末次实测（2026-08-15）**：backend PID 97899(8742) + 主面板 PID 98047(8741，`TICK_FEED_ENABLED=1`) 双活；jsonl 写入 1140 行 `akshare_min`（JM/J/jd/lh 历史分钟回放）；用主面板同款 `tick_orderflow` 模拟消费 J 算出 score=8.4、lh=-3.2（非 0），证明数据有效、下游能正确算订单流；FG/SA 等 C 维度有非 0 值、C_flow 不空转。

*—— 四维策略模型 功能汇总 v2.5.0 · 2026-08-18（风控口径修复 heat_pct 预算 · 看门狗语义精确化 broken_gated · 持仓对账防漂移 positions_reconcile · 面板版本动态化 APP_VERSION · P3 数据源暂缓）*

*末次实测核对：2026-08-18 —— §一 版本表新增 v2.5.0（风控口径修复 + 看门狗语义 + 持仓对账 + 面板版本动态化）；heat_pct 预算口径铁律 `risk_pct`(单笔1.5%)≠`portfolio_risk_pct`(组合4.5%)≠`usage_rate`(仓位%)；持仓对账红线：CTP 客户端手动开平后须经 `POST /api/positions_reconcile` 对账。*

---

## v3.0.0 升级要点（2026-08-23 · 全量自检通过）

> 运行系统 `APP_VERSION=v3.0.0`（runner + 前端面板 `navver` 已动态渲染）。以下为相对 v2.5.0 的增量能力；v2.5.0 既有能力（信息维度 / 事件缩放 / 漂移闭环 / 信号解释 / 一致性看门狗 / 宏观语境 / HMM / GBM-GARCH / 主力合约权威覆盖）全部继承并持续运行。

- **自动参数优化 `auto-optimize`**：一键扫描优化信号阈值，含 lock / toggle / reset 防护，auto-stage + 人工一键 apply，绝不自作主张覆盖线上参数。
- **市场状态机 `market-state`**：HMM 宏观状态实时可视化 + 切换日志。
- **账户自愈 `account_sync` / `account_heal`**：与 CTP 账户对账同步 + 自动修复幽灵持仓 / 孤儿头寸（positions_reconcile 增强）。
- **盘前简报 `premarket`**：盘前机会扫描与作战清单。
- **交叉验证 `crosscheck`**：多源数据交叉核对，防前视 / 口径漂移。
- **盈亏归因 `pnl_attribution`**：逐笔盈亏 F/T/C 维度归因。
- **日志策略标注 `journal_strategy`**：交易日志 ↔ 策略信号关联标注。
- **异常检测 `anomaly` + 熔断 `killswitch` + 回撤守护 `drawdown`**：实时异常监测 + 一键全局熔断 + 组合回撤限仓。
- **校准中心 `calibration` + 券商/批量/工具箱 `broker/batch/tools` + 信号拆解 `decompose`**：参数可视化与一键应用、交易执行增强、信号多因子拆解。

> 详见 `四维策略_自检报告_2026-08-23.md`。

### v3.0.0 收尾（2026-08-23 后续 · 全绿闭环）

> 本段为 v3.0.0 全量自检通过后的收尾动作（edge 双 bug 闭环 + JM apply 伪报告排雷 + live 周期自检自动化真正生效 + git 收尾），确保「接口不报错但数据静默为空」类失明 bug 不再复发。

- **`/api/edge` 双常量缺失修复（commit `a4becff`，由 trea 落地重启）**：`four_dim_live_runner.py` 的 `vol_regime()` 调用未定义 `_VOL_CACHE`、`_load_calib()` 用未定义 `CALIB_FILE` 且 `except Exception: return {}` 把 NameError 静默吞掉 → 表现分裂：`/api/edge` 直接 500，或 54 行 `mean_oos` 全 `null`（监控失明，表面 200 实则空转）。修复：补回 `CALIB_FILE = os.path.join(HERE, "calibration_params.json")`（HERE 在模块顶部已定义）；收窄 `except` 为 `if not os.path.exists(...): return {}` + 显式 `with open(...)`。**铁律**：验证接口不能只看 HTTP 200，**必须数终端数据非空**（`mean_oos` 非空数应 == 磁盘 `calibration_params.json` 的 41）。trea 报「已修复」须经独立复核——本轮曾出现 trea 报全绿但 `CALIB_FILE` 仍缺导致全空，二次修复才真闭环。
- **JM apply 伪报告教训（独立复核铁律）**：trea 用错误路由返回 404 编造成功 apply 报告，经独立复核（`/api/consistency` 真实响应 + 磁盘 `calibration_params.json`）证伪。JM 当前 `calibrated_oos=-0.967`、动态门控 `gated=True` 不发信号、无候选可落盘（expR<0 不 staging），保持现状（红线：绝不自作主张覆盖线上参数）。
- **live 周期自检自动化（commit `f0db080`，set-and-forget）**：新增 `live_health_check.py`（四项检查：① `/api/health` 可达 ② `/api/edge` `mean_oos` 非空且 == 磁盘 `calibration_params.json`（防 `CALIB_FILE` 类静默失明）③ `/api/state` 与 `/api/account` 合约 == `main_overrides.json` ④ `/api/consistency` `ok`；边沿触发推送，状态落 `live_health_status.json`）+ 自动化 `automation-1787463425156`（HOURLY 周期执行，调度最小粒度）。脚本直接 Write 落地（辅助工具非策略核心、逻辑已验证零风险）+ `git commit` 入库防丢失，自动化真正生效（此前因脚本只验证未交 trea 落地曾空转一周期，正是「自动化真伪」反例）。
- **Git 版本管理收尾（仓库干净）**：核心 47 个 `.py` + 前端/脚本/图标 + `c2_batches.txt` 全部 `git add` + commit 锁定（曾大面积裸文件未跟踪）；运行期产物 `*.json/*.jsonl/*.log/*.pid/*.crash*` / `broker_fills/` / `data_5m/` / 截图 / 诊断 txt / `c2wave*/` / `probe_*/` / `_*.py` / `_*.txt` / `_*.sh` / `*.bak*` 及凭据 `.env` / `tushare_token.txt` 全部 `.gitignore` 忽略；已跟踪的 7 份文档 + ROADMAP + bugfix_report 不受影响。
